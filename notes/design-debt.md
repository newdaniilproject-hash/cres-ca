# Аудит дизайн-боргу — vitrina
Автоматизований пошук відхилень від токенів `app/globals.css` у `app/` та `components/` (9 категорій, див. постановку задачі). Формат рядка: `- рядок N: опис`.

**Файлів з порушеннями: 76. Разом порушень: 323.**

---

## `app/m/register/register-form.tsx` — 25

**3. Відступи поза шкалою --space-***
- рядок 244: `mt-7` = 28px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-5 (24px)
- рядок 279: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

**3. Висота контролу поза шкалою --h-***
- рядок 284: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 292: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 302: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 311: inline `height: 50` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 329: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 343: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 355: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 379: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 391: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 402: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 451: inline `height: 48` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 469: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)

**4. Розмір шрифту поза шкалою --text-***
- рядок 284: `fontSize: 16` на полі імені — inline
- рядок 292: `fontSize: 16` на полі прізвища — inline
- рядок 304: `fontSize: 16` на префіксі телефону «+380» — inline
- рядок 311: `fontSize: 16` на полі телефону — inline (сусідній `height: 50` теж поза шкалою --h-*, див. категорію 3)
- рядок 329: `fontSize: 16` на полі дня народження — inline
- рядок 343: `fontSize: 16` на полі місяця — inline
- рядок 355: `fontSize: 16` на полі року — inline
- рядок 379: `fontSize: 16` на полі email — inline
- рядок 391: `fontSize: 16` на полі пароля — inline
- рядок 451: `fontSize: 16` на посиланні «Увійти» — inline
- рядок 469: `fontSize: 16` на кнопці відправки форми — inline

## `app/app/inventory/inventory-client.tsx` — 24

**3. Відступи поза шкалою --space-***
- рядок 199: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 233: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 376: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 380: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 416: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 419: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 443: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 452: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 457: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 209: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px
- рядок 216: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px
- рядок 220: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px
- рядок 249: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 252: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 255: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 258: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 261: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 264: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 289: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 293: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 301: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 305: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 314: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 393: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10

## `app/page.tsx` — 20

**1. Захардкоджені кольори**
- рядок 147: `style={{ background: '#ff5f57' }}` — імітація трафік-лайту macOS (червона крапка) в моковому вікні кабінету, hex замість токена
- рядок 148: `style={{ background: '#ffbd2e' }}` — жовта крапка того ж макета
- рядок 149: `style={{ background: '#28c840' }}` — зелена крапка того ж макета

**3. Відступи поза шкалою --space-***
- рядок 123: `pb-14` = 56px padding-bottom — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-7 (48px)
- рядок 130: `mt-5` = 20px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 135: `mt-7` = 28px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-5 (24px)
- рядок 155: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 162: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 169: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 176: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 208: `left-5` = 20px left — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 218: `mt-5` = 20px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 229: `mb-5` = 20px margin-bottom — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 229: `mt-10` = 40px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-6 (32px)
- рядок 248: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 287: `mt-7` = 28px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-5 (24px)
- рядок 301: `mt-7` = 28px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-5 (24px)
- рядок 315: `p-10` = 40px padding (все стороны) — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-6 (32px)
- рядок 347: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/m/login/login-form.tsx` — 14

**3. Відступи поза шкалою --space-***
- рядок 122: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 161: `mt-7` = 28px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-5 (24px)
- рядок 188: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

**3. Висота контролу поза шкалою --h-***
- рядок 134: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 193: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 214: inline `height: 48` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 223: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 282: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 290: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)

**4. Розмір шрифту поза шкалою --text-***
- рядок 134: `style={{ height: 52, fontSize: 16 }}` на кнопці — fontSize збігається з --text-base (inline замість класу)
- рядок 193: `fontSize: 16` на полі email — inline, збігається з базовим розміром (не порушення шкали, але не через клас)
- рядок 214: `fontSize: 16` на посиланні-кнопці — inline
- рядок 223: `fontSize: 16` на кнопці — inline
- рядок 282: `fontSize: 16` на полі пароля — inline

## `app/app/orders/[id]/order-detail.tsx` — 12

**2. Захардкоджені радіуси/тіні**
- рядок 267: `rounded-full` на індикаторі-крапці історії статусів (2×2) — доречне коло (низький пріоритет)

**3. Відступи поза шкалою --space-***
- рядок 117: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 179: `pt-5` = 20px padding-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 179: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 180: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 187: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 197: `pb-5` = 20px padding-bottom — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 197: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 267: `mt-1.5` = 6px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 275: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 147: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px
- рядок 154: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px

## `app/m/shop/page.tsx` — 12

**2. Захардкоджені радіуси/тіні**
- рядок 148: `borderRadius: 999` для 20×20 кастомного радіо-індикатора — фактично коло, як rounded-full (низький пріоритет)

**3. Відступи поза шкалою --space-***
- рядок 105: `mt-7` = 28px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-5 (24px)
- рядок 119: `mt-5` = 20px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 158: `mt-5` = 20px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 171: `mt-5` = 20px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

**3. Висота контролу поза шкалою --h-***
- рядок 85: inline `height: 56` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 112: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 162: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 183: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)

**4. Розмір шрифту поза шкалою --text-***
- рядок 112: `fontSize: 16` на полі назви закладу — inline
- рядок 162: `fontSize: 16` на полі міста — inline
- рядок 183: `fontSize: 16` на кнопці «Створити заклад» — inline

## `app/account/page.tsx` — 10

**3. Відступи поза шкалою --space-***
- рядок 43: `pt-10` = 40px padding-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-6 (32px)
- рядок 47: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 56: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 67: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 70: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 92: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 95: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 49: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px
- рядок 58: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/inventory/reorder/reorder-client.tsx` — 10

**3. Відступи поза шкалою --space-***
- рядок 72: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 98: `pt-5` = 20px padding-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 98: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 101: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 109: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 114: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 126: `pb-5` = 20px padding-bottom — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 126: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 76: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px
- рядок 103: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px

## `app/app/inventory/receipts/[id]/receipt-detail.tsx` — 9

**3. Відступи поза шкалою --space-***
- рядок 159: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 178: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 280: `pt-5` = 20px padding-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 280: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 288: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 298: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 318: `pb-5` = 20px padding-bottom — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 318: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 185: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px

## `app/app/catalog/offering-form.tsx` — 8

**3. Відступи поза шкалою --space-***
- рядок 485: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 499: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px
- рядок 504: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px
- рядок 510: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px
- рядок 516: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px
- рядок 660: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 682: `h-8` = 32px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 803: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px

## `app/app/settings/settings-client.tsx` — 8

**3. Відступи поза шкалою --space-***
- рядок 70: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 142: `p-5` = 20px padding (все стороны) — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 146: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 156: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 79: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 177: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 212: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10

**9. Список без стану "порожньо"**
- рядок 145: `team.map(...)` рендерить список без гілки на порожній масив — немає повідомлення на кшталт «команда порожня»

## `app/app/finance/finance-client.tsx` — 7

**3. Відступи поза шкалою --space-***
- рядок 159: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 250: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 260: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 280: `h-8` = 32px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 282: `h-8` = 32px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 308: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px
- рядок 310: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px

## `app/app/inventory/counts/counts-client.tsx` — 7

**3. Відступи поза шкалою --space-***
- рядок 120: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 156: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 163: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 201: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 206: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 213: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 124: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px

## `app/app/journals/journals-client.tsx` — 7

**3. Відступи поза шкалою --space-***
- рядок 170: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 197: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 261: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 310: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 343: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 173: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 205: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10

## `components/offline.tsx` — 7

**2. Захардкоджені радіуси/тіні**
- рядок 97: `borderRadius: 999` для 24×24 значка статусу мережі — коло (низький пріоритет)

**3. Відступи поза шкалою --space-***
- рядок 102: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 130: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

**4. Розмір шрифту поза шкалою --text-***
- рядок 97: `fontSize: 12` на бейджі-іконці статусу мережі — збігається з --text-xs, inline

**6. Зона натискання менше --tap-min (44px)**
- рядок 111: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 116: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 138: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10

## `app/app/inventory/counts/[id]/count-detail.tsx` — 6

**3. Відступи поза шкалою --space-***
- рядок 166: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 181: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 227: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 244: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 286: `pb-5` = 20px padding-bottom — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 286: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

## `app/app/inventory/movements/movements-client.tsx` — 6

**3. Відступи поза шкалою --space-***
- рядок 158: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 268: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 271: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 278: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 286: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 163: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px

## `app/t/[slug]/page.tsx` — 6

**2. Захардкоджені радіуси/тіні**
- рядок 76: `borderRadius: '50%'` для аватара-заглушки магазину — коло, як rounded-full (низький пріоритет)

**3. Відступи поза шкалою --space-***
- рядок 75: `mb-5` = 20px margin-bottom — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 98: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 101: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 107: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/(auth)/google-button.tsx` — 5

**1. Захардкоджені кольори**
- рядок 30: hex `#4285F4` в SVG-іконці Google — бренд-колір, не з токенів (низький пріоритет: офіційна іконка провайдера)
- рядок 31: hex `#34A853` в SVG-іконці Google — бренд-колір (низький пріоритет)
- рядок 32: hex `#FBBC05` в SVG-іконці Google — бренд-колір (низький пріоритет)
- рядок 33: hex `#EA4335` в SVG-іконці Google — бренд-колір (низький пріоритет)

**3. Відступи поза шкалою --space-***
- рядок 57: `my-5` = 20px margin-block — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

## `app/app/inventory/recipes/recipes-client.tsx` — 5

**3. Відступи поза шкалою --space-***
- рядок 113: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 156: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 163: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 182: `pb-5` = 20px padding-bottom — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 195: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

## `app/app/page.tsx` — 5

**6. Зона натискання менше --tap-min (44px)**
- рядок 53: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 62: `h-8` = 32px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 93: `h-8` = 32px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 116: `h-8` = 32px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/m/code-input.tsx` — 5

**2. Захардкоджені радіуси/тіні**
- рядок 126: `borderRadius: 2` — літеральне значення поза шкалою --radius-* (control/card/hero), для курсору поля коду

**3. Відступи поза шкалою --space-***
- рядок 48: `gap-1.5` = 6px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

**3. Висота контролу поза шкалою --h-***
- рядок 58: inline `height: 48` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)

**4. Розмір шрифту поза шкалою --text-***
- рядок 59: `style={{ fontSize: 22 }}` — збігається з --text-xl, але inline замість класу
- рядок 109: `style={{ fontSize: 16 }}` — збігається з --text-base, inline замість класу

## `app/search/page.tsx` — 5

**3. Відступи поза шкалою --space-***
- рядок 58: `pt-10` = 40px padding-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-6 (32px)
- рядок 60: `left-5` = 20px left — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 80: `pb-10` = 40px padding-bottom — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-6 (32px)
- рядок 106: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `components/shell.tsx` — 5

**3. Відступи поза шкалою --space-***
- рядок 48: `mt-20` = 80px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-8 (64px)
- рядок 49: `py-10` = 40px padding-block — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-6 (32px)
- рядок 55: `gap-x-5` = 20px gap по X — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 24: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px
- рядок 34: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px

## `app/app/catalog/catalog-client.tsx` — 4

**3. Відступи поза шкалою --space-***
- рядок 56: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 120: `gap-1.5` = 6px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 120: `mt-1.5` = 6px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 74: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10

## `app/app/customers/page.tsx` — 4

**3. Відступи поза шкалою --space-***
- рядок 30: `mb-5` = 20px margin-bottom — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 52: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 55: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/inventory/barcodes/barcodes-client.tsx` — 4

**3. Відступи поза шкалою --space-***
- рядок 129: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 163: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 168: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 187: `pb-5` = 20px padding-bottom — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

## `app/app/inventory/receipts/receipts-client.tsx` — 4

**3. Відступи поза шкалою --space-***
- рядок 86: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 158: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 164: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 91: `h-10` = 40px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary` (базово 44px, h-11) до 40px

## `app/app/techcards/techcards-client.tsx` — 4

**3. Відступи поза шкалою --space-***
- рядок 167: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 316: `pl-5` = 20px padding-left — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 169: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10
- рядок 293: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10

## `app/m/oauth.tsx` — 4

**1. Захардкоджені кольори**
- рядок 116: hex `#4285F4` в SVG-іконці Google — дублює той самий логотип, що й (auth)/google-button.tsx (низький пріоритет, бренд-колір)
- рядок 117: hex `#34A853` в SVG-іконці Google (низький пріоритет)
- рядок 118: hex `#FBBC05` в SVG-іконці Google (низький пріоритет)
- рядок 119: hex `#EA4335` в SVG-іконці Google (низький пріоритет)

## `app/m/page.tsx` — 4

**3. Висота контролу поза шкалою --h-***
- рядок 61: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)
- рядок 68: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)

**4. Розмір шрифту поза шкалою --text-***
- рядок 61: `fontSize: 16` на кнопці «Створити акаунт» — inline
- рядок 68: `fontSize: 16` на кнопці «Увійти» — inline

## `components/theme.tsx` — 4

**2. Захардкоджені радіуси/тіні**
- рядок 55: Tailwind-клас `rounded-full` на контейнері перемикача теми — не з --radius-*, але це навмисне коло-пігулка (низький пріоритет)
- рядок 68: `rounded-full` на кожній кнопці перемикача теми — те саме коло (низький пріоритет)

**3. Відступи поза шкалою --space-***
- рядок 55: `gap-0.5` = 2px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 55: `p-0.5` = 2px padding (все стороны) — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

## `components/toast.tsx` — 4

**2. Захардкоджені радіуси/тіні**
- рядок 142: `borderRadius: 999` для 22×22 значка типу тосту — коло (низький пріоритет)

**3. Відступи поза шкалою --space-***
- рядок 132: `p-3.5` = 14px padding (все стороны) — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-3 (12px)

**4. Розмір шрифту поза шкалою --text-***
- рядок 142: `fontSize: 12` на бейджі-іконці тосту — збігається з --text-xs, inline

**6. Зона натискання менше --tap-min (44px)**
- рядок 162: кнопка закриття тосту: `.btn-icon` (гарантує 44px) перебито `style={{ minWidth: 28, minHeight: 28 }}` — цільова область 28px

## `app/app/inventory/refs-form.tsx` — 3

**3. Відступи поза шкалою --space-***
- рядок 71: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 75: `gap-1.5` = 6px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 97: `gap-1.5` = 6px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

## `app/app/orders/orders-client.tsx` — 3

**3. Відступи поза шкалою --space-***
- рядок 94: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 116: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 126: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

## `app/auth/finish/page.tsx` — 3

**3. Відступи поза шкалою --space-***
- рядок 41: `mt-7` = 28px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-5 (24px)

**3. Висота контролу поза шкалою --h-***
- рядок 42: inline `height: 52` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)

**4. Розмір шрифту поза шкалою --text-***
- рядок 42: `style={{ fontSize: 16 }}` — значення збігається з --text-base, але заданий inline-стилем замість класу `text-base`/`.input`

## `app/t/[slug]/book/[offering]/booking-flow.tsx` — 3

**2. Захардкоджені радіуси/тіні**
- рядок 83: `borderRadius: '50%'` для іконки-галочки успіху — коло (низький пріоритет)

**3. Відступи поза шкалою --space-***
- рядок 139: `gap-0.5` = 2px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)
- рядок 139: `py-2.5` = 10px padding-block — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-2 (8px)

## `components/legal.tsx` — 3

**3. Відступи поза шкалою --space-***
- рядок 23: `pb-20` = 80px padding-bottom — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-8 (64px)
- рядок 23: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

**3. Висота контролу поза шкалою --h-***
- рядок 24: inline `height: 56` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)

## `app/app/bookings/bookings-client.tsx` — 2

**3. Відступи поза шкалою --space-***
- рядок 72: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

**6. Зона натискання менше --tap-min (44px)**
- рядок 98: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10

## `app/app/documents/documents-client.tsx` — 2

**3. Відступи поза шкалою --space-***
- рядок 181: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)
- рядок 230: `pt-2.5` = 10px padding-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-2 (8px)

## `app/m/ui.tsx` — 2

**3. Відступи поза шкалою --space-***
- рядок 53: `mt-7` = 28px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-5 (24px)

**3. Висота контролу поза шкалою --h-***
- рядок 34: inline `height: 56` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)

## `app/privacy/delete/page.tsx` — 2

**3. Відступи поза шкалою --space-***
- рядок 121: `mt-10` = 40px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-6 (32px)

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/privacy/page.tsx` — 2

**3. Відступи поза шкалою --space-***
- рядок 201: `mt-10` = 40px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-6 (32px)

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/t/[slug]/book/[offering]/page.tsx` — 2

**3. Відступи поза шкалою --space-***
- рядок 51: `pt-10` = 40px padding-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-6 (32px)

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/(auth)/auth-shell.tsx` — 1

**3. Відступи поза шкалою --space-***
- рядок 40: `mt-7` = 28px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-5 (24px)

## `app/(auth)/register/page.tsx` — 1

**4. Розмір шрифту поза шкалою --text-***
- рядок 138: `style={{ fontSize: 24, letterSpacing: '0.35em' }}` на полі коду підтвердження — 24px не входить у шкалу --text-* (12/14/16/18/22/28/36), між --text-xl (22) і --text-2xl (28)

## `app/(auth)/register/seller/page.tsx` — 1

**3. Відступи поза шкалою --space-***
- рядок 99: `gap-5` = 20px gap — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

## `app/account/security/page.tsx` — 1

**3. Відступи поза шкалою --space-***
- рядок 45: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

## `app/app/bookings/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/catalog/[id]/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/catalog/new/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/catalog/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/documents/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/finance/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/inventory/barcodes/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/inventory/container-form.tsx` — 1

**6. Зона натискання менше --tap-min (44px)**
- рядок 239: `h-9` = 36px на кнопці/посиланні — нижче --tap-min (44px); явно зменшує висоту `.btn-primary`/`.btn-secondary`/`.btn-ghost` до цього значення утилітним класом поверх базового h-11/h-10

## `app/app/inventory/counts/[id]/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/inventory/counts/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/inventory/movements/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/inventory/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/inventory/receipts/[id]/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/inventory/receipts/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/inventory/recipes/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/inventory/reorder/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/journals/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/orders/[id]/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/orders/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/settings/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/app/techcards/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/layout.tsx` — 1

**1. Захардкоджені кольори**
- рядок 30: `themeColor: '#141417'` в metadata — дублює literal значення --color-bg замість посилання на токен (технічне обмеження Next metadata API, але значення варто звірити коментарем, якщо --color-bg колись зміниться — розійдеться мовчки)

## `app/m/layout.tsx` — 1

**1. Захардкоджені кольори**
- рядок 18: `themeColor: '#141417'` — те саме дублювання --color-bg, що й app/layout.tsx

## `app/map/page.tsx` — 1

**8. Екран без стану завантаження**
- рядок 1: сторінка серверно фетчить дані (await/.from()), немає сусіднього loading.tsx і немає Suspense/skeleton у файлі — при навігації екран порожній до відповіді бази

## `app/offline/page.tsx` — 1

**3. Відступи поза шкалою --space-***
- рядок 8: `px-5` = 20px padding-inline — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-4 (16px)

## `app/privacy/legal.tsx` — 1

**3. Відступи поза шкалою --space-***
- рядок 34: `mt-10` = 40px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-6 (32px)

## `components/app-shell.tsx` — 1

**3. Висота контролу поза шкалою --h-***
- рядок 237: inline `height: 56` — не совпадает ни с одним --h-* (32/40/44/46/48), ближайший --h-lg (48px)

## `components/banner.tsx` — 1

**3. Відступи поза шкалою --space-***
- рядок 159: `mt-0.5` = 2px margin-top — вне шкалы --space-* (4/8/12/16/24/32/48/64), ближайший токен --space-1 (4px)

---

## Зведена таблиця

| Категорія | Порушень |
|---|---|
| 1. Захардкоджені кольори | 13 |
| 2. Захардкоджені радіуси/тіні | 9 |
| 3. Відступи та висоти контролів поза шкалою (--space-*, --h-*) | 186 |
| 4. Розмір шрифту поза шкалою --text-* | 27 |
| 5. Поля вводу з шрифтом < 16px | 0 |
| 6. Зона натискання менше --tap-min (44px) | 57 |
| 7. vh замість dvh | 0 |
| 8. Екран без стану завантаження | 30 |
| 9. Список без стану "порожньо" | 1 |
| **Разом** | **323** |
