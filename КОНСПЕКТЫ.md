# МОДУЛИ — переносимая система решений

Проект собран из модулей. Модуль — это законченная функция с известным
составом: файлы кода + объекты базы + ручные настройки. Модуль можно
унести в другой проект списком его состава, не разбирая чужой код.

**Правила для агента:**
1. Новая функция строится КАК МОДУЛЬ — свои файлы, свои объекты БД, свой
   раздел в этом файле (состав, зависимости, перенос, грабли). Размазанная
   по чужим файлам функция непереносима — ошибка приёмки.
2. **Один модуль за раз.** Довёл до идеала → проверил модуль И его стыки
   (список стыков — в «Зависит от») → следующий. Чужие модули не трогаются,
   если задача их не касается; вынужденная правка соседа — отдельно
   названное изменение с проверкой соседа. Цель: точечная починка и замена
   модуля без остановки продукта с живыми пользователями.
3. Прежде чем строить что-то из списка ниже — прочитай раздел: каждое
   решение уже оплачено днями отладки (креска, DaKi, cres-ca).

**Рамка, в которой существуют все модули** (решение владельца 19.08.2026):
продукт — не законченное приложение, а СРЕДА, куда функции добавляются,
пока люди в ней работают. Отсюда: модуль подключается ДАННЫМИ, а не выкатом;
новый модуль по умолчанию выключен; миграции добавляющие, а сужающая идёт
отдельным шагом после выката. Полный текст принципа и измеренная цена
добавления функции (восемь мест, цель — два) — в CLAUDE.md, раздел
«Принцип проекта: живая среда, а не законченное приложение».

**Стыки между модулями — только именованные точки** (их немного, и это
сознательно): `nextRoute()` — куда после входа; JWT-хук и `tenant_can()` /
`hasModule()` — доступ; `enqueue_notification()` — всё, что хочет написать
или пушнуть; `enqueue()`/`isNetworkError()` — всё, что должно переживать
офлайн; `record_stock_movement()` — всё, что трогает остаток; CustomEvent
`cres:*` — натив ↔ веб. Новый модуль подключается к соседям через эти
точки, а не лезет в их внутренности.

Папки диктует Next.js (`app/` — экраны, `lib/` — логика, `components/` —
общие детали, `supabase/migrations/` — база), поэтому модуль — это не
папка, а именованный список файлов. Список — здесь, и он источник правды.

## ✅ Обёртка вернулась, Flutter — параллельный трек (11.08.2026)

**Решение 11.08.2026 отменяет решение 05.08.2026.** Обёртка Capacitor
снова основное приложение для магазинов; Flutter в `mobile/` не отменён,
но отложен в параллельный трек (список 8 плана). Код обёртки восстановлен
из коммита `e4fffd4`, весь целиком.

**Модули М5, М6, М13, М16 ниже — снова руководство, а не история.**
Файлы, перечисленные в их «Составе», существуют. **М4 — исключение:
вход через провайдера удалён 18.08.2026, его раздел оставлен как список
того, что придётся вернуть РАЗОМ, если провайдера когда-нибудь вернут.**

⚠️ **ЗАПИСЬ ОТ 11.08.2026 БЫЛА НЕВЕРНОЙ, ИСПРАВЛЕНО 12.08.**
Тогда здесь утверждалось: «причина отказа найдена и исправлена коммитом
`e4fffd4`». **Это опровергнуто фактом: сборка 8 (коммит `5dc1a01`)
содержит ту правку и всё равно показывает «This page couldn't load».**
Цикл через `getLaunchUrl()` был реальным дефектом и починен верно,
но причиной отказа он не был.

**Причина на 12.08.2026 достоверно НЕ УСТАНОВЛЕНА.** Три попытки
точечной починки 11–12.08 провалились. Что закрыто фактами и что
опровергнуто — в М6, разделы «Как опознать отказ за минуту» и «Что
проверено и опровергнуто». Читать их ДО любой новой гипотезы: там же
объяснено, почему «`errorPath` не показался» сужает круг сильнее всего
остального.

**Урок процесса, отдельно от техники.** Три раза подряд причина
объявлялась найденной по совпадению механизма с симптомом, без проверки
на устройстве. Каждый раз это стоило дня. Правило на будущее: пока
сборка не проверена на живом телефоне, формулировка — «гипотеза,
согласующаяся с признаками», а не «причина найдена».

**Что из прежней записи осталось правдой:** обёртка не даёт офлайна
чтения. Веб-вью грузит страницы с сервера, кэшировать их можно только
просмотренные. Это ограничение реальное, и снимает его только Flutter.
Офлайн ЗАПИСИ при этом работает и в вебе — очередь `lib/offline/`,
см. М12.

**Одна ошибка того же дня, которую надо знать:** вместе с обёрткой
удалили `components/pwa.tsx`, хотя PWA — это не Capacitor. В нём жила
регистрация service worker, и после удаления она пропала из проекта
целиком: `public/sw.js` лежал мёртвым файлом, PWA не устанавливалась
и офлайн-страница не показывалась. Восстановлено вместе с обёрткой.
Урок общий: при выпиливании стека проверять, что удаляемый файл
принадлежит именно ему.

Что из этих модулей ВЫЖИЛО в вебе и не является историей:
`components/sheet.tsx` и `components/keyboard-fit.tsx` из М13 (обе работают
в браузере: вторая считает клавиатуру через `visualViewport`) и оба роута
`.well-known` из М5 (веб-половина М4 удалена вместе со входом через
провайдера, 18.08.2026):
домен и бандл (`com.cresca.app`) у Flutter те же, поэтому переписывать
их не придётся, только положить новые отпечатки подписи.

Удалено из М13 целиком: `lib/haptic.ts`, `components/swipe-back.tsx`,
`components/native-feel.tsx`. Все трое проверяли `data-native` (а хаптика
по устройству молчала в браузере), то есть без обёртки навсегда
превратились в пустышки.

Спецификация мобильного приложения — в проекте Claude «Маркетплейс»:
`МОБИЛЬНОЕ-ПРИЛОЖЕНИЕ-FLUTTER-РЕШЕНИЕ.md` (объём, слой офлайна, правила
синхронизации, порядок работ) и `СРЕДА-РАЗРАБОТКИ-FLUTTER.md` (что уже
установлено на машине директора и какие грабли при установке пройдены).

**Что остаётся общим у веба и мобильного — база.** Склад, PAO, журналы,
роли и RLS, модули, очередь уведомлений, техкарты, аудит: всё это
Postgres, и Flutter работает поверх ровно того же. Модули М7–М12
из списка ниже читать обязательно — правила синхронизации и работы
с остатком одинаковы для любого клиента.

## Навигация

Статус на 16.08.2026 (✅ построено и проверено вживую · 🟡 построено,
ждёт проверки/деплоя · ⛔ отменено, код удалён · подробный аудит —
в проекте Claude, `СТАТУС-МОДУЛЕЙ`). Пометки ⛔ у обёрточных модулей
сняты решением 11.08 и восстановлением 12.08 — код на месте:

| № | Модуль | Что даёт | Статус |
|---|---|---|---|
| [М1](#м1) | Регистрация | анкета, черновик, согласия с журналом | ✅ |
| [М2](#м2) | Вход | пароль + код + восстановление кодом | ✅ |
| [М3](#м3) | Код из письма | 6 цифр одним полем, SMTP | ✅ |
| [М4](#м4) | OAuth Apple/Google | УДАЛЁН 18.08.2026, вход только почтой | ⬛ |
| [М5](#м5) | Глубокие ссылки | cresca://, App Links, Universal Links | 🟡 |
| [М6](#м6) | Нативная обёртка | Capacitor вокруг живого сайта | 🟡 |
| [М7](#м7) | Доступ | JWT-хук, роли, модули арендатора | ✅ |
| [М8](#м8) | Мультиарендный каркас | 8 правил, RLS | ✅ |
| [М9](#м9) | Склад | остатки журналом, PAO, QR, сканер | ✅ |
| [М10](#м10) | Журналы и Audit Trail | неизменяемые записи, кто-что-менял | 🟡 |
| [М11](#м11) | Уведомления | outbox, письма Resend, пуши OneSignal | 🟡 |
| [М12](#м12) | Офлайн | очередь действий, service worker — ВЕБ | 🟡 |
| [М13](#м13) | Нативные повадки | хаптика, свайп, полоска пуша | 🟡 |
| [М14](#м14) | Навигация приложения | разделы снизу, остальное под аватаром | ✅ |
| [М15](#м15) | Дизайн-система | токены, слои CSS | 🟡 |
| [М16](#м16) | Сборка | Codemagic (Capacitor + Flutter), GitHub Actions | 🟡 |
| [М17](#м17) | Команда і доступи | приглашения, роли, блокировки, сеансы | ✅ |
| [М18](#м18) | Журнал доступа к данным | кто открывал карточки и выгружал списки | ✅ |
| [М19](#м19) | Доступ сотрудников платформы | грант на заведение с причиной и сроком | ✅ |
| [М20](#м20) | Карточка мастера | расписание, отпуска, услуги, «приймає записи» | 🟡 |
| [М21](#м21) | Выгрузка заведения | все разделы одним файлом, JSON и CSV | 🟡 |
| [М22](#м22) | Возвраты | документ от заказа, склад журналом, доход гасится | 🟡 |
| [М23](#м23) | Відгуки і рейтинг | «мало оцінок» до п'яти, оцінка захищена від правки | 🟡 |
| [М24](#м24) | Атрибуція | ?from=ig на заказе/записи, вікно 30 днів | 🟡 |
| [М25](#м25) | Ответ базы человеку | один разбор ошибок, ни одного сырого сообщения Postgres | 🟡 |
| [М26](#м26) | Підтвердження нотифікації | доказательство от поставщика, о котором нельзя забыть | 🟡 |
| [М27](#м27) | Сканер коду | один сканер на продукт, работает и на iPhone | 🟡 |
| [М28](#м28) | Візуальна мова | две полные темы, светлая по умолчанию, узоры из макетов | 🟡 |
| [М29](#м29) | Офлайн-екран і service worker | одна задача: экран «немає мережі», кеша страниц нет | 🟡 |
| [М30](#м30) | Номер сборки для магазинов | `date +%s`, а не счётчик CI и не вопрос к Apple | 🟢 |
| [М31](#м31) | Екран складу | один поиск, один сканер, счётчик = фильтр | 🟡 |
| [М32](#м32) | Візуальна мова за прототипом | палитра, шапка и склад сведены с прототипом CRESKO | 🟡 |
| [П-А](#п-а) | Приложение: Postgres-грабли | | |
| [П-Б](#п-б) | Приложение: среда Claude-агента | | |

---

<a id="м1"></a>
## М1. Регистрация

**Состав:** `app/m/register/` (экран + форма) · `app/m/ui.tsx` (каркас
экрана, Field, keepVisible) · `lib/legal.ts` (LEGAL_VERSION, LEGAL_DOCS) ·
`app/terms` `/privacy` `/cookies` + `components/legal.tsx` · БД: миграция
profiles (first/last/birth + constraint даты) + таблица `user_consents`
+ триггер `handle_new_user`.
**Зависит от:** М3 (код подтверждения), М7 (nextRoute после входа).
**Перенос:** миграция consents/profiles → страницы документов → форма →
константа LEGAL_VERSION.

**Решения и грабли:**
- Поля: имя, фамилия, телефон +380 (ровно 9 цифр), дата рождения
  (реальная дата, возраст ≥16 — проверка и в форме, И в базе constraint'ом:
  форма не граница доверия), почта, пароль ≥8 с глазиком.
- Черновик анкеты в sessionStorage: уход на страницу оферты не стирает
  введённое; после signUp черновик чистится.
- Согласие — журнал, не галочка: `user_consents` (document, version,
  accepted_at, source in ('web','ios','android')) БЕЗ политик UPDATE/DELETE.
  Версия уходит в `options.data` при signUp, триггер кладёт в журнал.
  Меняешь текст документа → меняешь LEGAL_VERSION, иначе журнал врёт.
- Занятая почта (`already registered`) → карточка «увійдіть» со ссылкой
  на вход с предзаполненной почтой, не голый текст ошибки.
- Ссылки на документы БЕЗ target="_blank": в приложении новое окно
  веб-вью — белая страница без «назад».

<a id="м2"></a>
## М2. Вход

**Состав:** `app/m/login/` · `lib/where.ts` (nextRoute — одно место
решения «куда после входа») · `app/m/where.ts` (ре-экспорт).
**Зависит от:** М3, М7. **Перенос:** вместе с М1/М3 — формы двух экранов
и nextRoute.

- Основной способ — пароль (человек его только что придумал; поход
  в почту на каждый вход раздражает). Код — второй путь и способ
  восстановления. Восстановление КОДОМ, не ссылкой: ссылка открывается
  в браузере телефона, человек выпадает из приложения (обжигались).
- `signInWithOtp({ shouldCreateUser: false })` — вход не создаёт аккаунт
  молча; `signups not allowed` → карточка «створіть акаунт».
- `email not confirmed` → «увійдіть кодом — і підтвердимо»: verifyOtp
  подтверждает почту сам.
- Новый пароль после кода — не выходя из приложения (updateUser), сразу вход.
- nextRoute: нет сессии → приветствие; сессия без заведения → создание;
  есть → кабинет. Ошибку чтения не превращать в тупик — пускать в кабинет.

<a id="м3"></a>
## М3. Код из письма (6 цифр)

**Состав:** `app/m/code-input.tsx` · шаблоны в `lib/email/` · настройки
Supabase (руками). **Перенос:** компонент + две настройки + шаблон.

- Supabase → Auth → Email: длина OTP 6, срок 600с. В шаблоне —
  `{{ .Token }}`, НЕ `{{ .ConfirmationURL }}`. Самая частая причина
  «код не приходит» — приходит ссылка.
- **Длина живёт в ДВУХ местах — в настройке Supabase и в поле, — и это
  ломалось молча.** До 13.08.2026 было 8 цифр 4+4; веб-регистрация ждала
  шесть знаков, Supabase слал восемь, и кнопка «Підтвердити» не
  разблокировалась НИКОГДА. Меняешь одно — меняй второе тем же заходом.
- Поле: 6 клеток одной группой, невидимый настоящий input поверх,
  `autocomplete="one-time-code"` (iOS подставляет сам), inputMode="numeric",
  рисованный мигающий курсор. Автопроверка на последней цифре, без кнопки.
  ОДНО настоящее поле, а не шесть: на шести Backspace не уводит назад,
  вставка из буфера кладёт всё в первую клетку, а автоподстановка iOS
  не понимает, куда подставлять. Повтор — таймер 60с; подпись «діє
  10 хвилин, подивіться в спам».
- Бесплатный Supabase = общий отправитель, 2–3 письма В ЧАС на весь проект,
  спам. Рабочая почта = свой SMTP: smtp.resend.com:465, пользователь
  `resend`, пароль — API-ключ; домен верифицировать (DKIM TXT + MX send +
  SPF; у этих DNS-записей прокси Cloudflare ВЫКЛЮЧИТЬ).

<a id="м4"></a>
## М4. OAuth Apple / Google — УДАЛЁН 18.08.2026

**Модуля больше нет.** Вход через Google снят из продукта целиком
решением владельца: его страница во встроенном окне отрисовывается
криво (заголовок налезает на статус-бар, имя приложения рвётся
посреди слова), а это страница Google — починить её мы не можем.
Формулировка владельца: «если он не может отрисовываться нормально,
он не нужен пока».

Единственный путь входа теперь — почта с паролем и код на почту.

**Что удалено** (правило 8 — выключено значит удалено, история в git):
`lib/oauth.ts` · `app/m/oauth.tsx` · `app/(auth)/google-button.tsx` ·
`app/auth/callback/route.ts` · `app/auth/finish/page.tsx` ·
`ensureConsent()` и `consentSource()` из `lib/consent.ts` ·
ветка возврата провайдера в `components/deep-link.tsx` ·
мост `AndroidOAuth` и зависимость `androidx.browser` в
`scripts/patch-android.sh` · домены провайдеров в `allowNavigation`
(`capacitor.config.ts`) · классы `.oauth*`, `.btn-google`, `.btn-apple`
в `globals.css` · девять ключей словаря.

**Что ОСТАЛОСЬ и почему:**

- **схема `cresca://` и весь `components/deep-link.tsx`** — по ней
  приходят тапы по пушам и ссылки на товар из мессенджера. Возврат
  провайдера был лишь одним из трёх её применений;
- **`lib/auth-errors.ts`** — словарь ответов GoTrue читают экраны входа
  и регистрации почтой, он к провайдеру не привязан;
- **`signupSource()` в `lib/consent.ts`** — журнал согласий заполняется
  и при регистрации почтой.

**Если провайдера будут возвращать, вернуть придётся ВСЁ перечисленное
разом.** Ниже — то, ради чего этот раздел не удалён совсем: цена
ошибки в каждом пункте оплачена днями отладки.

- **Провайдеры не пускают вход в веб-вью.** Google отвечает
  `disallowed_useragent` любому встроенному окну. Ввод пароля обязан
  открываться там, где человек видит адресную строку: системный
  браузер на iOS, Custom Tabs на Android.
- **`oauth.ts` однажды отстал от возврата обёртки — и вход был сломан
  молча почти неделю.** Обёртку вернули 11.08.2026 (М6), а `lib/oauth.ts`
  остался в состоянии «обёртки нет» от 05.08: развилка веб/приложение,
  мост `AndroidOAuth` и схема `cresca` были удалены по правилу 8 и не
  восстановлены вместе с остальным. Всё это время `signInWithProvider()`
  делал голый `window.location.href = data.url`, то есть открывал Google
  ВНУТРИ веб-вью. Ошибки видно не было: до входа никто не доходил,
  обёртка падала раньше.
  **Урок шире самого OAuth:** снимая функцию по правилу 8, надо
  записать, ЧТО именно снято и что придётся вернуть вместе с ней —
  иначе возвращают половину, и вторая половина ломается молча.
  Ровно для этого написан список выше.
- **Для App Store вход через Apple обязателен**, первым и не мельче
  Google, кнопка по их гайдлайну. Приложение с одним лишь Google
  отклоняют. То есть возвращать провайдеров придётся ПАРОЙ, а не
  по одному.

<a id="м5"></a>
## М5. Глубокие ссылки

**Состав:** `components/deep-link.tsx` (один обработчик в корневом layout) ·
iOS: шаг PlistBuddy в codemagic.yaml (CFBundleURLTypes) · Android: блок
в `scripts/patch-android.sh` (intent-filters + singleTask + мосты) ·
`app/.well-known/apple-app-site-association/route.ts` ·
`app/.well-known/assetlinks.json/route.ts`.
**Зависит от:** М6. **Перенос:** обработчик + два well-known роута +
куски CI; сменить схему и домен.

- Одна дверь на всё: возврат OAuth (`схема://auth?code=`), тап по пушу
  (`схема://open?path=/...`), https-ссылка на домен. Один обработчик —
  иначе на одном экране работает, на другом нет.
- **Android `launchMode="singleTask"` ОБЯЗАТЕЛЕН**: без него ссылка
  запускает вторую копию активности и onNewIntent не приходит никогда.
- Доставка: iOS — плагин App (`appUrlOpen` + `getLaunchUrl()` для холодного
  старта). Android при удалённом server.url — моста нет: MainActivity
  кладёт ссылку запуска в `window.AndroidDeepLink.consume()` (отдаёт ОДИН
  раз) и стреляет CustomEvent для открытого приложения.
- App Links: intent-filter autoVerify + assetlinks.json с SHA-256 подписи.
  У debug / release / Google Play Signing отпечатки РАЗНЫЕ — список;
  нет отпечатков → пустой массив, ничего не ломается (ссылки идут
  в браузер). Universal Links iOS: entitlement `applinks:домен` + AASA
  (строго application/json, без редиректов); entitlement — ТОЛЬКО за
  флагом CI, пока возможность не включена в App ID: иначе подпись падает,
  билд потерян.
- `/auth/*` из перехвата ИСКЛЮЧИТЬ (exclude в AASA): код OAuth должен
  доехать схемой, мимо перехвата.

<a id="м6"></a>
## М6. Нативная обёртка (Capacitor вокруг живого сайта)

**Состав:** `capacitor.config.ts` · `components/native-boot.ts` (синхронный
скрипт data-native) · `components/native.tsx` (мост, биозамок, твины) ·
`scripts/patch-android.sh` (ВЕСЬ Android-натив) · `native-config/`
(google-services.json, LaunchScreen.storyboard) · `codemagic.yaml` ·
CSS-классы `.web-only`/`.native-only`.
**Перенос:** это ядро обёртки — переносится целиком, меняются appId,
домен, цвета.

- Приложение НЕ содержит JS: веб-вью открывает боевой сайт
  (`server.url`). Фичи прилетают с git push; пересборка бинаря — только
  иконка/название/плагины.

⚠️ **ОГРАНИЧЕНИЕ, КОТОРОЕ НАДО ЗНАТЬ ДО ЛЮБОЙ ОТЛАДКИ.** Документация
Capacitor про `server.url` говорит дословно: «This is intended for use
with live-reload servers. **This is not intended for use in production.**»
Вся конструкция обёртки стоит на режиме, который вендор держит за
инструмент разработки и не гарантирует между версиями. Отсюда и то, что
она ломается на каждой смене мажора. Решение оставаться на `server.url`
принято владельцем осознанно; при следующем необъяснимом отказе первым
вопросом ставить не «что мы сломали», а «не пора ли уходить с этого
режима».

### ⭐ НАСТОЯЩАЯ ПРИЧИНА отказа 04.08–12.08.2026, подтверждена на устройстве

Десять сборок, шесть опровергнутых гипотез, четыре версии «причина
найдена» — и три из них были неверны (см. таблицу ниже). Настоящая
снята маячками с живого iPhone 12.08 в 04:58 UTC:

```
head → boot(cap=1 native=1) → mounted → native(ish=1) → error
                                                  ↑ через 18 мс
TypeError: …addListener("appUrlOpen", …).then is not a function
```

**Мост Capacitor отдаёт handle СИНХРОННО, а не Promise.** Плагины
в проекте берутся из `window.Capacitor.Plugins` (мост), а не из
npm-обёртки — так и задумано, у моста при удалённом `server.url`
нет альтернативы на Android (урок DaKi). Но контракты разные:

```
npm-обёртка (@capacitor/app)   addListener -> Promise<PluginListenerHandle>
МОСТ (window.Capacitor.Plugins) addListener -> PluginListenerHandle СИНХРОННО
```

`components/deep-link.tsx` был написан по контракту npm-обёртки —
`.then` прямо на результате `addListener`. На синхронном handle такого
метода нет: `TypeError`. Бросок синхронный, внутри `useEffect`
компонента `DeepLink`, смонтированного в корневом макете и не
накрытого границей ошибок → React сносит всё дерево до первой
отрисовки → снаружи системный экран «This page couldn't load».
Ровно там же и `getLaunchUrl()` — тот же мост, тот же контракт,
та же ошибка чуть ниже по файлу.

Сходится всё, что не сходилось раньше: в Safari `window.Capacitor`
нет и ветка не исполняется — потому там работало всегда; отказ
не зависел от версии Capacitor — мост так себя ведёт и в 6, и в 8;
бинарь был безупречен; ни `JetsamEvent`, ни падений процесса —
приложение не падало, падало ИСПОЛНЕНИЕ; `errorPath` не показывался,
потому что навигация не отказывала вовсе. Сломал коммит `adb48796`
(добавил слой глубоких ссылок), актуален с 04.08.

**Исправлено и подтверждено:** `df3d89f` (`Promise.resolve` вокруг
обоих вызовов моста + `try/catch` во всех эффектах, трогающих мост,
в `deep-link.tsx`, `native.tsx`, `pwa.tsx`, `banner.tsx`). Подтверждение
12.08 на устройстве: лестница маячков доходит до `native`, ступени
`error` нет ни в одном запуске. Маячки сняты тем же заходом
(`app/api/ping/*`, `components/ping*` — метод описан ниже, воскресить
за десять минут при следующем непонятном отказе).

**Два правила, оплаченные неделей простоя:**

1. **Мост Capacitor синхронен, npm-обёртка асинхронна.** Любой вызов
   `window.Capacitor.Plugins.*` оборачивать в `Promise.resolve(...)`
   ПЕРЕД `.then`/`await` — он принимает и синхронное значение,
   и настоящий промис, поэтому безопасен при любом контракте.
   `await` без `.then` тоже безопасен сам по себе, но привычка
   выборочно защищать код хуже единого правила — оборачивать всегда.
2. **Отказ нативного слоя гасит ВОЗМОЖНОСТЬ, а не приложение.** Любой
   эффект, трогающий мост (`window.Capacitor`, `window.Android*`),
   обязан жить в `try/catch`. Он смонтирован в корневом макете без
   границы ошибок — синхронный бросок там сносит всё дерево React.
   React error boundary как системное решение НЕ заведён — это
   отдельный архитектурный шаг со своим экраном запасного вида,
   а не подстраховка задним числом.

### Метод маячков — переиспользуемый инструмент, не одноразовый

Когда обычные средства отладки недоступны (консоль веб-вью не читается
с устройства), а факт «страница доезжает, дальше тишина» уже известен
(в логах Vercel видно `GET /m 200`, следом `GET /manifest.webmanifest
304` — манифест запрашивается только после разбора HTML), сделать так:

1. Маршрут-приёмник `GET /api/ping/[stage]` — 204 без тела,
   `console.log('[PING]', stage, …)`, `dynamic = 'force-dynamic'`.
   Ступень путём, не query-параметром: логи Vercel группируют
   по `requestPath`.
2. Отправка через `new Image().src` — работает в самом раннем инлайн-
   скрипте `<head>`, ничего не требует от окружения, переживает уход
   страницы. Параметр `r=<случайное>` одинаков на весь запуск.
3. Лестница ступеней: `head` (первая строка `<head>`) → `boot` (после
   `nativeBootScript`, флаги `cap`/`native`) → `mounted` (эффект
   в корневом макете) → `native` (первая строка эффекта, трогающего
   мост) → `error` (`window.onerror` + `onunhandledrejection`, текст
   200 символов) → `hide` (`pagehide`).
4. Первая ступень, до которой лестница дошла, минус следующая, —
   и есть окно, где искать. `error` с текстом — почти всегда конец
   поиска, а не начало.

Локализует обрыв за один запуск вместо вечера гипотез. Снимается
одним коммитом, как только точка обрыва названа, — временный код
опаснее отсутствующего, если его забыть.

### Как опознать отказ за минуту, а не за вечер

Шесть признаков, снятых с живого устройства 12.08.2026. Вместе они
отсекают почти всё:

| Признак | Что означает |
|---|---|
| Экран сразу, содержимого не видно ни кадра | Умирает до первой отрисовки |
| Одинаково у гостя и у вошедшего | До веб-кода дело не доходит |
| Переустановка не помогает | Дело в бинаре или в сервере, не в хранилище |
| Тот же адрес в Safari открывается | Сайт, сеть, DNS и TLS ни при чём |
| **`errorPath` НЕ показывается** | Самый диагностичный. См. ниже |
| В TestFlight «Сбои» — прочерк | Приложение не падало, умер процесс веб-контента |

**Почему «`errorPath` не показался» — главная улика.** Из исходника
`WebViewDelegationHandler.swift`: `errorPath` грузится ТОЛЬКО
в `didFail` и `didFailProvisionalNavigation`. А при смерти процесса
веб-контента вызывается другой обработчик:

```swift
open func webViewWebContentProcessDidTerminate(_ webView: WKWebView) {
    bridge?.reset()
    webView.reload()          // никакого errorPath
}
```

То есть: **`errorPath` показался — это отказ навигации** (сеть, TLS,
редирект, политика). **`errorPath` не показался, а экран есть — это
смерть процесса веб-контента**, и искать надо то, что жрёт память или
вешает поток: циклы вёрстки, бесконечные перерисовки, синхронный код
до первой отрисовки.

### ЭТАЛОН: проект DaKi — полезен, но версия Capacitor была не при чём

`D:\Claude.DaKi` — та же архитектура (веб-вью вокруг живого сайта,
удалённый `server.url`, OneSignal, Codemagic, «фичи прилетают с git
push»), и она открывается. Сверка с ней дала пять содержательных
отличий и настроила пайплайн правильно (`npm ci`, зафиксированные
версии, `UIBackgroundModes`) — но **сама причина отказа лежала не
в одной из этих пяти строк, а в контракте моста** (раздел выше).
Откат на Capacitor 6.2 был правильным методом (сверка с работающим
эталоном — быстрее чтения исходников), но целился не в ту переменную:
версия Capacitor к отказу отношения не имела, мост ведёт себя
одинаково и в 6, и в 8. Урок не «эталон бесполезен», а «сверка
с эталоном сужает круг подозреваемых, но не отменяет проверку
устройством»: реальная причина нашлась не сравнением с DaKi,
а маячками.

Отличия на 12.08.2026 (наши — те, что убраны в этот заход):

| | DaKi, работает | cres-ca, отказ |
|---|---|---|
| Capacitor | 6.2 | 8.5 |
| node в пайплайне | 20 | 24 |
| `cap add ios` | без флагов | `--packagemanager Cocoapods` |
| `server.errorPath` | нет | `offline.html` |
| Лишние плагины | — | биометрия, `@capacitor/browser` |

⚠️ **Отличие, которого нет в таблице задания, но оно реально:**
`Keyboard.resize` — у DaKi `'body'`, у нас `'native'`. В этот заход
НЕ трогали (у нас это осознанное решение: при `'body'` поле «Місто»
трижды оставалось под клавиатурой), но при разборе по одному
проверять и его.

### Что проверено и ОПРОВЕРГНУТО — заново не искать

Каждый пункт закрыт фактом, а не рассуждением. Таблица дороже кода:
она стоила пяти вечеров.

- **`index.html` в `webDir` НЕ обязателен при удалённом `server.url`.**
  Опровергнуто дважды. Исходник `CAPBridgeViewController.loadWebView()`:
  ```swift
  guard FileManager.default.fileExists(atPath: bridge.config.appStartFileURL.path)
  else { fatalLoadError() }
  ```
  `appStartFileURL` = `appLocation` + `appStartPath`; у `appStartPath`
  **умолчания нет** (`CAPInstanceDescriptor` читает его только если задан),
  поэтому путь равен КАТАЛОГУ `public`, а `fileExists` на каталоге —
  `true`. Guard проходит. Вдобавок `fatalLoadError()` делает `exit(1)`:
  приложение бы ЗАВЕРШИЛОСЬ, а не показало экран ошибки. **И второе:
  у DaKi в `public/` тоже нет `index.html`, и она работает.**
  Файл заводился 12.08 и удалён в тот же день.
- **`iosScheme: 'https'` ИНЕРТЕН — не лечит и не ломает.**
  `CAPInstanceDescriptor.normalize()`:
  ```swift
  if let scheme = urlScheme, WKWebView.handlesURLScheme(scheme) == false, … {
      schemeValid = true
  }
  if !schemeValid { urlScheme = InstanceDescriptorDefaults.scheme }
  ```
  `handlesURLScheme("https")` возвращает `true` → условие «`== false`»
  не выполняется → `schemeValid` остаётся `false` → схема сбрасывается
  на `capacitor`. Так и в Cap 6, и в Cap 8. Следствия два:
  удаление строки в `e533aef` **не могло** сломать iOS, и DaKi работает
  **не благодаря** ей. Не добавлять и не удалять впредь без цитаты
  из исходника.
- **`errorPath` резолвится в ЛОКАЛЬНЫЙ файл, не в сайт.**
  `errorPathURL` = `localURL` + `errorPath`, где `localURL` —
  это `capacitor://localhost` (`CAPInstanceConfiguration.m`).
  Убран 12.08 при сверке с эталоном.
- **Память ни при чём.** В «Аналитике» устройства за 11–12.08 нет ни
  одного `JetsamEvent` и нет логов `com.apple.WebKit.WebContent`.
  Процесс не убивали за память и он не падал.
- **Редирект апекса на `www` — реальный, отдельный, исправленный баг,
  но НЕ причина финального отказа.** В Vercel основным доменом был
  `www`, апекс отдавал 308. Веб-вью при удалённом `server.url`
  получал кросс-хостовый редирект и падал за пределы своего origin.
  Исправлено 11.08 (домен переключён, `server.url` указывает на
  `www`). **После этого исправления, в сборке 8, экран остался тот
  же** — доказательство, что это была не единственная причина.
  Заодно этим ударом были биты Universal Links: Apple тянет
  `/.well-known/apple-app-site-association` с указанного хоста и за
  редиректом не идёт. Проверка перед каждой сборкой, одна команда:
  `curl -sI https://cres-ca.com/m | grep -i location` — пусто,
  если не пусто, `server.url` указывает не туда.
- **`getLaunchUrl()`-цикл — реальный защитный код, но он НИКОГДА
  не был действующей причиной.** Фильтр «принимать только схему
  `cresca:`» (коммит `e4fffd4`, 05.08) защищает от честного риска:
  при удалённом `server.url` `getLaunchUrl()` может отдать сам
  `server.url`, и без фильтра `handle()` → `go('/m')` → перезагрузка
  → `getLaunchUrl()` снова → цикл. Но `.then` на этом же вызове
  висел на СИНХРОННОМ мосте (см. «Настоящая причина» выше) — значит
  до 12.08 колбэк с фильтром попросту НИКОГДА НЕ ЗАПУСКАЛСЯ, крах
  происходил раньше, на `addListener`. Фильтр не трогать: теперь,
  когда `.then` исправлен через `Promise.resolve`, колбэк действительно
  выполняется, и без фильтра цикл станет реальным. Android этим
  не болел: `linkOf()` требует `ACTION_VIEW`, при запуске с иконки
  там `ACTION_MAIN`, а `consume()` одноразовый.
- Петля `--vvh` ↔ `transition: height` — исправлена `6a3b544`,
  проверено на бою: анимации высоты в разметке `/m` больше нет.
  Дефект реальный, правку НЕ откатывать.
- Кеш service worker, куки, localStorage — полная переустановка стёрла
  хранилище, не помогло.
- Сборка не падает: все шаги зелёные, `App.ipa` 2.5 МБ.

### Порядок разбора «сравнялись с эталоном» — ЗАКРЫТ, план не понадобился

Ниже — план на случай, если бы приложение открылось от одного только
приведения к DaKi (Capacitor 6.2, node 20, флаги `cap add`). Он не
понадобился: причина нашлась независимо, маячками, и версия Capacitor
в ней не участвует (см. «ЭТАЛОН» выше). Оставлено как метод на будущее
— «сверка с работающим эталоном, потом возврат отброшенного по одному»
работает, когда причина действительно в одном из отличий.

1. вернуть node 24;
2. вернуть `--packagemanager Cocoapods`;
3. вернуть `@capacitor/browser`;
4. вернуть биометрию;
5. поднять Capacitor 6.2 → 7 → 8 по одному мажору;
6. вернуть `Keyboard.resize: 'native'`, если его меняли.

Фактический порядок восстановления теперь другой — простое возвращение
без диагностической ценности, следующим заходом с пересборкой (нужны
все четыре разом, разбирать по одному незачем):
Capacitor 8.5, `@capgo/capacitor-native-biometric`, `@capacitor/browser`,
исключение `accounts.google.com`/`appleid.apple.com` из `allowNavigation`
(см. М4, ниже).

### Плагины, снятые ВРЕМЕННО 12.08.2026 — сняты по неверной причине

`@capgo/capacitor-native-biometric` и `@capacitor/browser` удалены
из `package.json` при сверке с DaKi — версия Capacitor и лишние
плагины были не при чём (см. «ЭТАЛОН» выше), но раз уж сняты — пусть
остаются до планового возврата следующим заходом, спешки нет.

**Для веба это безопасно, проверено по коду:** ни один файл в `app/`,
`lib/`, `components/` не импортирует нативные пакеты. Плагины берутся
из `window.Capacitor.Plugins` и `window.Android*` в рантайме
(`components/native.tsx` → `cap()?.Plugins?.NativeBiometric`,
`components/deep-link.tsx` → `w.Capacitor?.Plugins?.Browser?.close?.()`),
все обращения через `?.`. Отсутствие пакета отключает возможность,
но не ломает сборку.

Что при этом выключено: биозамок по Face ID и программное закрытие
системного браузера после возврата от Apple/Google (окно придётся
закрывать вручную). Возвращать по одному, шаги 3 и 4 порядка выше.

### Ключи конфига, которые iOS вообще читает (Capacitor 8)

Снято из `CAPInstanceDescriptor.swift`, а не из документации. Всё,
чего здесь нет, на iOS игнорируется молча:

```
server.url · server.errorPath · server.appStartPath · server.allowNavigation
server.hostname · server.iosScheme
ios.contentInset · ios.scrollEnabled · ios.limitsNavigationsToAppBoundDomains
ios.backgroundColor · ios.appendUserAgent · ios.overrideUserAgent
ios.allowsLinkPreview · ios.zoomEnabled · ios.initialFocus
ios.preferredContentMode · ios.handleApplicationNotifications
ios.loggingBehavior · ios.webContentsDebuggingEnabled
backgroundColor · appendUserAgent · overrideUserAgent · zoomEnabled
initialFocus · loggingBehavior · plugins
```

`server.cleartext` и `server.androidScheme` в этом списке ОТСУТСТВУЮТ —
они только для Android. В конфиге они есть и вреда не делают, но на iOS
ни на что не влияют.

### Мины помельче, из более ранних заходов

- **Главный урок (DaKi):** на Android Capacitor НЕ инжектит JS-мост
  в удалённый server.url (на iOS инжектит). Всё нативное для Android —
  в MainActivity через `addJavascriptInterface`: пуши (нативный OneSignal),
  биометрия, вибрация, deep links, Custom Tabs. Веб зовёт
  `window.AndroidXxx.метод()`, натив отвечает
  `evaluateJavascript("...dispatchEvent(new CustomEvent(...))")`.
- Папки android/ и ios/ пересоздаются (`cap add`) — правки живут
  В ПАЙПЛАЙНЕ (patch-скрипт), не в репозитории.
- `data-native` ставит СИНХРОННЫЙ скрипт в `<head>` до первой отрисовки
  (в useEffect виден кадр чужого хрома): жёсткие признаки (Capacitor,
  свои мосты) + залипание в localStorage + UA-фолбэк (`; wv)` на Android,
  на iOS нет `Safari/`) с чёрным списком чужих веб-вью (Instagram, FB…).
  Там же — редиректы веб-двойников (`/login → /m/login`).
- Кнопка «назад» Android = история веб-вью, иначе первый свайп выбрасывает
  из приложения. Клавиатура — `resize: 'native'` (М13-К).
- Один набор серверных действий и форм на веб и приложение, различается
  только раскладка («общий слой вместо паритета»): добавление поля —
  правка одного файла.

<a id="м7"></a>
## М7. Доступ: JWT, роли, модули арендатора

**Состав:** `lib/tenant.ts` (getMemberships/currentMembership/can/hasModule) ·
`components/module-gate.tsx` (экран отказа по модулю) · БД: role_grants,
tenant_members, tenants.modules, функция `custom_access_token_hook`,
`tenants_with()`, `tenant_can()` · Supabase Dashboard: включить хук (руками!).
**Перенос:** миграция доступа + lib/tenant.ts + module-gate; хук включить
в дашборде.

- Роль = что можно ЧЕЛОВЕКУ; модуль = что купил БИЗНЕС. Две разные оси:
  владелец с полными правами не видит каталог, если бизнес взял только
  склад. Модули в токене, навигация фильтруется автоматически.
- **Меню — не граница доступа.** Пункт прятался, а прямой адрес открывал:
  обе проверки обязаны стоять на самой странице. Отказ по праву —
  `redirect('/app')`, по модулю — экран `<ModuleOff>`, а не молчаливый
  возврат: «нажал — и меня выкинуло» читается как поломка.
- **«Модулей нет» и «клейма нет» — РАЗНЫЕ вещи, и обе давали пустой
  массив.** Хук выдаёт `modules` всегда рядом с `memberships`; отсутствие
  ключа означает сломанный или устаревший токен, а не заведение без
  модулей. Тогда подставляется УМОЛЧАНИЕ (не пустота — она запирает
  владельца везде, и починить он это не может, кабинет за тем же токеном;
  и не «всё разрешено» — иначе старый токен открывает неоплаченное),
  а признак подмены уезжает в `modulesFromToken`, чтобы экран отказа
  советовал повторный вход.
- **Грабли на день:** хук кладёт данные в ПОЛЕЗНУЮ НАГРУЗКУ токена;
  `session.user.app_metadata` — это запись из базы, хук её не трогает.
  Читать оттуда = «членств нет никогда» = петля «створи заклад ще раз».
  Расшифровывать payload руками (base64url), подпись не проверять — её
  проверяет Supabase на каждом запросе.
- После создания заведения токен на руках старый — обязателен
  `refreshSession()` перед входом в кабинет, иначе то же кольцо.
- Пока хук не включён в дашборде — кабинет не откроется вовсе.

<a id="м8"></a>
## М8. Мультиарендный каркас — восемь правил

Нарушение любого = переписывание половины проекта.
1. `tenant_id` в каждой строке; уникальные ограничения составные; нумерация
   документов пер-арендаторная; в хранилище — первый сегмент пути.
2. Права — данные (role_grants); в коде `tenant_can()`, в RLS
   `tenant_id in (select tenants_with('право'))`; никаких email в коде.
3. Роль из JWT, не из базы; политика с запросом к таблице на каждой
   строке — ошибка приёмки; сервисный ключ только в фоне.
4. Товары и услуги — одна модель; характеристики в jsonb по схеме категории.
5. Остаток напрямую менять нельзя — кэш от журнала движений (М9).
6. Скорость — критерий приёмки (docs/PERFORMANCE.md).
7. Каждая функция миграции заканчивается revoke/grant (П-А);
   анонимные функции — закрытый список; `using (true)` — ошибка приёмки.
8. Выключено = удалено, в том же коммите.

<a id="м9"></a>
## М9. Склад: остатки журналом, PAO, QR

**Состав:** `app/app/inventory/**` · БД: materials, material_batches,
material_containers, material_documents, material_barcodes,
stock_movements + `record_stock_movement()`, триггеры containers_guard
и блокировки прямого update · `lib/report/labels.ts` (наклейки A4 3×8).
**Зависит от:** М7, М8. **Перенос:** миграции склада подряд + экраны.

- Источник правды — журнал `stock_movements`; stock_qty — кэш, прямой
  update блокирует триггер. Ошибочное движение гасится встречным.
  У операций ключ идемпотентности (`p_idempotency_key`).
- PAO: `use_by = least(срок_партии, opened_at + PAO)` — считает и запирает
  ТРИГГЕР; дату вскрытия править нельзя (это журнальный факт): ошибочную
  ёмкость списывают и заводят новую. Розлив в дозатор = ёмкость-потомок
  со своим QR.
- Сканер: BarcodeDetector (Chrome/Android из коробки) + ручной ввод;
  камера только по HTTPS. Результат скана остаётся на экране, поле готово
  к следующему — это цикл.
- Смена срока гасит pending-предупреждения; списание — тоже (М11).

<a id="м10"></a>
## М10. Журналы и Audit Trail

**Состав:** `app/app/journals/**` (+ вкладка «Дії») · БД:
sanitation_solutions, cleaning_tasks/entries, sterilization_cycles,
`audit_log` + одна триггер-функция на ~16 таблиц + запрет UPDATE/DELETE.
**Перенос:** миграция журналов + миграция audit + экран.

- Неизменяемость = ОТСУТСТВИЕ политик UPDATE/DELETE (+ запрещающий триггер
  для верности), а не проверка в коде. Ошибочная запись гасится встречной.
- Audit-триггер: action=lower(TG_OP), entity=имя таблицы, label из
  name/title/code, changes = диф ТОЛЬКО изменённых полей `{k:{was,now}}`,
  шумовые поля (updated_at, кэши, search_tsv) исключены, пустой диф
  не пишется. Чтение по праву compliance.read; экран грузит лениво.
- Версионные документы (техкарты): утверждённое не редактируется —
  только следующая версия.
- PDF-отчёт для проверки — страница + печать браузера, «в 1 клик».
- **Инспектор читает ТОЛЬКО проекции `compliance_*`, и они обязаны
  покрывать свои экраны целиком.** У роли ровно одно право,
  `compliance.read`; пока экраны ходили в таблицы напрямую, она получала
  не отказ, а ПУСТОТУ: «засоба не існує» на карточке и прочерк в колонке
  «Виконавець» отчёта — причём у владельца тоже, потому что `profiles`
  читается политикой только про себя. Молчаливая пустота дороже отказа:
  документ с юридическим весом обещал то, чего не показывал. Ветвлений
  «если инспектор» в коде нет ни одного — компланс-часть всегда из
  представлений, коммерческая всегда из таблиц.

<a id="м11"></a>
## М11. Уведомления

**Состав:** БД: notification_outbox, notification_templates,
`enqueue_notification()`, `enqueue_expiry_warnings()`, pg_cron-задание ·
`app/api/cron/notifications/route.ts` (разбор сервисным ключом) ·
`lib/email/` (шаблоны), `lib/notify/` · OneSignal-мост в patch-android.sh ·
`components/banner.tsx` (полоска в открытом приложении).
**Зависит от:** М6 (пуш-мосты). **Перенос:** миграция outbox + cron-роут +
шаблоны; ключи в env.

- Триггеры ТОЛЬКО ставят в очередь; разбирает один cron-роут: email через
  Resend, пуш через OneSignal. Шаблоны в таблице по (event, channel, locale).
  dedupe_key + `on conflict do nothing`.
- **База даёт ТЕКСТ, код — КАРКАС** (`lib/email/queue.ts`). Тело письма
  нельзя брать из кода: арендатор переопределяет шаблон, и переопределение
  молча игнорировалось бы. Обратная сторона: пока каркаса не было, письма
  очереди уходили голой строкой мимо всякой вёрстки, и это не замечали —
  проверять надо не «письмо пришло», а как оно выглядит.
- **Предупреждение о сроке ставится НА КАЖДУЮ ёмкость.** Обещанный
  дайджест в схеме не собран, и восемь одинаковых писем утром перестают
  открывать. Дайджест — это отдельное событие, а не отправитель.
- Расписание — pg_cron ВНУТРИ Supabase раз в 5 минут, НЕ Vercel Cron
  (бесплатный умеет раз в сутки).
- **Уведомление без получателя — мусор:** обработчик падает, строка висит.
  Получателей выбирать при постановке («члены арендатора с правом X, кроме
  инспектора»). Это ловилось как «механика есть, а письма не приходят никому».
- Предупреждения о сроках: «за 14 дней» не досылать задним числом; завели
  за <7 дней до конца — слать сразу; молчать хуже всего.
- Пуш при ОТКРЫТОМ приложении система не показывает: перехват
  (Android addForegroundLifecycleListener / iOS foregroundWillDisplay +
  preventDefault) → CustomEvent → своя полоска сверху, свайп закрывает.
  Тап по пушу из шторки — переход по url из additionalData.

<a id="м12"></a>
## М12. Офлайн

**Состав:** `lib/offline/queue.ts` (очередь IndexedDB + isNetworkError) ·
`components/offline.tsx` (полоса + автодосылка) · `public/sw.js` ·
`app/offline/` · вызовы enqueue в экранах действий.
**Перенос:** три файла + подключение кнопок; типы действий переписать
под домен нового проекта.

- Ошибка СЕТИ → действие в очередь, работаем дальше. Ошибка ДАННЫХ →
  показать текст, в очередь НЕ класть (не отправится никогда). Различение:
  `!navigator.onLine` или /failed to fetch|load failed|network/i
  (Chrome и Safari формулируют по-разному).
- **Грабли аудита:** построить очередь мало — каждую кнопку надо
  ПОДКЛЮЧИТЬ. Неподключённая очередь — мёртвый код, выглядящий готовой
  фичей. И: в отложенной записи должен ехать userId — NOT NULL «кто сделал»
  без default роняет досылку.
- Время действия — момент нажатия, не появления сети (журнал не должен
  врать). Списания — только через RPC с ключом идемпотентности
  (ключ = id записи очереди).
- UI: галочка сразу (локальный Set), полоса «працюємо офлайн / N дій
  чекають», beforeunload-предупреждение, автодосылка на online +
  visibilitychange.
- SW: Supabase не трогать ВООБЩЕ (авторизация), POST не кешировать,
  страницы network-first + предкешированный /offline.

<a id="м13"></a>
## М13. Нативные повадки (UI-киты)

**Состав:** `lib/haptic.ts` · `components/sheet.tsx` ·
`components/swipe-back.tsx` · `components/native-feel.tsx` (тап мимо поля,
pull-to-refresh) · `components/keyboard-fit.tsx` · куски globals.css.
Все сквозные — подключаются ОДИН раз в корневом layout (на одном экране
есть, на другом забыто — хуже, чем нигде). **Перенос:** файлы + CSS-куски.

- **Клавиатура (К):** `Keyboard: { resize: 'native' }` — НЕ 'body': при
  'body' веб-вью остаётся во весь экран, 100dvh и visualViewport врут,
  и ни одна JS-поправка не работает (три неудачных фикса). Дальше:
  рамка `/m` высотой `--vvh` (= visualViewport.height); кнопка
  sticky bottom:0 ВНУТРИ формы (sticky ограничен родителем); поля
  ≥16px (iOS зумит); высоты в dvh. Chrome для теста бесполезен: нет
  веб-вью, в фоновой вкладке заморожен rAF и focus() не даёт focusin.
- **Поправка на клавиатуру сама стала дефектом, и это четвёртый заход
  (18.08.2026).** Она ТАЩИЛА поле к верхней кромке на каждый фокус
  (`scrollIntoView({ block: 'start' })` дважды, 60мс и 420мс) и добавляла
  форме `padding-bottom: 55vh`, чтобы дотащить было куда. Пока веб-вью
  не ужималось, это спасало. После перехода на `resize: 'native'`
  спасать стало нечего — а рывок и пустота остались: заголовок экрана
  уезжал за верхний край, под кнопкой зияла половина экрана. Владелец:
  «клавиатура очень некрасиво обрезает всё».

  Стало: **пока поле видно — не двигается НИЧЕГО**; если поле зашло за
  кромку, доводим до кромки и не дальше (`block: 'nearest'`, проверка
  через 350 и 650мс — раньше измерять нечего, клавиатура ещё едет).
  `scroll-margin` у полей срезан с 84px до 20px: при минимальной
  прокрутке большой отступ возвращает рывок обратно.

  Правило на будущее: **запас под клавиатуру и подтягивание поля —
  ОДНА механика, и снимать их можно только вместе.** Оставить запас
  «на всякий случай» нельзя — он виден всегда, а решает задачу,
  которой уже нет.
- **ЧЕТВЁРТЫЙ И ПОСЛЕДНИЙ ЗАХОД: `Keyboard.resize: 'none'`.** Требование
  владельца дословно: «не должно появляться чёрной подложки под
  клавиатурой, при открытой клавиатуре ничего не должно подниматься,
  страница не должна вообще дёргаться». Оба дефекта оказались ОДНОЙ
  причиной: `'native'` физически УЖИМАЕТ веб-вью. Отсюда перекладка всей
  страницы (высота экрана меняется на треть) и тёмная полоса на месте,
  которое веб-вью освободило, — цвет там из бинаря, а не из веба,
  поэтому в установленной сборке он оставался старым.

  С `'none'` клавиатура ложится ПОВЕРХ, веб-вью не меняет размер никогда,
  рамка `/m` — постоянные `100dvh`. Меняется одно число: `--kb` (сколько
  закрыто снизу), и оно идёт в ОТСТУП прокручиваемой области. Раскладка
  не пересчитывается вообще.

  Считается как `innerHeight − (visualViewport.height + offsetTop)`,
  порог 80px (мелкие значения — это адресная строка и «резинка»,
  а не клавиатура). **Именно `'none'`, не `'body'`:** при `'body'` оба
  числа уменьшаются одинаково, разность выходит нулём и клавиатуру
  посчитать нечем — на этом уже обжигались.

  Побочно приложение и браузер сошлись в одну ветку: в браузере плагина
  нет, веб-вью тоже никто не ужимает, расчёт тот же.
- **Одного `visualViewport` мало — нужен ВТОРОЙ источник, плагин.**
  Оплачено отказом в тот же день: «под клавиатурой не видно и страница
  не скролится». Причина в том, что конфиг Capacitor живёт В СБОРКЕ:
  на телефоне месяцами может стоять бинарь со старым `resize: 'native'`.
  А он ужимает веб-вью — тогда `innerHeight` уменьшается ровно
  на клавиатуру, разность выходит нулём, отступ не появляется, и низ
  экрана недостижим. **Веб обязан быть верным при ОБОИХ бинарях**,
  иначе каждая правка конфига ломает всех, кто ещё не обновился.

  Плагин (`Capacitor.Plugins.Keyboard`, события `keyboardWillShow`
  и `keyboardWillHide`) говорит высоту прямо, независимо от режима.
  Формула одна на оба случая: из высоты клавиатуры вычитается то,
  на сколько веб-вью ужалось само. Ужалось целиком — добавлять нечего;
  не ужалось — добавляем всё.
- **При клавиатуре ПОВЕРХ браузер сам поле не подвинет.** Он не считает,
  что видимая область изменилась. Поэтому нужна страховка: если поле
  реально закрыто — довести его до кромки и НЕ ДАЛЬШЕ
  (`block: 'nearest'`, проверка через 350мс). Поле видно — не двигается
  ничего. Это не возврат прежнего «тащить наверх»: там движение было
  безусловным, здесь — только когда иначе поле недостижимо.
- **Полоса главного действия — `fixed`, а НЕ `sticky` внутри формы.**
  `position: sticky; bottom: 0` прибивает элемент к низу области
  ПРОКРУТКИ, и на длинной форме он оказывается посреди неё: сверху над
  ним поле пароля, снизу «Підтвердіть пароль». Форма разрезана плашкой
  пополам — владелец описал это как «кнопка выше, чем надо, и под ней
  подложка в воздухе». Увидено снимком локального рендера, а не по
  описанию.

  Теперь `.m-actionbar`: `fixed` у нижней кромки экрана, `bottom: var(--kb)`
  (поднимается над клавиатурой), а прокручиваемая область отступает
  на её высоту через `:has(.m-actionbar)`. Высота панели — токен
  `--actionbar-h`: разъехавшись, отступ и высота дадут либо щель, либо
  поле под панелью. Панель остаётся ВНУТРИ `<form>`: связь кнопки
  с формой определяется деревом документа, а не раскладкой.
- **Честной ВЫСОТЫ мало — рамку надо ПРИБИТЬ к видимой области.**
  После снятия запасов владелец сообщил: «на долю секунды всё нормально,
  потом клавиатура так же всё закрывает». Высота рамки `/m` была верной
  (`--vvh`), но сама рамка лежала в обычном потоке — и документ под ней
  прокручивался. А он прокручивается: браузер, доводя поле до видимой
  области, двигает СТРАНИЦУ. Верх рамки уезжает над экраном, низ — под
  клавиатуру. Тот же эффект даёт «резинка» iOS у края.
  Лечится `position: fixed` + `top: var(--vvt)`, где `--vvt` —
  `visualViewport.offsetTop`. Поправка обязательна: фиксированный элемент
  привязан к РАЗМЕТОЧНОМУ окну, а не к видимому, и без неё уезжает вместе
  с ним. Побочно исчезает прокрутка документа вовсе: рамка вне потока,
  высота body схлопывается, прокручивается только `.m-scroll`.
- Хаптика: iOS — Capacitor.Plugins.Haptics; Android — мост
  (VibrationEffect 8–18мс, амплитуда 110–200); web — тишина.
- Шторка: drag-to-dismiss (⅓ высоты или флик), скролл-лок, Escape.
  Формы на телефоне — шторками, не блоками, раздвигающими список.
- Свайп-назад от левого края 24px, отмена по вертикали, хаптика на пороге.
- **Pull-to-refresh: только у верха списка, сопротивление ×0.5, порог 72px,
  и обновляет `router.refresh()`, а НЕ `location.reload()`.** Полная
  перезагрузка сносит документ целиком, все `.rise` стартуют с нулевой
  прозрачности — и человек на полсекунды видит голый фон. Владелец описал
  это как «экран темнеет на секунду, а должно быть бесшовно» (18.08.2026).
  `router.refresh()` подменяет содержимое НА МЕСТЕ: узлы не пересоздаются,
  анимации не стартуют заново, прокрутка остаётся. Кружок крутится, пока
  идёт переход, и снимается по `isPending` — момент окончания знает React,
  а не обработчик касания. Новую СБОРКУ этот путь не подтянет намеренно:
  про неё сообщает своё уведомление из `pwa.tsx`, и вот там перезагрузка
  полная и уместная.
- **Задержка появления — это время ПУСТОГО экрана**, потому что `both`
  держит нулевую прозрачность всю задержку. Прежние 400ms плюс задержки
  до 280ms давали 680ms до последнего блока; стало 260ms и до 160ms.
  Анимация появления, которую замечают, — уже не появление, а ожидание.
- Переход экрана 180мс сдвиг+фейд; tap-highlight transparent;
  `:active{scale(.975)}` только `@media (hover:none)`.

<a id="м14"></a>
## М14. Навигация приложения

**Состав:** `components/app-shell.tsx` (TABS + MENU + шторка + заголовки) ·
`components/shell.tsx` (публичный хром, ре-экспорт AppShell) ·
`components/icons.tsx` · CSS .drawer\*/.bottomnav\*. **Перенос:** app-shell
+ иконки + CSS; TABS и MENU переписать под разделы нового проекта.

Архитектура (**решение владельца 15.08.2026, отменяет схему 05.08 «разделы
за бургером, снизу — табы раздела»**): **снизу — сами разделы, четыре**
(Склад · Записи · Послуги · Профіль), **всё остальное — под аватаром**,
шторкой снизу. Бургера слева нет. Причина: мастер за смену прыгает между
складом, записями и услугами десятки раз, а внутрь одного раздела заходит
подряд — панель обязана держать то, между чем прыгают.
- Больше четырёх на 390px не помещается так, чтобы подпись читалась,
  а зона нажатия осталась 44px.
- Сверху — поиск, сканер и аватар. Поиск ведёт на склад адресом `?q=`,
  сканер — `?scan=1`: строка работает с любого экрана.
- Пункт фильтруется по ДВУМ осям: `module` (что купил бизнес) и `perm`
  (что можно человеку). Пока фильтра по праву не было, мастер и бухгалтер
  видели «Команда» и «Фінанси», нажимали — и страница молча возвращала
  их на `/app`. Пункт, который нельзя открыть, — это не «защищено», это
  сломанная навигация. Скрытый пункт при этом ничего не запрещает:
  граница — RLS и проверки на самих страницах.
- **Оболочка живёт в `app/app/layout.tsx`, один раз на весь кабинет,
  и AppShell идемпотентен.** Пока её рисовала каждая страница, при переходе
  Next показывал `loading.tsx` сегмента, а тот рисовал ВТОРУЮ оболочку:
  две нижние панели одна поверх другой и скелетон чужого экрана под
  правильным заголовком.
- Заголовок и подпись раздела — часть НАВИГАЦИИ, а не страницы (список
  `HEADINGS`, `*` — любой один сегмент). В двадцати шести страницах это
  двадцать шесть источников правды и «Каталог» в заголовке там, где
  в панели написано «Послуги».
- Табы одного адреса — через `?tab=`: ссылка на вкладку шарится в чат.
- Шапка: слева «назад» (карточка) или ничего (корень); заголовок; справа
  ОДНО действие. Десктоп — постоянный сайдбар.
- **Подсветка пункта — оптимистичная, а не по факту перехода.** Отзыв
  владельца 18.08.2026: «переход между табами прогружается где-то секунду,
  а должен быть мгновенным». Секунда честная и никуда не денется: страницы
  кабинета `force-dynamic` и ходят в базу в Ирландии. Но ждал он не её —
  `usePathname()` меняется только КОГДА переход совершился, поэтому всю
  секунду нажатая вкладка выглядела ненажатой: палец попал, а панель этого
  никак не показала. Читается как «не сработало», и человек жмёт второй раз.
  Замер после правки: подсветка переезжает за ~90 мс при сервере,
  задержанном на 1,5 с, — то есть адрес ещё старый, а панель уже ответила.
  Обязательна страховка по времени: переход может не состояться ВОВСЕ
  (порвалась сеть, ушли из приложения), `pathname` тогда не сменится
  никогда, и подсветка осталась бы врать бессрочно.
- **Скелетон вкладки — часть навигации, а не украшение экрана.** Без
  `loading.tsx` Next держит на экране ПРЕЖНЮЮ страницу всё время серверной
  отрисовки: нажатие не меняет ничего. Из четырёх вкладок нижней панели
  скелетона не было у двух — «Послуги» и «Профіль» (заведены 18.08.2026).
  Отсюда правило: у КАЖДОЙ вкладки панели есть `loading.tsx`, и его форма
  повторяет форму экрана — иначе в момент прихода данных раскладка
  прыгает. Переделал экран — переделай его скелетон тем же коммитом
  (скелетон склада отстал ровно так и показывал раскладку до М31).
- **Мгновенность — это ОТСУТСТВИЕ запроса, а не быстрый запрос.**
  Требование владельца 18.08.2026: «молниеносно, как в инсте, телеграмме».
  Ни инстаграм, ни телеграм в момент нажатия никуда не ходят: экран
  рисуется из того, что уже на устройстве, а сеть догоняет потом и правит
  его молча. Пока переход ждёт сервер, он не станет мгновенным никакими
  скелетонами — скелетон лишь честно показывает ожидание.

  Собрано из двух частей, и обе обязательны:

  1. **`prefetch` у четырёх вкладок панели** — именно `true`, а не
     умолчание. По умолчанию Next для ДИНАМИЧЕСКОГО адреса тянет только
     до ближайшего `loading.tsx`: заранее приезжает скелетон, а данные
     всё равно едут в момент нажатия. `true` тянет ответ целиком.
     Вкладки всегда на экране, значит их ответы приезжают, пока человек
     смотрит на первую.
  2. **`staleTimes.static` = 180** (next.config.ts). Полный упреждающий
     ответ живёт по `static`, а НЕ по `dynamic` — перепутать эти два числа
     значит выбросить всё, что напрефетчили, и упреждать впустую.
     `dynamic` (30) остаётся для обычных переходов, у которых упреждения
     не было.

  Замер на боевой сборке, сервер отвечает 1200 мс на отрисовку: четыре
  упреждающих запроса уходят сами, нажатие меняет содержимое за ~80 мс
  и делает **ноль** запросов в сеть.

  Ставить `prefetch` на девять пунктов под аватаром НЕЛЬЗЯ: это грело бы
  на каждом открытии девять страниц, которые открывают раз в день.
- **За кеш платят свежестью, поэтому рядом обязано стоять тихое
  обновление.** `router.refresh()` через 300 мс после отрисовки и всегда
  при возвращении в приложение (`visibilitychange`). Три условия, каждое
  оплачено отдельно: в `useTransition` — иначе экран мигнёт скелетоном
  вместо того, чтобы обновиться на месте (проверено: 134 замера за 3 с,
  скелетона нет, пустых кадров нет); не чаще раза в 20 секунд на адрес —
  иначе прыжок туда-обратно стоит серверу столько же, сколько стоил бы
  БЕЗ кеша, и смысл пропадает; не при `navigator.onLine === false` —
  мастер у кресла работает офлайн штатно, и запрос в никуда там лишний.
  Клиентское состояние экрана (вкладка, фильтр) `refresh` не теряет —
  на этом и держится «бесшовно».

<a id="м15"></a>
## М15. Дизайн-система

**Состав:** `app/globals.css` — единственный источник значений.
Захардкоженный цвет/радиус в компоненте — ошибка приёмки.

- Второго визуального языка не заводить: в первой креске их стало четыре,
  склейка стоила сорока правил с !important.
- **Слои:** utilities Tailwind идут ПОСЛЕ components — внутри @layer их
  не перебить. Правила, обязанные выигрывать (хром приложения, шторки,
  клавиатура), пишутся ВНЕ @layer: unlayered бьёт все слои. Это заменяет
  !important полностью.
- Радиус растёт с размером объекта; тень двухслойная вместо жирной границы;
  обновляющиеся числа — .tabular; тёмная тема основная, системную
  НЕ слушать, светлая переключателем, класс ставит синхронный скрипт.
- **Грабли: зона нажатия задаётся классом кнопки, а не утилитой по месту.**
  `.btn-ghost` держал `h-10` (40px) — ниже `--tap-min` (44px), и в семи
  местах по коду пришлось точечно ставить `min-h-11`, чтобы дотянуть
  клик до нормы. Починка не «добавили ещё утилиту», а `min-height:
  var(--tap-min)` в самом классе (`.btn-ghost`, заодно нашлись те же
  дыры в `.chip`/`.chip-active` и `.sidebar-item`) — видимая высота
  и отступы не меняются, растёт только невидимая область клика.
  `min-height` поверх меньшего `height` в CSS всегда побеждает, поэтому
  трюк безопасен даже когда класс уже задаёт фиксированную высоту.
  Прежде чем костылять утилитой в конкретном месте — проверь, не в классе
  ли дыра: если она в классе, лечится один раз там, а не на каждом вызове.

**Задание на раскладку (Claude Design, шаг 9).** Не описание того, что
сделано, а к чему приводить шесть экранов. Общие правила: одно крупное
число сверху отвечает на главный вопрос экрана; три круглые кнопки —
только частые действия ИМЕННО этого экрана и только там, где их
действительно три равнозначных; список — всегда значок-якорь слева
(`.list-anchor`), значение справа; группировка по датам (`.list-date-sep`)
только там, где записи упорядочены во времени; одна главная кнопка на
экран; цветом выделяется исключение, а не норма; `--color-faint` — только
для дат группировки и разделителей.

1. **Головна зміни** (эталон ритма для остальных). Число: скільки позицій
   прострочено зараз (`.num.shift-hero-figure`), підпис зверху «Прострочено
   позицій» (`.t-sm.prose-muted`), знизу «з 142 у наявності» (`.t-sm`,
   `--color-faint`). Колір `--color-text`, стає `--color-danger` тільки
   через `[data-alert="true"]`, коли прострочено більше десятої частини
   залишку. Одна кнопка `.btn-secondary` «Переглянути прострочене» — не
   `.btn-primary`, щоб не конкурувати з числом. Три круги: сканувати,
   відкрити зміну, списати. Групування по датах немає — це зведення.
2. **Склад.** Число: скільки позицій закінчується, зі скількох на складі.
   Три круги: сканувати, прийняти, інвентаризація. Вкладки над списком:
   `.chip-active` «Усі», `.chip` «Закінчуються», `.chip` «Прострочені».
   Рядок `.row`: зліва `.list-anchor` зі значком категорії, посередині
   назва і партія у два рядки (`.t-md` + `.t-sm.prose-muted`), справа
   залишок (`.num`). Колір-виняток `--color-warn` тільки на числі залишку,
   рядок лишається нейтральним. Групування по датах немає — список
   статичний.
3. **Рухи залишку.** Число: скільки списано за тиждень — зведення періоду.
   Три круги не використовуються: щоденна дія тут одна — перегляд.
   Групування по датах: `.list-date-sep` «Сьогодні», «Вчора». Рядок
   `.row`: зліва `.list-anchor` зі значком засобу, посередині назва
   (`.t-md`) і під нею дрібно майстер і причина (`.t-xs.prose-muted`),
   справа число зі знаком: прихід `.amount-in` (`--color-success`, «+»),
   списання `.amount-out` (`--color-text`, «−»). Кольором позначений
   тільки прихід — виняток із звичайного списання.
4. **Журнали.** Число: скільки чек-листів не закрито сьогодні. Три круги:
   дезрозчин, стерилізація, прибирання. Рядок чек-листа `.row`: зліва
   квадрат-відмітка (порожній — рамка `--color-border-strong`; відмічений
   — заливка `--color-surface-2` з галочкою), посередині назва пункту,
   справа час і майстер, якщо відмічено, інакше риска. Відмічений рядок
   приглушується до `--color-muted`, без закреслення. Групування по датах
   немає — це чек-лист одного дня.
5. **Записи.** Число: скільки записів сьогодні, під ним «наступний о
   13:00» (`--color-faint`). Одна кнопка `.btn-primary` «Новий запис» —
   тут це головна дія екрана. Три круги не використовуються: у записів
   немає трьох рівнозначних щоденних дій. Групування по датах:
   `.list-date-sep` «Сьогодні», «Завтра», далі датами. Рядок `.row`: зліва
   `.list-anchor` з ініціалами клієнта, посередині ім'я (`.t-md`) і
   послуга під ним (`.t-sm.prose-muted`), справа значок статусу
   (`.badge-success` / `.badge-warn`) поруч із часом великим (`.t-lg`).
   Статус тільки значком, колір рядка не змінюється.
6. **Клієнт, картка.** Числа: два поряд — візити і сума за весь час, обидва
   `.num`, більші за підпис під ними (`.t-sm.prose-muted`), у плашках
   `--color-surface-2`. Три круги не використовуються. Блок формули
   (`.card-flat`, сітка 2×2): схема плетіння, канекалон, довжина,
   кількість пачок — кожне значення (`.t-md`) більше за підпис над ним
   (`.t-xs.prose-muted`). Алергії: окремий блок, фон `--color-warn-soft`,
   текст і значок `--color-warn`, показується тільки якщо алергія є.
   Групування по датах: `.list-date-sep`, історія візитів знизу, рядок —
   послуга зліва, сума справа.

**Что добавлено в globals.css в эту сессию (источник — Claude Design).**
Недостающие состояния `:active`/`:disabled` для `.btn-ghost`/`.btn-danger`/
`.btn-icon`; `:disabled`/`:read-only`/`-error` для `.input`/`.select`/
`.textarea` (специфичность у `-error` поднята составным селектором
`.input.input-error`, а не `!important` — см. правило выше); варианты
`.empty` (`-icon`, `-title`, `-desc`, `-actions`, тон `data-tone="danger"`);
`.shift-hero`/`.quick-actions` для крупного числа и трёх круглых кнопок;
`.list-date-sep`/`.list-anchor`/`.amount-in`/`.amount-out` для списков;
переходы между экранами (`.screen-enter`/`.screen-exit-scrim`/
`.screen-back-enter`/`.screen-back-exit`/`.tab-fade`/`.sheet-closing`/
`.notify-closing`/`.content-swap`) — вне `@layer`, рядом с drawer/sheet/
notify по тому же принципу. Два отклонения от присланного CSS исправлены
при вставке: `!important` у `-error`-классов убран в пользу специфичности,
`.screen-exit-scrim` переведён с хардкод-чёрного на `var(--color-bg)`
(интенсивность даёт анимация `opacity`, не альфа-канал цвета).

<a id="м16"></a>
## М16. Сборка (Codemagic)

**Состав:** `codemagic.yaml` · `scripts/patch-android.sh` ·
`scripts/patch-android-signing.sh` · `native-config/` · `resources/icon.png`.
Теги запуска: `ios-v*` / `android-v*` / `play-v*`.

**Версии Capacitor ПРИБИТЫ ТОЧКАМИ, без `^` (12.08.2026).** Обёртка
трижды ломалась на смене поведения между версиями (Cap 6 молча игнорировал
`iosScheme`, Cap 8 ведёт себя иначе), поэтому «свежий минор приехал сам»
в сборке для магазина недопустим. Зафиксировано:

```
@capacitor/core, cli, ios, android   8.5.0
@capacitor/app                       8.1.1
@capacitor/browser                   8.0.4
@capacitor/haptics                   8.0.2
@capacitor/keyboard                  8.0.5
@capacitor/preferences               8.0.1
@capacitor/push-notifications        8.1.2
@capacitor/splash-screen             8.0.2
@capacitor/status-bar                8.0.3
@capacitor/assets                    3.0.5
@capgo/capacitor-native-biometric    8.6.4
onesignal-cordova-plugin             5.5.2
```

В пайплайне `npm ci`, а не `npm install`: `ci` ставит ровно то, что в
`package-lock.json`, и падает, если lock разошёлся с `package.json`.
Поднимать версию — отдельным осознанным коммитом, не самотёком.

`ios/` и `android/` в `.gitignore`: папки пересоздаются `cap add`
в пайплайне и затирают любые правки в репозитории. Правки живут
в `scripts/patch-*.sh`.

Мины (оплачены DaKi):
- `environment.ios_signing` НЕ использовать: срабатывает ДО `cap add ios`,
  падает «No matching profiles». Подписывать скриптом ПОСЛЕ cap sync.
- Постоянный RSA-ключ подписи в env — иначе каждый билд плодит сертификат
  до лимита Apple.
- Entitlements ПРИВЯЗАТЬ к таргету (xcodeproj CODE_SIGN_ENTITLEMENTS),
  иначе APNs не выдаёт токен.
- Версия = счётчик Codemagic (get-latest-app-store-build-number для билда
  «в проверке» возвращает 1).
- Android собирается на mac_mini_m2 (linux недоступен на бесплатном).
- В perl-заменах разделитель `/` и никаких `{}` в замене; heredoc в YAML
  ломается — только вызов .sh.
- Purpose-строки (камера, FaceID, локация из-за OneSignal) +
  ITSAppUsesNonExemptEncryption=false. Splash перекрывать своим storyboard.
- **`UIBackgroundModes` = `remote-notification`** — добавлен 12.08.2026.
  Без него `cap sync` пишет «Configuration required for
  onesignal-cordova-plugin», а пуш не будит приложение в фоне: тихие пуши
  и доставка при свёрнутом приложении не работают вовсе.

<a id="м17"></a>
## М17. Команда і доступи

**Состав:** `app/app/team/` (page.tsx — всё читается на сервере, team-client.tsx,
loading.tsx) · `app/invite/[token]/` (page.tsx с развилкой «вход или
регистрация», accept-client.tsx) · `app/api/team/invite/route.ts` (письмо,
`runtime = 'nodejs'` ради sha256) · `mailTeamInvite` в `lib/email/templates.ts` ·
БД (миграции 0050–0054, 0076–0082): таблицы `invitations`, `permission_audit`,
`permission_templates`, `role_discount_caps`; колонки `tenant_members` —
`access_expires_at`, `discount_cap_pct`, `blocked_at` с причиной и автором;
функции `create_invitation`, `accept_invitation`, `revoke_invitation`,
`block_member`, `unblock_member`, `transfer_ownership`, `end_sessions`,
`team_overview`, `team_sessions`, `permission_audit_log`,
`apply_permission_template`, `discount_cap`, `effective_perm_set`,
`assert_grant_within`, `role_rank`; триггеры `tenant_members_guard`,
`tenant_members_audit`, `permission_audit_immutable`, `staff_access_guard`;
представление `team_access_log` · тесты `supabase/tests/09_team.sql`
и `10_contacts.sql`.
**Ручные настройки:** JWT-хук (М7) и `RESEND_API_KEY` — без второго ссылка
всё равно показывается на экране, письмо просто не уходит.
**Зависит от:** М7 (хук, `tenant_can`, ранги ролей), М8 (правила 1–3 и 7),
М2/М3 (приглашённый сначала входит или регистрируется). Очередь уведомлений
(М11) НЕ участвует: письмо шлётся прямо, потому что роут здесь — только
конверт для серверного ключа Resend, а ссылка нужна отправителю сразу.
**Перенос:** миграции 0050–0054 и 0076–0082 подряд → два экрана → роут
письма; переписать список ролей, `role_grants` и `role_discount_caps`.

**Правило модуля:** ни одно действие экрана не пишет в `tenant_members`
«своим» способом. Роль, дозволы, потолок и срок — обычный UPDATE, потому
что на нём висят и сторож, и неизменяемый журнал, и разрыв сеансов;
блокировка и передача владения — функции. «Быстрый путь» мимо делает
журнал неполным, и обнаруживается это в день разбирательства.

**Решения и грабли.** Первые пять — один класс ошибки: доступ меняется
несколькими путями, а проверку ставили на одном. Все пять работали на бою.

- **`create or replace` поверх чужой функции — это ПЕРЕПИСЫВАНИЕ.** 0076
  заменила `tenant_members_guard()` из 0052 и не вернула половину тела:
  исчезли ветка INSERT и проверка ранга (сотрудник с `team.write` вписывал
  себе роль владельца), а заодно умерли `transfer_ownership()` и
  `delete_my_account()` — они ставят транзакционные флаги, которых новый
  сторож не читал. Postgres на это не жалуется. Прежде чем писать
  `or replace` — прочитать действующее тело целиком и перенести руками всё,
  что не собирался трогать. Плюс: два триггера с разными именами на одной
  таблице звали одну функцию, и регресс из-за этого выглядел «сторожимым».
- **Ранний выход по списку колонок стареет молча.** Триггер журнала
  выходил, если не изменились роль и права, — а потом появились срок
  доступа, потолок скидки и блокировка, и каждая пошла мимо журнала И мимо
  разрыва сеансов: **заблокированный работал ещё час**, до истечения токена.
  Список признаков доступа теперь лежит в ОДНОМ месте с прямым указанием
  дописывать сюда новую колонку.
- **`permissions = {"*": true}` — это права владельца.** Хук кладёт в токен
  любой ключ со значением `true`, а `tenant_can` понимает `"*"` как «всё».
  Ключ запрещён ограничением на всех трёх таблицах, куда мог попасть, плюс
  общее правило: нельзя выдать право, которого нет у тебя самого.
- **Заблокированный разблокировал себя сам.** Сеанс рвётся, но токен на
  руках живёт до часа, и `team.write` в нём остаётся. Значит запрет обязан
  стоять В БАЗЕ: сторож запрещает любое РАСШИРЕНИЕ собственного доступа —
  снять свою блокировку, отдалить свой срок, поднять свой потолок скидки.
- **Через `staff` отбирали доступ у владельца.** Политики `staff` не
  ограничивают колонок, а проверка доступа смотрела на `staff.blocked_at`:
  админ вписывал строку с `user_id` владельца — и тот терял вход без
  возможности починить. Теперь доступ определяет ТОЛЬКО
  `tenant_members.blocked_at`, а `staff.blocked_at` значит «не працює»
  и правится исключительно `block_member`/`unblock_member` через
  транзакционный флаг.
- **Три разных «не работает» нельзя смешивать:** нет доступа
  (`blocked_at`), не работает — карточка мастера погашена
  (`staff_blocked_at`), не принимает записи — отпуск (`staff_is_active`).
  `team_overview` сначала склеивала первые два через `coalesce`, и мастер
  в отпуске показывался лишённым доступа: из одного значения два обратно
  не достаются. **Экрана карточки мастера в проекте нет**, поэтому второе
  и третье состояния видно, но переключить их негде — честный долг,
  а не забытая кнопка.
- Тем же заходом нашлось, что у `authenticated` есть INSERT/UPDATE/DELETE
  на представлениях `v_bookings`, `v_orders` и `team_access_log`, которых
  не выдавала ни одна миграция, — право читать давало право править мимо
  RLS. Механизм и лечение — П-А; проверять при заведении КАЖДОГО
  представления, это уже седьмой раз.
- **Приглашение.** Секрета в базе нет — только `sha256`, сам токен
  показывается один раз в ответе `create_invitation`. Роут письма ОБЯЗАН
  сверять хеш присланного токена: без сверки в письмо от имени бренда
  кладётся любой адрес нашего же домена, и одного живого приглашения
  хватает, чтобы рассылать такие письма сколько угодно. Права в роуте
  проверяются заново, адрес собирается из токена, а не из тела запроса.
- У `authenticated` были INSERT и UPDATE прямо на `invitations`, а приём
  брал роль и права ИЗ СТРОКИ — приглашение себе с ролью `admin`
  выписывалось одним INSERT. Роль проверяется НА МОМЕНТ ПРИЁМА: выписавший
  мог за 72 часа лишиться `team.write`.
- **Форма `permissions` — ОБЪЕКТ, а не массив.** Массив ломает
  `jsonb_each_text` в хуке выдачи токена: человек не «видит меньше
  разделов», а НЕ ВХОДИТ, и починить из интерфейса нельзя — интерфейс
  за токеном. Ограничение стоит на всех трёх путях записи и на самой
  колонке. Тест на это писал массив и был зелёным, потому что хук в стенде
  не зовётся.
- **Ключ в `permissions` — это ОТКЛОНЕНИЕ от роли, а не копия её набора.**
  Совпало с ролью — ключ убираем, иначе набор роли застывает слепком
  и завтрашняя правка `role_grants` этого человека уже не касается.
- **Галочки пишутся очередью, и наборов два.** Права уходят целым объектом,
  а пропсы обновляются только после перезагрузки данных: три быстрых клика,
  посчитанных от пропса, отменяли друг друга. `pending` — каким набор
  СТАНЕТ, от него считается следующий; `saved` — что подтвердил сервер,
  и рисуется галочка из него.
- **Сеансы читаются из `auth.sessions`, своей таблицы нет.** Копия
  неминуемо разъедется (сеанс истёк сам, человек вышел с телефона), а
  список «активных», где половина мертва, хуже отсутствующего: по нему
  принимают решение «выгнать». Принудительный выход сверяет ранг —
  иначе младший разлогинивает владельца бесконечно.
- **Ранг ролей на экране — числа `role_rank`, а не порядок в списке.**
  У бухгалтера и мастера ранг РАВЕН, и сравнение по индексу массива
  прятало законные действия. Экран этим только решает, показывать ли
  кнопку; отказ всё равно выносит база, и её текст (украинский, для
  человека) показывается дословно.
- **После приёма приглашения и после передачи владения — жёсткая
  перезагрузка, а не мягкий переход.** Токен на руках старый: в первом
  случае в нём нет нового заведения (кабинет без разделов читается как
  «приглашение не сработало»), во втором он всё ещё говорит «owner»,
  и человека молча выбрасывает через несколько минут.
- Приглашение принимается только той почтой, на которую выписано, поэтому
  не вошедшему показывается РАЗВИЛКА «увійти / створити акаунт», обе двери
  с `next` на этот же адрес. Прежний `redirect('/login')` терял адрес
  возврата на ссылке «створити акаунт», и половина приглашённых оказывалась
  в пустом кабинете.

<a id="м18"></a>
## М18. Журнал доступа к данным

**Состав:** БД (миграции 0089–0092, 0095, 0097–0099): функции обезличивания
`mask_phone`, `mask_email`, `mask_name`, `mask_text_pii`, `redact_pii`;
триггеры `security_events_redact`, `notification_outbox_redact`; точка записи
`log_data_access`; читающие функции `customer_card`, `customers_export`,
`document_access`; представление `data_access_log`; срок хранения
`retention_sweep` и `retention_anonymize_contacts` · приложение:
`app/app/team/data-access-log.tsx` (третий журнал экрана команды),
`app/app/customers/customers-client.tsx` (карточка и выгрузка),
`documentSignedUrl` в `lib/documents.ts`, вызов `log_data_access`
в `app/app/journals/report/route.ts`, `lib/redact.ts` (та же маскировка
на стороне приложения) · тест `supabase/tests/24_step7.sql` · разбор
незакрытого — `notes/pii-leaks.md`.
**Ручные настройки:** нет.
**Зависит от:** М7 (`tenant_can`, `tenants_with`), М8 (правила 1, 2, 7),
М10 (журнал живёт в `audit_log`, а не в своей таблице), М17 (экран команды —
место, где журнал показывается).
**Перенос:** 0089 → 0090 → 0092 → 0095 → экраны. 0091 и 0097 (срок хранения)
переносятся отдельно и позже: они удаляют и переписывают данные, и список
правил в них — про конкретный продукт, а не про механику.

**Правило модуля: читать чувствительное можно только функцией, которая
тем же вызовом пишет строку.** Интерфейс обходится — supabase-js бьёт
в PostgREST напрямую, и любой, у кого есть токен, читает таблицу мимо
нашего экрана. Журнал, который ведёт экран, в этом случае молчит.
Поэтому `document_access` отдаёт ПУТЬ к файлу, а не только разрешение:
подписать ссылку по пути из уже загруженного списка технически можно,
и ровно поэтому единственный путь идёт через функцию.

**Грабли:**

- **Журнал, в который можно писать, ничего не доказывает.** `data_access_log`
  — обычное автообновляемое представление и НЕ `security_invoker`. Права
  забыли отобрать у `authenticated` (0090), и любой вошедший мог вставить
  строку с чужим `actor_id` — проверено исполнением. Закрыто 0095.
  Общая форма грабли — в П-А, первая.
- **Уборка по сроку сама переносила данные вместо удаления.** `audit_row`
  висит на `invitations`, которые уборка удаляет: каждое удаление копировало
  строку с почтой и хэшем токена в `audit_log`. Закрыто флагом
  `app.retention_sweep` (0097) — той же формы, что щель 0092.
- **Маскировка в функции ничего не стоит, пока колонка открыта напрямую.**
  `customer_card` честно маскировал телефон тому, у кого нет
  `customers.contacts`, а `GET /rest/v1/customers?select=phone` отдавал базу
  целиком и мимо журнала. Колонку в RLS не скроешь — закрыто правами
  НА КОЛОНКУ (0099).
- **Подпись в журнале сама проходит обезличивание, и это ломает даты.**
  `mask_text_pii` вырезает последовательности цифр вместе с пробелами
  и дефисами, поэтому «01.01.2026 - 31.12.2026» превращается в
  «<номер прихований>». Периоды пишутся словом «по» (0098).
- **`STABLE`-функция не может писать в журнал.** Postgres не выполняет
  изменяющие данные операторы в не-VOLATILE функции. `compliance_report`
  пришлось перевести в `volatile` (0098) — цена нулевая, вызов один на отчёт.

<a id="м19"></a>
## М19. Доступ сотрудников платформы

**Состав:** БД (миграция 0093): таблица `platform_access_grants`, функция
`has_platform_access`, сторож `platform_access_guard`, письмо владельцу
`platform_access_notify` плюс шаблон `platform.access_granted`,
переписанные политики `profiles_self_read`, `tenant_members_read`,
`tenants_read` · приложение: `app/platform/access/` (page + client),
`app/api/platform/access/route.ts` (сервисный ключ), `isPlatformStaff`
в `lib/tenant.ts` · тест — раздел 0093 в `supabase/tests/24_step7.sql`.
**Ручные настройки:** `SUPABASE_SERVICE_ROLE_KEY` (роут выдачи) —
он же нужен очереди уведомлений, отдельно заводить нечего.
**Зависит от:** М7 (признак `is_staff` кладёт в токен тот же хук),
М11 (письмо владельцу идёт очередью), М17 (форма срока доступа взята
у инспектора, 0054, — второго способа делать то же самое не заводим).
**Перенос:** одна миграция плюс два файла приложения. Переписать
`is_platform_staff()` под свою модель «кто сотрудник платформы».

**Правило модуля: признак — необходимое условие, но не достаточное.**
До 0093 `is_platform_staff()` открывал профили всех людей платформы,
состав всех команд и карточки всех заведений с ИНН — бессрочно, без причины
и без следа. Теперь нужен ещё действующий грант на конкретное заведение.
Пока сотрудник платформы один, разница незаметна; она появляется
в день, когда появится второй.

**Грабли:**

- **Грант выдаёт платформа, а не арендатор, и это решение.** Обращение
  в поддержку приходит владельцу платформы, а не в базу клиента. Просить
  клиента «нажмите кнопку выдачи доступа» — значит получить либо отказ,
  либо привычку нажимать не глядя. Поэтому политик на INSERT и UPDATE
  у таблицы НЕТ, а выдача идёт сервисным ключом. Клиент не выдаёт доступ,
  но УЗНАЁТ о нём письмом и видит его у себя.
- **Признак читается из базы, а не из токена — там, где он что-то решает.**
  Для рисования экрана хватает клейма (`lib/tenant.ts`); для выдачи доступа
  к чужим данным — нет: снятый вчера признак не должен работать сегодня.
  Роут спрашивает `profiles.is_staff` напрямую.
- **Списка заведений на экране выдачи нет намеренно.** Заведение
  указывается слагом из обращения. Выпадающий список всех заведений
  платформы — это и есть та база, доступ к которой ограничивает вся
  эта механика.
- **Потолок 30 дней — в ограничении таблицы, а не в форме.** Разбор
  обращения занимает часы. Грант «до конца года» — тот же вечный ключ,
  только с датой.
- **Служебный адрес отвечает 404, а не «нет прав».** Существование
  механики доступа — это уже сведения.

<a id="м20"></a>
## М20. Карточка мастера

**Состав:** БД (миграция 0101): триггер `working_hours_no_overlap`
и функция `add_time_off`; таблицы `staff`, `working_hours`, `time_off`,
`staff_services` и политики к ним пришли ещё с 0010 · приложение:
`app/app/bookings/staff/` — `page.tsx` + `staff-list.tsx` (список),
`[id]/page.tsx` + `[id]/staff-card.tsx` (карточка), `range.ts` (разбор
`tstzrange`), два `loading.tsx` · навигация: две строки в `HEADINGS`
(`components/app-shell.tsx`) и ссылка из `bookings-client.tsx` ·
словарь: ключи `staff.*` и `app.screen.staff*` в `uk.json` ·
тест — `supabase/tests/25_staff_card.sql`.
**Ручных настроек нет.**
**Зависит от:** М7 (обе оси доступа), М14 (живёт ВНУТРИ раздела записей,
а не пунктом панели), М17 (блокировка карточки ставится только оттуда).
**Перенос:** миграция плюс папка экранов. Часовые пояса в `ZONES`
переписать под свою страну.

**Зачем модуль вообще появился.** Таблицы лежали с 0010, а входа к ним
не было ни одного: `staff.is_active` («у відпустці, записи не приймає»)
переключать было негде. При этом экран команды это состояние ПОКАЗЫВАЛ
и отсылал «в картку майстра — розділ «Записи», не тут». Ссылка в никуда
у первого платящего клиента: салон, у которого мастер ушёл в отпуск,
снять его из записи не мог вообще.

**Три состояния мастера не сводятся в одно, и это главное правило модуля.**
`blocked_at` — доступ в кабинет закрыт и карточка погашена заодно;
`is_active = false` — отпуск, доступ есть, записей нет; пустой
`working_hours` — формально работает, а слотов не будет ни одного.
Снимаются они в трёх разных местах, поэтому и показываются раздельно.
Кнопки «розблокувати» в карточке НЕТ намеренно: блокировку ставят и
снимают только `block_member`/`unblock_member` — они пишут в неизменяемый
журнал прав и рвут сеансы (М17). Второй путь означал бы неполный журнал,
и обнаружилось бы это в день разбирательства.

**Грабли:**

- **Пустой список услуг мастера значит «ВСЕ услуги», а не «никакие».**
  Так устроен подбор слотов в 0010: `not exists (...) or exists (...)`.
  Экран обязан говорить это словами — иначе пустые галочки читаются как
  «нічого не робить», и администратор идёт проставлять их вручную,
  создавая ровно то ограничение, которого не было.
- **Границы отпуска считает БАЗА, в часовом поясе мастера.** Человек
  задаёт дни, `time_off.period` — это моменты. Считать их в браузере
  значит записать в данные пояс ТОГО, КТО ЗАВОДИТ: администратор из
  Варшавы промахнётся на час, а на границе суток — на сутки, и последний
  день отпуска окажется рабочим. Ошибка невидимая: в списке всё верно,
  а в слотах появляется приём. Отсюда `add_time_off` (0101), считающая
  границы тем же `at time zone s.timezone`, которым `available_slots`
  разворачивает рабочие часы.
- **Верхняя граница — начало СЛЕДУЮЩЕГО дня со скобкой `[)`.** «По 25-е»
  человек понимает как весь 25-й день. Показывать эту границу как есть
  нельзя — «до 26 серпня» читается как лишний день, поэтому `lastDay()`
  отнимает сутки при выводе.
- **Пересекающиеся промежутки дня запрещены триггером, а не формой.**
  `available_slots` гонит `generate_series` по КАЖДОМУ окну: два
  пересекающихся дают один и тот же час дважды в списке слотов.
  Соприкасающиеся (09:00–13:00 и 13:00–18:00) разрешены — это обед,
  самый частый способ заполнения дня.
- **`weekday` в базе — это `extract(dow)`, где 0 — воскресенье.**
  Показывать в этом порядке нельзя: неделя, начинающаяся с воскресенья,
  читается как ошибка. Порядок отображения (`WEEK`) разведён со значением
  колонки ровно в одном месте и больше нигде.
- **Права у блоков РАЗНЫЕ.** `staff` и `staff_services` — `team.write`,
  `working_hours` и `time_off` — `orders.write` (так стоят политики 0010
  и 0013). То есть администратор записей ведёт расписание, но не
  переименовывает мастера. Признаки уходят в компонент раздельно —
  иначе экран нарисовал бы кнопку, на которую база ответит отказом.
- **Отпуска отбираются ПЕРЕСЕЧЕНИЕМ, а не сравнением.** Сравнение
  диапазонов в Postgres идёт по нижней границе, поэтому `period >= now`
  выбрасывает отпуск, начавшийся вчера и идущий сегодня, — то есть ровно
  тот, ради которого экран и открывают. Нужен `overlaps` c `[now,)`.

**Чего в модуле пока нет:** аватара мастера (`avatar_path` в таблице
есть, загрузки нет), порядка в списке (`position` правится только из
базы), привязки карточки к участнику команды — `staff.user_id`
проставляется извне, и карточка про это честно говорит.

<a id="м21"></a>
## М21. Выгрузка заведения

**Состав:** БД (миграция 0102): функция `tenant_export(tenant, section)` ·
приложение: `app/app/settings/export/` (page + client + loading),
`lib/export/csv.ts` · навигация: строка в `HEADINGS`
(`components/app-shell.tsx`) и блок «Ваші дані» в `settings-client.tsx` ·
словарь: ключи `export.*`, `settings.export.*`, `app.screen.export.*` ·
тест — `supabase/tests/26_tenant_export.sql`.
**Ручных настроек нет.**
**Зависит от:** М7 (права разделов), М18 (каждый вызов пишется в журнал
доступа), 0089 (маскирование контактов).
**Перенос:** одна функция плюс папка экрана. Список разделов в функции
и в `SECTIONS` клиента — единственные два места, которые правятся при
добавлении новой сущности.

**Зачем.** В условиях сделки записано: «его данные — его собственность
с выгрузкой в любой момент». Выгрузка существовала одна — база клиентов.
Продавец, который не может забрать свои данные, привязан к платформе
не удобством, а невозможностью уйти, и первый же вопрос «а если вы
закроетесь?» остаётся без ответа.

**Грабли:**

- **Права разделов, а не право «выгрузить всё».** Раздел отдаётся по тому
  же праву, что и его экран: заказы — `orders.read`, склад — `stock.read`,
  журналы — `compliance.read`, деньги — `finances.read`. Отдельное право
  на выгрузку обошло бы все остальные одним нажатием.
- **Контакты маскируются тем же правилом, что и на экранах.** Без
  `customers.contacts` телефон и почта уходят в файл через `mask_*` (0089).
  Выгрузка, отдающая больше экрана, — это дыра с кнопкой «скачать».
  И экран обязан сказать об этом ДО скачивания: файл со звёздочками
  бесполезен для переноса, а понять это по самому файлу можно только
  после того, как его открыли.
- **По разделам, а не одним вызовом.** «Отдай всё» одним jsonb — это
  десятки мегабайт, собранные в память целиком. Разделы вызываются
  отдельно; файл склеивает приложение.
- **Незнакомый раздел — ошибка, а не пустой массив.** Молча вернуть `[]`
  значило бы отдать человеку файл без его данных, и узнает он об этом
  тогда, когда они понадобятся.
- **Формат «всё сразу» — JSON, а не архив.** Zip в браузере требует
  библиотеку, несколько CSV подряд браузер блокирует как множественную
  загрузку. JSON сохраняет вложенность: позиции заказа, партии засоба,
  расписание мастера. CSV остаётся на каждый раздел отдельно — его
  открывают в Excel, и там `;` плюс BOM обязательны (`lib/export/csv.ts`).
- **Страница ничего не читает на сервере.** Данные тянет клиент вызовами
  функции. Прочитать всё «на всякий случай» при рендере значило бы писать
  в журнал выгрузку заведения при каждом ОТКРЫТИИ страницы — журнал
  перестал бы отличать «зашёл посмотреть» от «унёс всё».

**Чего в модуле нет:** файлов хранилища (MSDS, сертификаты, фото) —
выгружаются только данные; выгрузки по периоду — отдаётся всё целиком.

<a id="м22"></a>
## М22. Возвраты

**Состав:** БД (миграция 0103): таблицы `returns`, `return_items`,
`return_counters`, сторожа `returns_guard` и `return_items_guard`,
функция `create_return` · приложение: `app/app/orders/[id]/returns-block.tsx`
плюс запрос и передача признаков в `app/app/orders/[id]/page.tsx` ·
словарь: ключи `returns.*` · тест — `supabase/tests/27_returns.sql`.
**Ручных настроек нет.**
**Зависит от:** М9 (склад: возврат идёт через `record_stock_movement`),
0007 (финансы: доход гасится встречным расходом), М7 (два права).
**Перенос:** одна миграция плюс один компонент. Если в проекте нет
финансов — убрать вставку в `finance_records`, остальное не изменится.

**Зачем.** В правилах домена возврат описан с самого начала, а построен
не был: товар, принесённый обратно, продавец «возвращал» ручной
корректировкой остатка, а доход по заказу оставался в финансах навсегда.
Отчёт о прибыли врал ровно на сумму возвратов.

**Грабли:**

- **Доход не удаляется, а гасится встречной записью.** `finance_records`
  неизменяемы по той же причине, что и журнал остатка: правка задним
  числом превращает учёт в мнение. В отчёте видно две строки — продали
  и вернули, — а не «продажи не было».
- **На склад возвращает `record_stock_movement`, а не прямой UPDATE**
  (правило 5). Тип движения `return` лежал в перечислении с 0003
  и не использовался ни разу.
- **Строки схлопываются по позиции ДО проверок.** Одна и та же позиция,
  присланная дважды по одной штуке, иначе проходит обе проверки
  по отдельности и возвращает на склад больше проданного.
- **Вся проверка — до первой записи.** Отказ на середине оставил бы
  неизменяемый документ без части движений склада: ни дописать,
  ни стереть.
- **Сумма проставляется при вставке, а не апдейтом после сбора позиций.**
  Документ неизменяем с первой миллисекунды, и собственный сторож
  не пропускает даже свой же `update ... set total`. Это ловится
  сразу же — тестом, а не на бою.
- **Два права, а не одно.** `orders.write` (документ по заказу) И
  `stock.write` (товар ложится на склад). Иначе продавец, которому склад
  не доверен, меняет остаток через оформление возврата. Второе право
  спрашивается ТОЛЬКО когда в возврате есть позиция со складским учётом:
  возврат услуги склада не касается.
- **Финансовую запись функция создаёт без `finances.write`** — как и
  оплата заказа создаёт доход триггером (0007). Учёт правится следствием
  действия, а не отдельным разрешением: иначе половина возвратов
  оставляла бы доход висеть.
- **Сторож проверяется от роли, которой выданы права таблицы.**
  У `authenticated` нет INSERT/UPDATE на `returns` вовсе, поэтому в тесте
  отказ пришёл бы от ГРАНТА, а тело триггера не исполнилось бы ни разу —
  зелёный тест на пустом сторожа. В `27_returns.sql` эта проверка идёт
  после `reset role`.

**Чего в модуле нет:** возврата записи на услугу (только заказы), частичного
возврата денег и статуса заказа `returned` автоматом — статус переводит
человек, потому что возврат части заказа его не меняет.

<a id="м23"></a>
## М23. Відгуки і рейтинг

**Состав:** БД (миграция 0104): таблица `reviews`, сторожа `reviews_guard`
и `offerings_rating_guard`, триггер пересчёта `reviews_recompute_rating`,
функция `create_review` · приложение: `app/account/review-button.tsx`
(кнопка «Оцінити» на `/account`) и блок рейтинга на `app/t/[slug]/page.tsx`
· словарь: ключи `account.review.*`, `public.storefront.rating.*` ·
тест — `supabase/tests/28_reviews.sql`.
**Ручных настроек нет.**
**Зависит от:** М9/0007 (правило неизменяемости — тот же приём, что
у `finance_records` и возвратов), 0002 (колонки `rating_avg`/`rating_count`
лежали с самого начала каталога).
**Перенос:** одна миграция плюс два места в приложении. Если в проекте
нет записей (только заказы) — убрать ветку `booking`, `order` остаётся.

**Зачем.** `offerings.rating_avg`/`rating_count` лежали с 0002, `storefront()`
их отдавал с 0016, фронт их даже типизировал — и НИГДЕ не показывал.
Значений тоже не было: рейтинг существовал как мёртвые данные три уровня
подряд (колонка → RPC → тип), ни разу не значение.

**Единственная защита рейтинга, которая работает: отзыв только от того,
у кого есть выполненный заказ или запись.** Без этого условия рейтинг
ничего не значит — любой вошедший мог бы накрутить пятёрки своему
заведению или обвалить чужое.

**Грабли:**

- **Отзыв привязан к позиции заказа (`order_items.id`), а не к заказу
  целиком.** Заказ из трёх разных товаров — непонятно, к какому из них
  относить общий отзыв. У записи (`bookings.id`) этой проблемы нет:
  запись — всегда одна услуга.
- **Партиционированный unique-индекс ВСЁ РАВНО составной с `tenant_id`**
  (правило 1), хотя `order_item_id`/`booking_id` и так глобально
  уникальны — это чужие первичные ключи. Правило одно на весь проект без
  исключений, и именно так его проверяет `06_isolation.sql`; без
  `tenant_id` первым столбцом прогон падает с «уникальний індекс без
  tenant_id», даже если по факту дублей быть не может.
- **`rating_avg`/`rating_count` были доступны для прямой правки три
  миграции подряд.** `guard_stock_columns` (0003) их не касается вовсе —
  рейтинг, который продавец правит себе сам через обычный `UPDATE
  offerings`, не механизм доверия, а его подделка. Закрыто отдельным
  сторожем по образцу `guard_stock_columns`, с той же калиткой
  (`vitrina.allow_rating_write`), что открывается ровно на один UPDATE
  триггером пересчёта и закрывается обратно.
- **Отзывы неизменяемы** — тот же приём, что у `finance_records` (0007)
  и возвратов (0103). Продавец не может выпросить правку задним числом,
  разгневанный покупатель не может тихо смягчить оценку после спора.
  Опечатку в тексте не исправить — это цена доверия к цифре.
- **Порог «мало оцінок» — ровно то, что названо в правилах домена,
  и нигде не подобран на глаз:** меньше пяти отзывов показывает текст,
  а не число; ноль отзывов не показывает вовсе ни то, ни другое. Единичная
  пятёрка от знакомого не должна выглядеть как проверенная репутация.
- **Публичный список текстов отзывов не показывается.** Страница карточки
  товара с описанием и галереей в продукте пока не существует — есть
  только список позиций на `/t/[slug]`. Класть туда текст отзыва
  РАНОВАТО: девятая анонимная точка входа появилась бы раньше, чем нашлось
  бы, где её честно показать. Средний балл и число оценок сейчас идут через
  уже существующий `storefront()` (0016) — это расширение открытой точки,
  а не новая.

**Чего в модуле нет:** страницы отзывов продавца в кабинете (только
таблица читается напрямую через `catalog.read`), ответа продавца на отзыв,
публичного списка текстов (см. выше).

<a id="м24"></a>
## М24. Атрибуція

**Состав:** БД (миграция 0105): тип `attribution_source`, колонки
`orders.attribution_source/label`, `bookings.attribution_source/label`,
таблица `attribution_events`, функция `attribution_resolve`, три новых
опциональных параметра у `create_order` и `create_booking` · приложение:
`lib/attribution.ts` (localStorage, 30 дней, последний переход побеждает),
`components/attribution-capture.tsx` (читает `?from=` на `/t/[slug]`
и на `/t/[slug]/book/[offering]`), `booking-flow.tsx` (передаёт атрибуцию
в `create_booking`), ссылки `?from=search` в `app/search/page.tsx`
и `?from=map` в `app/map/map-view.tsx` · тест — `supabase/tests/29_attribution.sql`.
**Ручных настроек нет.**
**Зависит от:** М9/0087 (тот же приём «никогда не роняет действие»,
что у ограничителя частоты), правило 7 (закрытый список из восьми
анонимных точек — `create_order`/`create_booking` расширены, не добавлена
девятая).
**Перенос:** одна миграция плюс три маленьких файла приложения.

**Зачем.** «Продавец кладёт в шапку Instagram ссылку
`cres-ca.com/t/<slug>?from=ig`; переход по ней помечает заказ или запись
как «привёл продавец сам»» — то, из чего в будущем считается счёт
(«0% с заказов, которые продавец привёл сам, комиссия только
с приведённых нами»). Без атрибуции биллинг невозможен в принципе.

**Коррекция.** Прежняя запись в CLAUDE.md утверждала «таблицы под это
есть, механики нет» — неверно: не существовало ни `attribution_events`,
ни колонок на заказах/записях. Поймано при построении модуля.

**Находка по пути, не про атрибуцию.** `create_order` не вызывается НИ
ОТКУДА в приложении: у витрины нет ни кошика, ни формы оформления заказа
на товар, кнопка есть только у услуг («Записатися» → `create_booking`).
Это самостоятельный, крупный и не отменённый пробел — CHECKED против ТЗ
первого клиента (`notes/tz-conformance.md`): салон плетения продаёт
услуги, а не товары, и по ТЗ кошик не нужен. `create_order` всё равно
получил параметры атрибуции — расширение существующей анонимной точки
дешевле, чем ждать отдельной миграции, когда кошик наконец появится.

**Грабли:**

- **Отсутствие атрибуции — это ТОЖЕ «свой», а не «неизвестно».** Модель
  денег построена от обратного: «0% — дефолт, комиссия только при
  ДОКАЗАННОМ канале платформы». Ошибка в пользу продавца дешевле ошибки
  в пользу платформы.
- **`attribution_resolve()` не бросает исключений НИ ПРИ КАКОМ входе** —
  неизвестное значение, null, дата из будущего, дата старше 30 дней —
  всё превращается в `null`, оформление идёт как обычно. Тот же урок,
  что и у 0087: механика, которая в сомнении отказывает, ломает продажи
  первым же вызовом с испорченными данными, а cookie испортить легче,
  чем заголовок HTTP.
- **Атрибуция живёт в `localStorage`, а не в cookie.** `create_order`/
  `create_booking` вызываются из БРАУЗЕРА, а не читаются на сервере —
  второго пути к данным (например, через middleware) не заводим,
  пока он не нужен ничему другому.
- **`attribution_events` — журнал КОНВЕРСИЙ, а не журнал ВСЕХ переходов.**
  Строка появляется только вместе со своим заказом/записью, в той же
  транзакции. Логировать каждый визит анонимного посетителя значило бы
  открыть анониму девятую точку записи в базу — отдельное решение,
  которое здесь сознательно не принято (правило 7).
- **Партиционированный unique-индекс всё равно составной с `tenant_id`**
  (правило 1) — тот же приём, что в 0103 и 0104.
- **Сотруднику, оформляющему заказ вручную, атрибуция не считается.**
  Ручной заказ — это и есть «привёл продавец сам» по определению;
  запись cookie гостя, случайно сохранившейся в браузере сотрудника,
  не должна перепутать источник.
- **Добавление параметра в `create_order`/`create_booking` — это DROP
  старой сигнатуры, а не `create or replace`.** Иначе появился бы второй
  оверлоад рядом со старым и вызовы стали бы неоднозначными (тот же урок,
  что у `decant_container`, 0100). Тело обеих функций перенесено
  ДОСЛОВНО — сверено побайтово через `pg_get_functiondef` до и после.
- **`apply_migration` (инструмент агента) один раз стёр `--`-комментарии
  внутри тел `create_order`/`create_booking`/`attribution_resolve` при
  первом применении**, оставив исполняемую логику дословной (проверено
  количеством `end if`, наличием вставки в `attribution_events` и вызова
  `attribution_resolve`). Обнаружено сверкой длины тела с локально
  собранной базой, а не глазами. Исправлено переприменением тех же
  функций дословно из файла; лишняя строка в `supabase_migrations.
  schema_migrations`, оставшаяся от повторного применения, удалена —
  иначе «Звірка міграцій» покраснела бы на имени без файла.

**Чего в модуле нет:** самого кошика/чекаута витрины (см. находку выше);
разбивки комиссии по счёту (биллинга нет вообще — CLAUDE.md → «Биллинг
и подписка»); значения `referral` (заведено под будущую реферальную
механику, сейчас не выставляется никем); тега для главной страницы
маркетплейса (`app/page.tsx`) — намеренно не трогалась, чтобы не гадать
между `search` и несуществующим отдельным значением.

<a id="м25"></a>
## М25. Ответ базы человеку

**Состав:** `lib/errors/db.ts` (`dbErrorText`) · ключи `error.db.*`
в `uk.json` · 13 экранов переведены с прямого показа, 5 экранных
переводчиков склада получили новый запасной путь, `teamErrorText`
получил обезличивание · миграций НЕТ — это целиком клиентский слой.
**Ручных настроек нет.**
**Зависит от:** `lib/redact.ts` (`maskText` — точная копия
`public.mask_text_pii` из 0089), словаря локализации.
**Перенос:** один файл плюс блок ключей. Таблица `BY_CODE` переписывается
под свои коды, если база не Postgres.

**Зачем.** 71 место показывало человеку `error.message` как есть.
Postgres при нарушении уникальности печатает значение поля:

```
duplicate key value violates unique constraint "customers_tenant_phone_uidx"
DETAIL: Key (tenant_id, phone)=(…, +380671234567) already exists.
```

То есть телефон клиента уезжал на экран, а оттуда ушёл бы в любой
сборщик ошибок. Внутрь базы такой текст уже не попадал (0089 обезличивает
`security_events.detail` и `notification_outbox.last_error`) — наружу
уходил целиком.

**Правило разбора. Три ветки, и они не пересекаются:**

1. **`P0001`** — `raise exception` из нашей функции. Текст писали мы,
   по-украински, для человека. Показываем, но через `maskText`:
   в подстановку `%` могло уехать значение поля.
2. **Известный SQLSTATE** — своя подпись из словаря. «duplicate key
   value violates unique constraint» не объясняет мастеру ничего.
3. **Всё остальное** — общая подпись плюс КОД в скобках, сырой текст
   в `console.error`. Код не персональные данные, и по нему поддержка
   может спросить, что было.

**Грабли:**

- **Экранные переводчики не удалены, и это решение.** `humanize`
  в складе даёт лучшую подпись, чем общий случай: он знает, что
  `current_stock_check` — это «остаток ушёл бы в минус». Заменён только
  их запасной путь: вместо `return message` теперь `return dbErrorText(…)`.
  Удалить их значило бы обменять точность на единообразие.
- **Ошибки входа сюда НЕ идут.** У GoTrue свой разбор
  (`lib/auth-errors.ts`), и его сообщения — английский текст протокола,
  по которому узнаётся причина. Смешать два разбора значит потерять оба.
- **Сеть отделена от базы первой проверкой.** «Немає звʼязку» и «такий
  запис уже є» требуют от человека разного: подождать против исправить.
  Общая подпись на оба случая заставляет гадать.
- **`maskText` объявлен как `string | null`** ради null на входе. Внутри
  ветки вход заведомо непустой, поэтому `?? raw` — это про тип, а не
  про поведение.
- **Показывать код, а не текст.** Соблазн «покажем хоть что-то, чтобы
  можно было отладить» — это и есть та дыра, которую чинили: отладочная
  подробность и персональные данные приезжают в одной строке.

<a id="м26"></a>
## М26. Підтвердження нотифікації

**Состав:** БД: 0106 (колонки `materials.notification_doc_id`
и `notification_confirmed_at`, функции `confirm_notification`
и `revoke_notification`, частичный индекс, представление
`compliance_materials`), 0107 (`compliance_report` печатает реестр
косметики) · приложение: блок в `materials/[id]/docs/material-docs.tsx`
и метка на `materials/[id]/material-card.tsx` · словарь: ключи
`inventory.docs.moz.proof.*`, `inventory.material.moz.proof.*` ·
тест — `supabase/tests/30_notification_proof.sql`.
**Ручных настроек нет.**
**Зависит от:** М10 (документы в приватном бакете), 0083 (инспектор
читает только `compliance_*`).
**Перенос:** две миграции плюс блок в экране документов.

**Зачем.** Переходный период Технического регламента №65 КМУ закончился
3 августа 2026 года. Косметика без нотификации в обороте — нарушение,
и это главный аргумент при продаже. В базе лежал КОД нотификации
(0014, 0059), но код — это слова поставщика; проверка смотрит документ.
Отличить «код вписан со слов» от «документ в папке» было нельзя ничем,
кроме как открыть список документов глазами.

**Грабли:**

- **Автоматической проверки в реестре МОЗ НЕ СУЩЕСТВУЕТ и не появится.**
  Нотифицируют производители и импортёры, полный состав доступен только
  уполномоченным органам в закрытой части системы; публичного поиска нет,
  парсить нечего. Задача поэтому переформулирована: не «проверить
  в реестре», а хранить доказательство и не дать о нём забыть. Обещать
  клиенту автоподгрузку нельзя.
- **Связь ставится функцией, а не UPDATE.** Внешний ключ гарантирует лишь
  существование строки документа, но не то, что она про ЭТОТ засіб.
  Подтверждение, указывающее на MSDS соседнего средства, — это зелёная
  галочка, которая снимает вопрос, не отвечая на него. Функция сверяет
  заведение, принадлежность и вид.
- **Две колонки, а не одна.** Дата без ссылки на документ — утверждение
  без доказательства; ссылка без даты не отвечает на вопрос инспектора
  «с какого момента у вас это есть».
- **Запрета заводить косметику без нотификации НЕТ, и это решение.**
  Продавец получает средство физически раньше, чем документ от
  поставщика. Запрет заставил бы его либо ждать, либо врать в карточке.
  Вместо запрета — видимость: признак в представлении, красная метка
  на карточке, раздел в отчёте.
- **`on delete set null`, а не каскад.** Удаление файла не уносит карточку
  засоба, но признак честно гаснет — доказательства больше нет.
- **Порядок колонок представления взят С БОЯ.** `create or replace view`
  требует того же порядка ведущих колонок, а 0059 вставила
  `notification_url` и `notification_date` В СЕРЕДИНУ. Скопировав список
  из 0035, получаешь «cannot change name of view column» на накате с нуля.
- **Отчёт — отдельная миграция (0107).** Он переписывает чужую функцию
  целиком; мешать это с колонками в одном файле значит получить миграцию,
  которую нельзя откатить по частям.
- **В отчёте `order by notification_ok`** — проблемные позиции сверху.
  Проверка не должна искать их глазами по алфавиту.

<a id="м27"></a>
## М27. Сканер коду

**Состав:** `components/scanner.tsx` · зависимость `@zxing/library`
(Apache-2.0, импортируется динамически) · ключи `scan.*` в `uk.json` ·
подключён на трёх экранах: `inventory-client.tsx`,
`counts/[id]/count-detail.tsx`, `barcodes/barcodes-client.tsx`.
**Миграций нет** — слой целиком клиентский.
**Ручных настроек нет:** разрешение камеры уже прописано
(`NSCameraUsageDescription` в `codemagic.yaml`, `CAMERA`
в `scripts/patch-android.sh`).
**Перенос:** один файл плюс зависимость.

**Что было сломано.** Три экрана читали код своей копией `scanCamera()`,
и все три стояли на `BarcodeDetector` и `ImageCapture`. **Ни одной
из этих возможностей нет в Safari** — ни в браузере на айфоне, ни в
веб-вью приложения. На iPhone сканер не работал ВООБЩЕ: код доходил до
первой проверки и показывал предупреждение. Владелец описал это как
«нажал — и ничего не происходит», и это точное описание.

Второй дефект был на всех платформах: **превью камеры не показывалось**.
`ImageCapture` берёт кадры из потока, не рисуя его, — мастер наводил
телефон вслепую и ждал, пока сработает.

**Грабли:**

- **Один декодер, а не два.** Соблазн: `BarcodeDetector` там, где он есть
  (Android быстрее), zxing на iOS. Это две ветки с разным поведением,
  которые проверяются порознь и расходятся на третьей правке. Один путь
  дороже по весу и дешевле по сопровождению — и совпадает с правилом
  «общий слой вместо паритета».
- **Вес не платится, пока сканер не открыли.** `await import('@zxing/library')`
  внутри обработчика: до первого нажатия на значок в бандл экрана
  не попадает ничего.
- **Поток глушится в ТРЁХ местах** — размонтирование, закрытие шторки,
  ветка ошибки. Не остановленный `MediaStreamTrack` оставляет камеру
  включённой: горит индикатор, садится батарея, а следующий
  `getUserMedia` может не получить устройство вовсе.
- **`playsInline` у `<video>` обязателен.** Без него Safari на iPhone
  открывает поток в системном полноэкранном плеере поверх страницы,
  и шторка со сканером оказывается ПОД ним. Плюс `muted` — без него
  автовоспроизведение запрещено обоими браузерами.
- **`facingMode: { ideal: 'environment' }`, а не `exact`.** На ноутбуке
  задней камеры нет, и `exact` роняет запрос вместо того, чтобы взять
  единственную имеющуюся.
- **Отказ в доступе и отсутствие камеры — разные сообщения.** В первом
  случае человека надо отправить в настройки телефона, во втором —
  предложить ввести код руками. Одна общая подпись заставляет гадать.
- **Фонарик спрашивается у ТРЕКА, а не у платформы.** `getCapabilities().torch`
  честнее разбора строки браузера: у части камер фонарика нет физически.
- **Починка декодера не помогает, если до декодера не доходит нажатие.**
  После выката М27 владелец сообщил ту же жалобу слово в слово: «сканер
  не отвечает». Дефект оказался ВЫШЕ сканера, на экране склада. Значок
  сканера в шапке вёл на `/app/inventory?scan=1`, а этот признак лишь
  переключал строку поиска в режим ручного ввода кода — и в ЭТОМ режиме
  кнопка камеры вообще не рисовалась (`scanOpen ? …кнопки поиска… :
  …кнопка камеры…`). То есть оба входа, которыми мастер пользуется —
  значок в шапке и быстрое действие «Сканувати», — камеру не открывали
  никогда, а выход к ней существовал только из другого режима. Правила,
  которые из этого следуют: **кнопка камеры стоит в обоих режимах и на
  одном месте**, а ручной ввод — запасной путь второй строкой; **`?scan=1`
  открывает камеру, а не режим**; и **признак снимается из адреса сразу**
  (`history.replaceState`), иначе повторное нажатие значка ведёт на тот же
  адрес, компонент не перемонтируется и второй раз не срабатывает.
- **Выход к ручному вводу обязан жить в самой шторке** (`onManual`,
  добавлен 18.08.2026). Три подписи об ошибке здесь зовут «ввести код
  вручну», а пути к этому вводу в шторке не было: он существовал только
  вторым полем на экране склада. Пока то поле стояло рядом, дефект был
  не виден; после его удаления (М31) отказ камеры превратился в тупик
  «Скасувати». Свойство необязательное — экрану штрихкодов оно не нужно,
  он и так состоит из поля.

<a id="м28"></a>
## М28. Візуальна мова

**Состав:** `app/globals.css` (токены обеих тем, `.page-title`, `.stat-tile`,
`.fab`) · `components/theme.tsx` (выбор темы, загрузочный скрипт,
синхронизация нативного) · `components/app-shell.tsx` (заголовок экрана) ·
`app/layout.tsx`, `app/m/layout.tsx` (`themeColor`) ·
`app/auth/callback/route.ts` (своя копия цветов — страница отдаётся строкой
и не грузит бандл) · `capacitor.config.ts` (начальные цвета и стили) ·
`scripts/patch-android.sh` (мост `AndroidStatusBar`).
**Миграций нет.** **Ручных настроек нет.**
**Перенос:** `globals.css` плюс `theme.tsx`; всё остальное — точки, где
цвет обязан повториться, потому что живёт вне бандла.

**Решение, из которого всё следует** (владелец, 18.08.2026, по 24 макетам):
обе темы полные, **светлая по умолчанию**, тёмная — переключателем
в профиле. Это ОТМЕНЯЕТ прежнее «тёмный вид основной, светлая вторична»
(04.08.2026). Заодно принято: нижняя панель остаётся ЧЕТЫРЁХ вкладок, как
решено 15.08 — макеты от 09.08 рисуют пять и в шести разных сочетаниях,
то есть они иллюстрации, а не спецификация панели.

**Грабли:**

- **Умолчание живёт в `@theme`, а не в классе.** Значения без класса —
  это то, что видит человек до того, как что-то выбрал. Поэтому светлая
  тема переехала в `@theme`, а тёмная — в `html.dark`. Оставить тёмные
  значения умолчанием и «просто добавить светлую» нельзя: первый кадр
  до срабатывания скрипта будет чёрным у всех.
- **Акцент — два токена.** `--color-accent` (заливка, белый текст на нём,
  почти одинаков в обеих темах) и `--color-accent-ink` (акцентный ТЕКСТ
  на фоне страницы, светлеет на тёмном). Один токен не может быть и тем
  и другим: заливка в роли текста нечитаема, текстовый в роли заливки
  бледен. Обе ошибки видны только на ОДНОЙ из тем.
- **Прозрачность как «выключено» ломается при насыщенном акценте.**
  `opacity: .45` на кобальте даёт белый текст на светло-синем. Найдено
  снимком экрана входа в тот же день. Выключенная кнопка обязана быть
  нейтральной: поверхность плюс бледный текст.
- **Тема тянет за собой то, что вне бандла**, и каждое место надо назвать,
  иначе оно останется тёмным навсегда: `<meta name="theme-color">` (полоса
  браузера на телефоне), splash и статус-бар Capacitor, вид клавиатуры,
  и отдельная страница возврата OAuth, которая отдаётся строкой из ручки
  и `globals.css` не видит вовсе.
- **`Style.Dark` в Capacitor — это «СВЕТЛЫЙ текст для тёмного фона».**
  Имя дано по фону, а не по тексту. То же наоборот у Android:
  `setAppearanceLightStatusBars(true)` значит ТЁМНЫЕ значки. Перепутать
  легко, а видно только на устройстве.
- **Статус-бар на Android нельзя переключить плагином.** При удалённом
  `server.url` моста Capacitor там нет — ровно те же грабли DaKi, из-за
  которых у откликов свой `AndroidHaptics`. Пришлось завести
  `AndroidStatusBar` тем же способом.
- **Синхронизировать нативное надо и ПРИ ЗАПУСКЕ, а не только по нажатию.**
  Класс на `<html>` ставит загрузочный скрипт, но моста в этот момент ещё
  нет. Без `ThemeNativeSync` в корневом макете человек с тёмной темой видит
  правильную страницу и тёмные часы на ней — до тех пор, пока не зайдёт
  в профиль и не потрогает переключатель.
- **Третий пункт «як у системі» был враньём** и удалён, а не «починен»:
  он не читал `prefers-color-scheme` вовсе, а снимал класс, то есть
  возвращал тогдашнее тёмное умолчание. Владелец назвал ровно две темы.
- **Тон плитки-счётчика постоянный, а не «серый, пока ноль».** Плитка,
  меняющая цвет вместе с числом, заставляет читать её дважды: сначала
  «какого она цвета сегодня», потом само число. Спокойное состояние
  показывает НОЛЬ. И тон несёт смысл, а не настроение: rose — уже
  сломано, amber — сломается, emerald — норма. Раскрасить плитки «для
  красоты» значит отучить глаз от красного.
- **Размер текста — ОДИН множитель, а не вторая шкала** (`--type-scale`,
  решение владельца 18.08.2026: ползунок «уменьшить-увеличить шрифт»).
  Вторая шкала «крупный вид» разъезжается с первой на первой же правке,
  и половина текста остаётся прежнего размера — ровно поэтому такие
  режимы и выглядят недоделанными.

  Работает одной строкой потому, что `--text-*` лежат в `@theme`, то есть
  Tailwind берёт их КАК СВОЮ шкалу: `text-sm` внутри `@apply`
  разворачивается в `var(--text-sm)`. Проверено отдельно и это условие
  надо беречь: **ни один компонент не пишет `text-sm` сам** — все 54
  употребления живут в `globals.css`. Напишет — выпадет из множителя
  и останется единственным куском текста, который не меняется.
- **Интерлиньяж масштабируется вместе с кеглем.** У `.t-*` он в пикселях;
  оставить его так значит получить наползающие строки на крупном шрифте.
- **Отступы, высоты и `--tap-min` множитель НЕ трогает** намеренно. Они
  в пикселях и остаются. Растянуть заодно раскладку значит проверять
  заново каждый экран; зона нажатия и так 44px — крупному тексту есть
  где стоять. Проверено рендером на 390px: при 140% ни горизонтальной
  прокрутки, ни разъехавшихся строк.
- **У полей ввода ПОЛ в 16px, и он обязателен именно из-за уменьшения.**
  `max(16px, var(--text-base))` на касательных устройствах. Ниже порога
  iOS зумит страницу на фокусе и обратно не отъезжает — то самое правило
  проекта, которое до появления ползунка выполнялось само собой.
  `max()`, а не отмена масштаба: увеличивать поля можно и нужно.
- **Загрузочный скрипт обязателен, как и у темы.** Без него первый кадр
  рисуется обычным кеглем, и человек с крупным шрифтом видит, как
  страница переверстывается у него на глазах. Ровно 1 при этом СНИМАЕТ
  инлайновое свойство, а не пишет его: оставленное, оно перекрыло бы
  правку шкалы в `globals.css`, если та когда-нибудь изменится.

<a id="м30"></a>
## М30. Номер сборки для магазинов

**Состав:** три шага в `codemagic.yaml` — «Сборка IPA» (Flutter),
«Build number = секунды эпохи» (Capacitor), «Версия = секунды эпохи»
(Android). **Миграций нет. Ручных настроек нет.**

**Правило: номер сборки — это `date +%s`, и ничего другого.**

**Отказ 18.08.2026.** Публикация в TestFlight упала с
`ENTITY_ERROR.ATTRIBUTE.INVALID.DUPLICATE`: «The bundle version must be
higher than the previously uploaded version: 101»,
`previousBundleVersion = 101`. Счётчик Codemagic был на пятёрке —
приложение в Codemagic заводили заново, счётчик начался с нуля, а в App
Store Connect уже лежала сотня с лишним. **IPA собрался, упала только
отправка** — в интерфейсе это выглядит как «сборка провалилась»,
и легко пойти искать причину в коде, которого она не касается.

**Грабли:**

- **Счётчик сборочной системы НЕ ЗНАЕТ, что уже лежит в магазине.**
  Он обнуляется при пересоздании приложения, при переезде на другой
  аккаунт, при смене CI. Магазин же помнит всё и требует строго больше.
- **Спрашивать номер у Apple тоже нельзя.**
  `app-store-connect get-latest-app-store-build-number` для билда
  «в проверке» возвращает 1 (урок DaKi) — и публикация падает тем же
  дубликатом. То есть оба «умных» источника врут, просто по-разному.
- **Секунды эпохи не зависят ни от кого.** Число растёт само,
  монотонно, и заведомо больше любого прошлого. Помещается в UInt32
  (предел `CFBundleVersion`) до 2106 года; предел Android
  `versionCode` — 2100000000, хватает до 2036, дальше переходить
  на «минуты эпохи».
- **Не возвращать `$BUILD_NUMBER` «чтобы номер был красивый».**
  Номер сборки не читает ни один человек, а его совпадение с индексом
  CI не стоит ни одного упавшего выката.

<a id="м29"></a>
## М29. Офлайн-екран і service worker

**Состав:** `public/sw.js` · `public/offline.html` ·
`public/manifest.webmanifest` и иконки · `components/pwa.tsx`
(регистрация и предложение обновиться).
**Миграций нет.** **Ручных настроек нет.**
**Перенос:** два файла, `sw.js` и `offline.html`.

**Задача у него РОВНО ОДНА:** показать экран «немає мережі» вместо пустой
страницы. Ни страницы, ни файлы сборки он не кеширует и кешировать
не должен.

**Грабли:**

- **Кеш файлов сборки плюс кеш страниц дают «старый стиль после
  обновления».** Отказ 18.08.2026: владелец выкатил новое оформление,
  потянул страницу вниз — и получил обратно прежний вид, без единой
  ошибки в консоли. Половины по отдельности безобидны:
  `/_next/static/` отдавался из кеша, который чистится только при смене
  `VERSION` руками, то есть стили каждой прошлой сборки жили на телефоне
  вечно; страницы клались в свой кеш и отдавались оттуда, едва сеть
  подвела на мгновение, — а «потянул обновить» это ровно тот момент,
  когда соединение ещё не поднялось. Вместе: вчерашний HTML просил
  вчерашний css, и тот лежал рядом живой. Страница собиралась целиком
  в прошлом виде и выглядела исправной.
- **Лечится удалением кеша, а не аккуратной сменой версии.** Соблазн
  «поднимать VERSION при каждом выкате» — это ручное действие,
  привязанное к чужому событию: забыть его нельзя заметить, а заметит
  владелец через неделю на телефоне. Тем же способом уже накопился отказ
  15.08.2026 («VERSION не менялся никогда»). Чинить дисциплиной то, что
  чинится удалением кода, второй раз нельзя.
- **Кешировать тут и нечего.** Файлы сборки Vercel отдаёт с
  `immutable, max-age=31536000` — обычный HTTP-кеш делает ту же работу
  и сам выбрасывает лишнее. А офлайн-ЧТЕНИЕ в вебе невозможно в принципе
  (страницы приходят с сервера), то есть кеш страниц не приближал офлайн
  ни на шаг — он только показывал вчерашнее как сегодняшнее.
- **Запасной экран — ФАЙЛ, а не страница Next.** Отказ 15.08.2026:
  `/offline` ссылается на `/_next/static/...` с хешем сборки, хеши
  сменились, и офлайн отдавал HTML, просящий несуществующие файлы —
  браузер рисовал ровно ничего. `/offline.html` со стилями внутри
  протухнуть не может: ему не на что ссылаться.
- **`cache.addAll` — всё или ничего.** Один недоступный адрес отклоняет
  Promise, установка падает, service worker не активируется ВОВСЕ и молча.
  Только поимённо через `Promise.allSettled`.
- **Предложение «встановити на телефон» удалено** (владелец, 18.08.2026),
  вместе с полосой, обработчиком `beforeinstallprompt`, подсказкой для iOS
  и своими ключами словаря. Оно всплывало поверх формы входа и закрывало
  «Забули пароль?» — то есть мешало человеку сделать единственное, зачем
  он пришёл. Установка через меню браузера продолжает работать: манифест
  и иконки на месте, мы просто перестали её навязывать.

<a id="м31"></a>
## М31. Екран складу

**Состав:** `app/app/inventory/inventory-client.tsx` (раскладка и фильтры) ·
`app/app/inventory/page.tsx` (загрузка данных) · значки склада
в `components/icons.tsx` · ключи `inventory.*` в `uk.json` ·
`components/scanner.tsx` (появился `onManual`).
**Миграций нет** — слой целиком представленческий, запросы прежние.
**Ручных настроек нет.**
**Перенос:** один файл экрана плюс значки; всё остальное уже есть.

**Задача экрана.** Мастер приходит сюда с двумя вопросами — «що просрочено»
и «де ця банка», — и оба обязаны решаться без прокрутки. Приёмка,
справочники и документы к этим двум вопросам не относятся: это работа
администратора за столом (то же соображение, по которому приёмка намеренно
оставлена вне офлайн-очереди).

**Что было сломано.** Отзыв владельца 18.08.2026 дословно: «много дубляжа
и не нужного». Дубляж был буквальный, а не на глаз:

| действие | сколько раз лежало на экране |
|---|---|
| поиск | строка в шапке оболочки (`?q=`) **и** своё поле на странице |
| сканер | значок в шапке (`?scan=1`), плитка «Сканувати» **и** синяя кнопка у поля |
| пустое состояние | своя карточка у «Розхідників» **и** своя у «Ємностей» |
| разделы | четыре плитками сверху, четыре ссылками внизу, «Довідники» — кружком среди фильтров |

**Грабли:**

- **Счётчик без фильтра — это не информация, а тревога без выхода.**
  «Прострочені: 3» стояло на экране мёртвым числом: система сообщала беду
  и не давала способа увидеть, о каких именно позициях речь. Плитка обязана
  быть нажимаемой, иначе её незачем показывать. Это же снимает половину
  нужды в поиске.
- **Ноль не гасится ни прозрачностью, ни цветом.** Соблазн приглушить
  плитку с нулём. Тогда её сначала читают как вид («какая она сегодня»)
  и лишь потом как число, а спокойное «ноль просрочених» — такой же
  полноценный ответ, как тревожное. Нажатие при этом не делает ничего:
  фильтр на ноль — обещание показать то, чего нет. (Плюс прямой запрет
  из CLAUDE.md: выключенное нейтрально, а не полупрозрачно.)
- **Зелёная метка «Дійсний» на каждой здоровой строке — шум.** Двадцать
  засобів давали двадцать зелёных плашек, и на их фоне красная переставала
  выделяться. Метка состояния рисуется, только когда есть что сказать;
  срок и так напечатан строкой выше. Общее правило: подпись, которая стоит
  у всех, не отличает никого.
- **Фильтр обязан быть виден на экране, где он применён.** Поиск живёт
  в шапке оболочки, и укороченный им список выглядит как пропавшие данные.
  Снимаемая метка с крестиком — не украшение: без неё снять фильтр можно
  только в шапке, куда за этим никто не пойдёт.
- **Снимаемый фильтр НЕ рисуется как выбранная вкладка.** Обе сущности
  просились в `.chip-active`, и две одинаковые синие пилюли в соседних
  строках означали бы разное: «я на этой вкладке» и «нажми, чтобы снять».
  Вкладка — заливка (`--color-accent`), фильтр — обводка и акцентный ТЕКСТ
  (`--color-accent-ink`). Ровно та причина, по которой акцент разведён
  на два токена.
- **Один ряд кружков не может быть тремя разными вещами.** Прежний ряд
  смешивал фильтры («Всі», «Ємності»), переход в шторку («Довідники») и
  создание («+ Засіб»). На 390px он переносился на вторую строку, и
  «+ Засіб» вставал под «Товари» — читался как ещё один фильтр. Фильтры
  остались одной строкой с горизонтальной прокруткой (перенос запрещён),
  создание уехало в `.fab-wide`, переход — в список разделов.
- **Плавающая кнопка называет результат, а не себя.** «+ Засіб» и «+ Банка» —
  разные действия, и безымянный круглый плюс, меняющий смысл под вкладкой,
  прочитать нельзя. Поэтому `.fab-wide` с подписью, а не `.fab`.
- **Пустое состояние — одно на экран, а не одно на секцию.** На новом
  заведении экран состоял из двух почти одинаковых карточек во весь рост,
  и владелец видел их первыми в жизни продукта.
- **На пустом заведении плавающей кнопки нет.** Карточка пустого состояния
  уже несёт «Додати засіб»; две кнопки одного действия в двадцати
  сантиметрах друг от друга — тот самый дубляж, из-за которого экран
  и переделывался. Проверено рендером: без этого условия они стоят
  в кадре рядом.
- **Текстовые знаки вместо значков выглядят как сбой шрифта.**
  Экран рисовал состояния и разделы символами `◫ ◷ ⊘ ⌄ ⌗ ⬓ ⇅ ☰` — ровно
  то, о чём предупреждает шапка `components/icons.tsx`. На телефоне
  владельца половина вышла квадратами, и счётчик «Прострочені» был подписан
  знаком, который читается как «нет глифа», а не как беда. Значок обязан
  повторять смысл тона плитки, а не спорить с ним: часы — «скоро»,
  треугольник — «уже сломалось».
- **Удаляя поле, проверь, не был ли оно чьим-то единственным выходом.**
  Три подписи в сканере зовут «ввести код вручну», а пути к этому вводу
  в самой шторке не было: он существовал только вторым полем на экране
  склада. Пока поле стояло рядом, дефект был не виден; после удаления
  отказ камеры стал тупиком. Отсюда `onManual` у сканера — выход обязан
  жить там же, где отказ.
- **Строка поиска в шапке НАВИГИРУЕТ, а не фильтрует.** Значит состояние
  экрана надо синхронизировать с `?q=` эффектом, а не только начальным
  значением: иначе второй запрос подряд с той же страницы меняет адрес
  и не меняет список.

<a id="м32"></a>
## М32. Візуальна мова за прототипом CRESKO

**Состав:** `app/globals.css` (палитра обеих тем, `.searchfield`, `.metric`,
`.quick-tile`, `.list-card`, `.section-head`, `.clamp-2`) ·
`components/app-shell.tsx` (шапка) · `components/notify-bell.tsx` (новый) ·
`components/icons.tsx` (`IconBell`) · `app/app/inventory/inventory-client.tsx` ·
`components/theme.tsx`, `app/layout.tsx`, `app/m/layout.tsx`,
`capacitor.config.ts` (цвет статус-бара и заставки) · ключи `app.chrome.bell.*`,
`inventory.stats.short.*`, `inventory.quick.title` в `uk.json`.
**Миграций нет.** **Ручных настроек нет.**

**Откуда взялось.** Владелец 18.08.2026 передал кликабельный прототип
(`CRESKO — Клікабельний прототип.dc.html`) со словами «тут самый близкий
визуал что я хотел, от него плясать будем» и отдельно уточнил: макет —
эталон РЕЗУЛЬТАТА, а не буквальных элементов, «можешь взять колокольчик
лучше», главное чтобы показанное в макете было и в проекте.

**Грабли:**

- **Прототип пришлось запускать, а не читать.** 260 КБ размеченного HTML
  ничего не говорят о том, как экран выглядит; при этом он тянет React
  и Babel с unpkg, а из контейнера туда хода нет. Вылечилось `npm pack`
  нужных версий и подменой трёх адресов на локальные файлы. Правило шире:
  **макет надо ОТКРЫТЬ**, иначе половина решений принимается по названиям
  классов.
- **Палитра берётся ЗНАЧЕНИЯМИ, а не «похоже».** Наша светлая была
  `#f6f7f9`/`#1f5df5`, прототипная — `#f4f5f7`/`#2563eb`. Разница
  в один шаг, но она видна ровно там, где карточка встречается с фоном,
  а на этом стыке держится вся раскладка.
- **Цвет фона живёт в ПЯТИ местах, и все пять едут вместе.**
  `globals.css`, `components/theme.tsx` (BAR), `app/layout.tsx`
  и `app/m/layout.tsx` (`themeColor`), `capacitor.config.ts` (заставка
  и статус-бар). Забыть последний значит получить светлую полосу часов
  над тёмной страницей — тот самый стык, из-за которого приложение
  выглядит собранным из кусков.
- **Поиск вернулся НА страницу, и это не откат вчерашней правки.**
  Вчера его убрали как дубляж — потому что он был И в шапке, И на экране.
  В прототипе строки в шапке нет вовсе (там календарь, колокол, сканер,
  аватар), поэтому поиск на экране остаётся единственным. Заодно он стал
  лучше: фильтрует по мере набора, а не по Enter с серверным переходом.
- **Значок без экрана — сломанная навигация.** Колокол в макете есть,
  а экрана уведомлений в продукте нет. Поставить «просто красивый значок»
  значит получить кнопку, которая молчит. Поэтому он открывает настоящую
  очередь `notification_outbox` (строки `pending` — напоминания и
  предупреждения о сроках), а без права `customers.read` не рисуется:
  политика 0011 всё равно вернула бы пусто.
- **Счётчик считается ОДИН раз за открытие приложения.** Оболочка живёт
  в `app/app/layout.tsx` и не перерисовывается при переходах, значит
  `useEffect` с пустыми зависимостями отрабатывает раз за сеанс, а не
  на каждый экран. Список подтягивается только когда шторку открыли.
- **Четыре плитки в ряд на 390px — это ~86px на плитку**, и «Мало на
  складі» переносится на две строки, делая плитку выше соседних. Ряд
  ломается об ОДНУ подпись. Отсюда отдельные короткие ключи
  `inventory.stats.short.*`; длинные названия остались там, где место
  есть, — на снимаемой метке фильтра.
- **Имя в карточке списка занимает две строки, а не обрезается.**
  У засобів имена различаются КОНЦОМ («Absolut Repair Mask» против
  «Absolut Repair Shampoo»), и обрезка по первому слову превращает
  список в набор одинаковых строк.
- **Список — отдельные карточки, а не одна с разделителями.** У строки
  миниатюра, два уровня текста и метка состояния; в сплошном списке
  они слипаются. Это же место оставлено под фото засоба: фотографий
  пока нет ни у одного материала, стоит значок, и когда они появятся,
  меняется ровно `.list-card-thumb`.

**Продолжение того же дня.** Владелец увидел склад и шапку, ответил
«отвратительно, мне не нравится, не вижу изменений вообще» — оказалось,
он листал остальные три вкладки, которые ещё не были тронуты. Пройдены
«Сьогодні», «Записи», «Послуги», «Профіль».

**Состав добавки:** `app/app/page.tsx` (карточка-герой, приветствие) ·
`app/app/bookings/bookings-client.tsx` (точка статуса, сумма в карточке) ·
`app/app/catalog/page.tsx`, `catalog-client.tsx` (тривалість послуги,
счётчики, список карточками) · `app/app/profile/profile-client.tsx`
(значки-якоря у строк) · `app/globals.css` (`.today-hero`, `.status-dot`)
· `components/icons.tsx` (`IconLock`, `IconMail`).

**Грабли:**

- **«Доброго ранку» решает часовой пояс — и не тот, что нужно.**
  Первая версия приветствия выбирала «ранку/дня/вечора» по
  `new Date().getHours()` внутри серверного компонента — а сервер
  рисует страницу в Дублине, то есть по UTC. «Доброго ранку» провисело
  бы у продавца в Києві до полудня по местному времени. Не «починить
  часовой пояс», а снять зависимость целиком: одно приветствие без
  времени суток честнее, чем неправильное время суток.
- **Число «потребує уваги» — сумма, а не отдельная выборка.** Считается
  из уже посчитанных блоков сводки (неподтверждённые записи + сроки,
  что горят + то, что пора заказать), а не отдельным запросом: экран
  и так спрашивает три права-и-модуля отдельно на каждый блок (см. выше
  в этом же файле про `seeBookings`/`seeContainers`/`seeStock`), и
  четвёртая сумма обязана сойтись с тем, что человек уже увидел ниже,
  а не жить своим счётом.
- **Карточка-герой не рисуется тому, кто не увидит ни одного блока
  под ней.** Она показывает «0 і 0» иначе — а это утверждение о
  заведении, которого читающий может не иметь права знать (тот же
  разбор про право-и-модуль, что уже был на этом экране, распространён
  и на новый блок).
- **Придуманная плитка хуже отсутствующей.** У «Послуг» в прототипе
  стоят «Акції» и «Рейтинг». Промокодов и скидок в продукте нет —
  модуль `marketing` пуст (CLAUDE.md), а рейтинг — витринная величина
  не с этого экрана. Плитка со случайным числом обещала бы функцию,
  которой нет; она не заведена, а не «заведена нулём».
- **Тривалість пришлось ДОБАВИТЬ в запрос, а не выдумать.** Колонка
  `offering_variants.duration_minutes` уже существует и заполняется
  триггером (0010) — но экран каталога её не читал. Взята оттуда же,
  откуда цена по варианту, тем же `Math.min` по заполненным значениям.
- **Текстовые символы вместо значков — та же грабля, что на складе,
  третий раз подряд.** «◷ ◫» в карточке товара/послуги заменены на
  `IconClock`/`IconBox`: правило «не текстовым символом» распространяется
  на каждый новый экран, а не только на тот, где его один раз нашли.
- **Заметная попутно ложь в статичном тексте.** Подпись переключателя
  темы обещала «Світла, темна або як у системі» — третий пункт удалён
  ещё 18.08 отдельным решением (КОНСПЕКТЫ.md, М28: «третий пункт был
  враньём»), а строка словаря осталась. Поймано не задачей, а тем,
  что экран открыли рендером и прочитали, что на нём написано, —
  доказательство, почему рендерить обязательно, а не читать диff.

**Хендофф із цифрами, а не тільки прототип** (та ж дата, пізніше).
Владелец прислал полный бандл: `README.md` со спецификацией, `PROMPT.md`
и ТЗ. До этого я снимал значения с рендера прототипа — и это дало
системный сдвиг, который на глаз читался как «скруглённее и крупнее
макета», а в цифрах выглядел так:

| что | было (с рендера) | README |
|---|---|---|
| радиус карточки | 18px | **14px** |
| радиус поля, чипа | 14px | **12px** |
| высота поля | 46px | **48px** |
| поиск на экране | 48px, пилюля 999px | **44px, radius 12px** |
| число в счётчике | 20px/700 | **17px/800** |
| подпись счётчика | 11px | **10px** |
| иконка-кнопка шапки | круг 44px | **квадрат 40×40, radius 12** |
| иконка нижней панели | 22px | **26px** |
| кегли `.t-*` | 11 / 19 / 28 / 40 | **таких в шкале нет** |

**Грабли:**

- **Спецификация с цифрами бьёт рендер.** Прототип показывает, КАК
  выглядит; README говорит, ИЗ ЧЕГО это собрано. Снятое с картинки
  «на глаз» ошибается на 2–4px в каждом токене, и ошибки складываются:
  карточка на 4px круглее, поле на 2px выше, кегль на 2px крупнее —
  вместе это и есть «похоже, но не то».
- **Закрытая шкала — это ограничение, а не таблица.** README пишет
  прямо: «інших значень не існує». Наши 11 / 19 / 28 / 40 заменены
  ближайшей РОЛЬЮ, а не ближайшим числом: 11→10 (бейдж, подпись таба),
  19→17 (заголовок диалога), 28→24 (H1 онбординга), 40→30 (H1 входа).
- **Зона нажатия добирается псевдоэлементом, а не размером кнопки.**
  Иконка-кнопка шапки по README 40×40, а минимальный тач-таргет 44.
  Раздуть кнопку до 44 значило бы разъехаться с макетом; `::after`
  с отрицательным `inset` даёт 44px нажатия при 40px рисунка.
- **Четыре в ряд, а не лента.** «Швидкі дії» были уезжающей вбок
  лентой — на 375px четвёртая плитка оставалась за краем, и
  «Інвентаризація» существовала только для того, кто догадался
  потянуть ряд пальцем. README: четыре в ряд, плашка 32px, подпись 10px.
- **«Проблемні позиції сортуються вгору» — требование, а не вкус.**
  `expired → soon → ok`. Без него просроченная банка лежит там, куда
  её положил алфавит, и экран, существующий ради ответа «що горить»,
  прячет ответ в середину списка.
- **Набор счётчиков закрывает ОДНУ ось.** README: Позицій · Дійсні ·
  Закінч. · Прострочені — все четыре про срок годности, и они
  складываются в общее число. Стоявшее у меня «Мало на складі» мерило
  ОСТАТОК, то есть другую величину, и в ряду из четырёх читалось как
  часть той же суммы, хотя ею не было.
- **Одно расхождение с README оставлено намеренно и названо:** поля
  ввода на телефоне не опускаются ниже 16px, хотя README задаёт 14.
  Ниже порога iOS зумит страницу на фокусе и обратно не отъезжает —
  это поведение системы, оно старше макета.

**Что по прототипу всё ещё НЕ сделано** (называю, чтобы не искать заново):
полная сетка календаря-месяца на «Записи» (сделан только список дня —
переключатель «Календар/День» в прототипе есть, сама сетка месяца
с точками-маркерами нет: это отдельная фича с собственной
синхронизацией дат), карточка клиента шторкой, вкладка «Техкарти»
на экране послуг.

## М33. Один пошук, шапка без заголовка, тема на акаунті

**Состав:** `components/global-search.tsx` (новый) · `components/sheet.tsx`
(портал) · `components/app-shell.tsx` (шапка, отступы, строки настроек) ·
`components/theme.tsx` (`remember`, `themeServerScript`, `.seg`) ·
`components/lang-switch.tsx` · `app/app/layout.tsx` (тема из профиля) ·
`app/globals.css` (`.apphead` без линии, `.apphead-title`, `.seg`,
`.setting-row`, `.thumb-sm`; удалены `.page-title`, `.page-subtitle`,
`.searchbar`) · `app/app/inventory/{page,inventory-client}.tsx`,
`app/app/catalog/catalog-client.tsx` (поля поиска сняты) ·
`app/app/customers/customers-client.tsx` (`?id=` открывает карточку) ·
ключи `app.search.*`, `inventory.empty.filteredTitle`; снят
`app.chrome.calendar.aria`.
**Миграция:** `0109_profile_theme_and_staff_guard.sql`.
**Тест:** `supabase/tests/31_profile_theme.sql`.
**Ручных настроек нет.**

**Откуда взялось.** Пять требований владельца 19.08.2026 одним заходом:
единый поиск в шапке вместо полей на экранах; сломанный колокол;
синхронизация темы между вебом и приложением; минималистичные
переключатели темы и языка; убрать линию под шапкой, пустоты сверху
и снизу и заголовок экрана («достаточно что они подписаны в нижнем наве»).

**Грабли:**

- **`backdrop-filter` делает элемент содержащим блоком для `position:
  fixed` внутри него** — ровно как `transform` и `filter`. Колокол стоит
  в `.apphead` с `blur(12px)`, поэтому его шторка открывалась «внизу
  шапки»: полоска в 50px и затемнение поверх одной шапки. Кнопка
  выглядела мёртвой, хотя состояние менялось. Починка — портал в `body`
  в самом `Sheet`, то есть для ВСЕХ шторок сразу: следующая шторка
  внутри липкой панели или анимированного блока попала бы в ту же
  ловушку, и искали бы её так же долго.
- **Поиск на экране требует сначала угадать раздел.** «Оксана» — это
  клиент, заказ и запись в трёх разных экранах; на складе её нет вовсе.
  Отсюда разворот вчерашнего решения: один поиск в шапке по четырём
  разделам сразу, разделы — результат поиска, а не его условие.
- **Раздел спрашивается, только если он открыт по ОБЕИМ осям.** Пустая
  секция в выдаче читается как «у нас такого нет», а не как «вам сюда
  нельзя». Граница доверия при этом прежняя — RLS.
- **Телефон клиента в выдачу не попадает.** Поиск ищет по имени и не
  запрашивает `phone`/`email`: контакт отдаёт `customer_card` с проверкой
  права и записью в журнал доступа (0090). Строка выдачи с телефоном
  обошла бы и то, и другое.
- **`ilike` по номеру заказа не работает** — `number` это `bigint`.
  Цифровой запрос ищется ТОЧНЫМ номером: набравший «103» ищет заказ №103,
  а не всё, где встретилась сотня.
- **Клиент от поиска открывается ссылкой `?id=`, а не переходом в список.**
  Своей страницы у клиента нет, и без этого найденного человека пришлось
  бы искать второй раз глазами. Адрес чистится сразу: иначе обновление
  страницы писало бы в журнал доступа повторно.
- **Тема между устройствами НЕ синхронизируется хранилищем — никаким.**
  Обёртка открывает тот же боевой сайт, но в своём веб-вью: у него
  собственные `localStorage` и банка кук, не общие с Safari того же
  телефона. Значит источник правды только серверный — `profiles.theme`
  (0109), а `localStorage` остаётся кешем ради первого кадра.
- **Серверная тема применяется СКРИПТОМ в разметке, а не эффектом.**
  Эффект случается после отрисовки: выбравший тёмную на телефоне увидел
  бы на вебе белую вспышку и перекраску. Скрипт стоит в макете кабинета,
  а не в корневом: корневой обслуживает и витрину, и поход в базу оттуда
  сделал бы динамическими все публичные страницы.
- **Найдено попутно и закрыто той же миграцией: `is_staff` выдавался
  себе обычным UPDATE.** Политика `profiles_self_update` разрешает менять
  свою строку целиком, без списка колонок, а в строке лежит «единственная
  глобальная привилегия в системе», которую хук кладёт в токен. Сторож
  триггером, а не колоночным грантом: список колонок в гранте устаревает
  молча — та же ошибка, что ранний выход по списку колонок в 0080.
- **Тест `06_isolation.sql` поймал мою же миграцию** на первом прогоне:
  `revoke … from public` не отбирает EXECUTE у `anon` и `authenticated`
  (П-А). Восьмой раз в проекте — и первый, когда это нашла не отладка,
  а тест.
- **Заголовок экрана снят НЕ везде.** У четырёх разделов панели имя
  в шапке не рисуется — панель их называет. У остальных (Замовлення,
  Клієнти, карточки) оно осталось строкой в шапке рядом со стрелкой:
  их панель не называет, и без подписи экран стал бы безымянным.
  Описание снято везде.
- **Два запаса снизу складывались.** `pb-32` у оболочки И `pb-12`
  у `main` — 176px пустоты под последней карточкой при панели около
  семидесяти. Ставились в разное время и в разных файлах, поэтому
  никто их вместе не видел.
- **Значки ☀ и ☾ заменены словами.** Это эмодзи-глифы, и их наличие
  зависит от прошивки: на телефоне владельца уже приезжали квадраты
  вместо «◫ ◷ ⊘» (М32). Места на слова хватает.
- **Акцент — дефицитный ресурс.** Выбранное значение переключателя было
  сплошным кобальтом: настройка, которую трогают раз в жизни, кричала
  громче единственной кнопки действия. Стало — мягкая дорожка и
  приподнятая плашка поверхности, как в системных настройках телефона.

**Продолжение того же дня: свайп назад с предыдущим экраном.**
«Продумай систему возврата назад, чтобы как в инсте было свайпом бесшовно
и чтобы под этим показывалась страница предыдущая; стрелку назад
минималистичной сделай».

**Состав добавки:** `components/swipe-back.tsx` (переписан) ·
`app/layout.tsx` (обёртка `#page`) · `app/globals.css` (`#page`,
`#swipe-under`, `#swipe-scrim`, минимальная `.apphead-back`).

**Грабли:**

- **Предыдущей страницы взять неоткуда — её надо сохранить заранее.**
  Next отрисует её только ПОСЛЕ `history.back()`, то есть когда жест
  уже кончился. Поэтому снимок делается в момент нажатия на внутреннюю
  ссылку, в фазе захвата: живое дерево `#page` клонируется `cloneNode`
  (не строкой — ни сериализации, ни разбора) и кладётся в стек глубиной
  три. Стек чистится на `popstate`, иначе под жестом показывался бы
  экран через один.
- **⚠️ У КЛОНА НАДО СНИМАТЬ `id`.** Клон — копия `#page` вместе
  с идентификатором, а подложка вставляется ПЕРВЫМ узлом `body`, и
  `getElementById('page')` начинал возвращать КОПИЮ. Жест двигал снимок,
  живая страница стояла на месте. Коварство в том, что все замеры
  показывали правильные числа — они снимались с того же клона; поймалось
  только сравнением ТЕКСТА подложки с текстом страницы.
- **Двигать `body` было нельзя изначально.** Подложке нужно место под
  содержимым и вне того, что едет за пальцем, а `body` — это и есть
  содержимое. Отсюда обёртка `#page` в корневом макете: у неё своя стопка
  (`z-index: 1`), подложка ложится под неё, а шторки и уведомления
  (портал в `body`) остаются над.
- **Снимок сдвигается на прокрутку.** Он снят у прокрученной страницы,
  и без `top: -scrollY` под пальцем оказывается начало экрана, а не то
  место, где человек был.
- **Уехавшая вправо страница расширяет документ вбок.** Появляется
  горизонтальная прокрутка, и соскользнувший палец таскает страницу.
  На время жеста — `overflow-x: clip` на `<html>`; именно `clip`,
  а не `hidden`: `hidden` завёл бы ещё один контейнер прокрутки,
  и липкая шапка перестала бы липнуть.
- **Параллакс −25% и затемнение 0.16** — числа системного контроллера
  iOS. С другими жест ощущается чужим, хотя работает так же.
- **Проверено в браузере, а не «на глаз»**: подложка монтируется
  (180 узлов), страница уезжает на 173px, снимок догоняет на −75px,
  текст подложки — предыдущий экран, текст страницы — текущий,
  после отпускания адрес сменился и оба слоя убраны.

**Падение боя 19.08.2026 и правило, которое из него следует.**

Слияние М33 положило ВЕСЬ кабинет: каждая страница отвечала 500. Из журнала
Vercel дословно (digest `1712864787`): «Attempted to call themeServerScript()
from the server but themeServerScript is on the client».

`themeServerScript` лежал в `components/theme.tsx` с директивой `'use client'`,
а звал его серверный макет кабинета. Любой экспорт клиентского модуля
на сервере — не функция, а ссылка на клиентскую сущность; вызвать её нельзя.

- **Правило: всё, что зовёт СЕРВЕР, живёт в модуле без `'use client'`.**
  Клиентский файл может это импортировать, обратное — нет. Значения темы
  и обе строки загрузочных скриптов теперь в `lib/theme-script.ts`.
- **Реэкспорт не спасает.** `export { x } from '@/lib/...'` из клиентского
  модуля проходит через ту же границу: сервер получил бы ту же ссылку.
  Импортировать надо напрямую из модуля без директивы.
- **Строковые константы из клиентского модуля работают, вызовы — нет.**
  `themeBootScript` и `textScaleBootScript` в корневом макете жили так
  месяцами. Разница в том, что их читают как значение, а не зовут. Отсюда
  и обманчивость: соседняя строка рядом работает, а новая падает.
- **ГЛАВНОЕ. `next build` и `tsc --noEmit` этого НЕ ловят** — ошибка
  времени выполнения. Я собрал, проверил типы, прогнал тесты базы
  и не открыл ни одной страницы кабинета: авторизация в контейнере
  недоступна, и я счёл зелёную сборку достаточной. Не достаточна.
  **Приёмка серверного кода — рендер, а не сборка.** Когда экран
  недоступен без входа, годится временная серверная страница, зовущая
  то же самое: она отвечает 200 или 500 за минуту.
- **Проверить бой из сессии агента нельзя** — сеть контейнера закрыта
  политикой окружения (`CONNECT tunnel failed, 403` на `cres-ca.com`
  и на `*.vercel.app`). Единственный способ увидеть состояние боя —
  журналы Vercel через MCP (`get_runtime_errors`, `get_runtime_logs`)
  и состояние развёртывания. Это же значит, что «проверю после выката»
  не работает: проверять надо ДО.

**М34. Решта екранів кабінету за хендоффом** (19.08.2026).

**Состав:** `app/app/orders/{page,orders-client}.tsx` ·
`app/app/customers/customers-client.tsx` ·
`app/app/finance/finance-client.tsx` ·
`app/app/documents/{page,documents-client}.tsx` ·
`app/app/techcards/techcards-client.tsx` ·
`app/app/journals/journals-client.tsx` ·
`app/app/inventory/materials/[id]/material-card.tsx` ·
`app/globals.css` (`.doc-ext`, `.list-anchor[data-tone]`) ·
ключи `orders.stats.*`, `finance.add.cta`, `techcards.stats.*`,
`inventory.material.inci.*`; сняты `documents.nav.*`, `techcards.nav.*`,
`customers.card.cta`.
**Миграций нет.** **Ручных настроек нет.**

**Грабли:**

- **С телефона нельзя было переключить журнал.** На вкладках висело
  `hidden lg:inline-flex` с объяснением «на телефоне они живут в нижней
  панели» — объяснение устарело 15.08.2026, когда панель перестала
  держать экраны раздела. Четыре месяца человек видел первую вкладку
  и считал, что других журналов в продукте нет. Правило: спрятанное
  «потому что оно есть в другом месте» стареет молча.
- **Телефон покупателя ушёл из СПИСКА заказов.** `contact_phone`
  запрашивался и печатался сотне строк сразу — это выгрузка базы без
  кнопки «выгрузить», мимо журнала доступа. В карточке заказа он
  остался: там его открывают осознанно.
- **Счётчики считаются запросом БЕЗ строк** (`head: true`), а не из
  выданной сотни: иначе заведение с тысячей заказов видит «усього 100».
- **Формы уехали в шторки** на финансах и документах. Раскрытая форма
  из четырёх-пяти полей занимала первый экран телефона, а журнал
  и документы — то, ради чего заходят, — начинались за сгибом. Вносят
  раз в день, смотрят постоянно.
- **Ряды ссылок «Склад · Журнали · Техкарти» сняты** с документов
  и техкарт: все разделы лежат под аватаром, а на документах ряд
  у инспектора вообще состоял из двух кнопок неизвестно куда.
- **Размер файла в строке документа наконец показан.** Колонки
  `size_bytes` и `mime` завела ещё 0059, и до сих пор их не читал ни
  один экран: размер человек узнавал, только скачав файл.
- **Чего в макете взять нельзя:** сетка миниатюр у техкарт (у техкарты
  нет ни фото, ни чего-либо, что рисуется картинкой) и фото 4:3
  у карточки засоба (колонки под изображение у `materials` нет вовсе).
  Пустой серый прямоугольник честнее не станет — обе плитки не заведены
  и названы здесь, чтобы не искать заново.

## М35. Один источник правды для стиля

**Состав:** `lib/design/tokens.ts` (новый) · `scripts/check-tokens.mjs`
(новый) · `package.json` (`check:tokens`) · `.github/workflows/build.yml`
(шаг перед сборкой) · `lib/report/sanitation-report.ts` ·
`lib/report/labels.ts` · `lib/email/layout.ts` · `lib/email/templates.ts` ·
`lib/ratelimit/deny.ts` · `components/notify-bell.tsx`.
**Миграций нет.** **Ручных настроек нет.**

**Откуда взялось.** Требование владельца 19.08.2026: «если стили, то оно
в одном месте; изменил — меняет всё; и чтобы не остались артефакты старые».

**Что нашлось.** Палитр в продукте было ЧЕТЫРЕ, и три никто не менял при
смене оформления: отчёт для проверки, наклейки на банках и письма клиенту
печатались сепией первого оформления (`#16150f`, `#22443a`, `#6a6355`,
`#ddd7cb`). То есть документ, который клиент несёт в Держпродспоживслужбу,
был в цветах, которых в приложении нет с 18.08.2026.

**Грабли:**

- **Переменные CSS не годятся трём потребителям.** Печать (тема к бумаге
  отношения не имеет, документ обязан быть светлым у всех), письма
  (почтовые клиенты вырезают `<style>` из `<head>`), нативная оболочка
  (цвет приходит строкой в конфигурацию до всякого CSS). Значения обязаны
  существовать и в TypeScript — вопрос только в том, откуда они берутся.
- **Поэтому сторож, а не соглашение.** `check-tokens.mjs` читает
  `globals.css`, сверяет с `tokens.ts` и роняет сборку. Проверено
  ПОПЫТКОЙ НАРУШИТЬ: подмена одного знака в акценте — падение,
  возврат — зелено. Без этого «один источник» остаётся обещанием.
- **Искать объявление блока, а не имя.** Первая версия сторожа брала
  `css.indexOf('html.dark')` и попадала в УПОМИНАНИЕ в комментарии шапки,
  после чего сверяла светлые значения с тёмными и «находила» двадцать
  расхождений там, где их нет. Ищется `^html.dark {` регулярным
  выражением с начала строки.
- **Список пар ЯВНЫЙ, а не по правилу.** Соблазн вывести имя переменной
  из ключа (`accentInk` → `--color-accent-ink`) кончается тем, что
  опечатка означает «пары нет, сверять нечего» — то есть зелёную
  проверку на разъехавшемся значении.
- **Замена подстрокой портит соседей.** `#fff` → токен внутри `#ffffff`
  дал `${P.paper}fff` в цвете QR-кода. Ловится только чтением диффа
  глазами: сборка на этом не падает.

## М36. Склад і журнали: кожен елемент відкривається

**Состав:** `app/app/journals/journals-client.tsx` (история отметок) ·
`app/app/inventory/{page,inventory-client}.tsx` (товар ведёт в карточку) ·
ключи `journals.cleaning.history.*`.
**Миграций нет.** **Ручных настроек нет.**

**Откуда взялось.** Требование владельца 19.08.2026: «чтобы в складе были
все элементы, все растворы, все журналы, каждое нажатие открывает страничку
со всеми данными; всё отмечено кто, что, когда».

**Что нашлось при обходе:**

- **Журнал прибирання показывал ТОЛЬКО СЕГОДНЯ.** Экран отвечал
  «відмічено о 9:20, Оксана» либо «не відмічено», а вчерашнего дня не
  видел никто — ни владелец, ни инспектор. Записи при этом лежали
  в `cleaning_entries` и печатались в отчёте для проверки: данные были,
  показать их в приложении было негде, и на вопрос «а позавчора
  прибирали?» ответом служил PDF. ТЗ 3.3 называет это ЖУРНАЛОМ, а не
  состоянием на сегодня.
- **Товар — единственный элемент склада без входа.** Расходник вёл
  в карточку, ёмкость — в контроль вскрытия, товар не вёл никуда, хотя
  выглядел такой же строкой. Элемент, который выглядит нажимаемым и не
  нажимается, читается как поломка, а не как «сюда нельзя».
- **История грузится ПО НАЖАТИЮ.** Тридцать записей на пункт в месяц ×
  число пунктов — платить за это при каждом открытии экрана незачем.
  Тот же приём, что у колокола и общего поиска.
- **Имена исполнителей — через `compliance_actors`, а не связью
  к `profiles`.** Та отдаёт профиль ТОЛЬКО про себя (`profiles_self_read`,
  0001), и вложенная связь вернула бы null всем, включая владельца, —
  молча. Грабля уже описана в 0083 и повторилась бы здесь.

## М37. Реєстр модулів замість enum

**Состав:** `supabase/migrations/0110_module_registry.sql` ·
`supabase/tests/32_module_registry.sql` · `lib/modules.ts` (новый) ·
`lib/tenant.ts` · `components/app-shell.tsx` · `components/module-gate.tsx` ·
`app/app/layout.tsx` · правки девяти тестов под `text[]`.
**Миграция:** 0110. **Ручных настроек нет.**

**Откуда взялось.** Принцип продукта: функция подключается ДАННЫМИ,
а не выкатом. Измерено 19.08.2026: завести модуль стоило ВОСЬМИ правок,
три из них требовали выката кода вместе с миграцией. Стало ТРИ, и все
три про сам экран.

**Что переехало в базу:** список модулей, подписи, значки, адреса,
права, место в нижней панели, принадлежность к набору по умолчанию.

**Грабли:**

- **Две функции с одним именем ломают вызов литералом.** Соблазн оставить
  и `tenant_has_module(uuid, tenant_module)`, и новую `(uuid, text)`
  кончается тем, что `tenant_has_module(id, 'inventory')` становится
  неоднозначным и падает на разборе политики. Старая снята, новая одна.
  Перед этим проверено запросом к `pg_policies` на бою: функцию
  не вызывает НИ ОДНА политика, то есть смена безопасна.
- **Сняв enum, надо заменить его сторожем.** Тип не пускал значение вне
  списка; `text[]` пустит опечатку `invetory`, и раздел просто не
  появится — а искать это будут в правах. Отсюда `tenants_modules_guard`.
- **Умолчание колонки СНЯТО намеренно.** Держать список и в `default`,
  и в реестре значит воспроизвести ровно ту копию, ради устранения
  которой всё делалось. Набор проставляет триггер из `is_default`.
- **Тест изоляции поймал новое `using(true)`** на реестре — и правильно:
  исключение обязано называться явно, список в `06_isolation.sql`
  закрытый. Реестр в него добавлен пятым справочником с объяснением.
- **Девять тестов сравнивали набор с `tenant_module[]`** и упали на смене
  типа. Это не помеха, а признак живых тестов: они держали контракт
  и заметили его изменение.
- **`modules || \'inventory\'` на `text[]` не работает** — литерал
  разбирается как массив. Нужен `::text`.
- **⚠️ ПОРЯДОК ВЫКАТА ОБРАТНЫЙ ОБЫЧНОМУ.** Здесь код ТРЕБУЕТ таблицу:
  без реестра навигация останется без разделов. Миграция при этом
  совместима с уже развёрнутым приложением (формат токена не изменился —
  `to_jsonb` от `text[]` тот же, что от enum-массива). Поэтому сначала
  миграция, потом выкат. Применено на бой и проверено запросом:
  оба заведения сохранили наборы полностью, неизвестных кодов нет.

## М38. Свій склад: фото, нотатка і власні поля засобу

**Состав:** `supabase/migrations/0111_material_photo_note_attributes.sql` ·
`supabase/tests/33_material_photo.sql` · `app/app/inventory/material-form.tsx` ·
`app/app/inventory/materials/[id]/{page,material-card}.tsx` ·
`app/app/inventory/{page,inventory-client}.tsx` · ключи
`inventory.material.own.*`, `.photo.*`, `.note.*`, `.attrs.*`.
**Миграция:** 0111. **Ручных настроек нет.**

**Откуда взялось.** Владелец 19.08.2026: «Человек может на склад добавить
расходники — перчатки, растворы, — загрузить картинку. Описание дать,
кастомизировать, заметки сделать, вписать всё что угодно. Это его склад».

**В чём была дыра.** Паспорт по Техрегламенту №65 в карточке есть весь
и обязателен для всех — но СВОЕГО у продавца не было ничего: ни фото,
ни заметки, ни места под собственное поле.

**Грабли:**

- **Произвольные поля — `jsonb` с проверкой типа.** Без ограничения туда
  однажды ляжет `[]` или строка, и экран, обходящий ключи, молча покажет
  пусто. Тот же приём, что у характеристик каталога (правило 4).
- **Путь к фото сторожится В БАЗЕ, а не только политикой хранилища.**
  Политика следит за ЗАГРУЗКОЙ; колонка хранит ССЫЛКУ, и чужой путь
  можно было бы записать обычным UPDATE, не трогая хранилище вовсе.
- **Инспектор видит фото, но НЕ видит заметок и своих полей.** Заметка —
  внутренняя кухня заведения («брать у другого поставщика»), и в режиме
  проверки ей делать нечего; произвольные поля могут содержать что
  угодно, включая цены. В `compliance_materials` этих колонок нет вовсе,
  и тест проверяет именно состав представления.
- **Колонка в представление добавляется ТОЛЬКО В КОНЕЦ.** `create or
  replace view` требует того же порядка ведущих колонок; вставка
  в середину даёт «cannot change name of view column». Грабля из 0106,
  повторилась бы здесь.
- **Нет фото — значок, а не серый прямоугольник.** Пустая рамка читается
  как «картинка не загрузилась», то есть как поломка. По той же причине
  не сделан блок 4:3 из README: его пришлось бы показывать и тем,
  у кого фото нет, отдавая треть первого экрана серому месту.
- **Старое фото удаляется при замене.** Одна картинка на засіб; оставленный
  файл стал бы невидимым мусором навсегда — его уже ничто не найдёт.

## М39. Витратники на послугу: рецепт на картці послуги

**Состав:** `app/app/catalog/[id]/recipe-block.tsx` (новый) ·
`app/app/catalog/[id]/page.tsx` · ключи `catalog.recipe.*`.
**Миграций нет.** **Ручных настроек нет.**
**Зависит от:** `variant_materials` и `consume_materials_for_variant`
(0003), политики на `catalog.*` (0075), `set_booking_status` (0003).

**Откуда взялось.** Владелец 19.08.2026: «Он открывает услугу и
прикрепляет расходник, определяет, сколько уходит на услугу расходника.
И всё считывается, а потом сколько услуг было проведено, сколько
расходников было истрачено».

**В чём была дыра — и это не та дыра, о которой думаешь.** Механизм
списания есть с 0003 и работает: `set_booking_status` при переводе записи
в `completed` сам зовёт `consume_materials_for_variant`, расход ложится
в журнал движений обычным списанием, тест это покрывает. Не хватало
не механизма, а ВХОДА в него: завести рецепт можно было только
на отдельном экране склада (`/app/inventory/recipes`) — другой раздел,
два перехода от услуги. На боевой базе рецептов было ровно НОЛЬ.
Функция существовала и не существовала одновременно.

**Грабли:**

- **Права рецепта — `catalog.*`, а не `stock.*`.** Политики 0075 стоят
  на правах каталога, потому что рецепт принадлежит УСЛУГЕ. Первый
  вариант этого экрана держал кнопки на `stock.write`, и это давало обе
  ошибки сразу: у кого есть `catalog.write` без склада — блок только
  для чтения при разрешённой записи; у кого наоборот — форма есть,
  а база отказывает.
- **Без `stock.read` блока нет ВОВСЕ, а не «пусто».** Список засобів
  пришёл бы пустым по RLS, а уже заведённые строки рисуются по этому же
  списку — и нарисовались бы голыми идентификаторами. Пустой список
  ещё и врёт: «На складі немає засобів» вместо «вам сюда нельзя».
- **Расход берётся из ЖУРНАЛА, а не из «рецепт × число визитов».**
  Умножение дало бы красивое число, расходящееся с остатком: рецепт
  мог меняться между визитами, а списание могло быть погашено встречным
  движением. Журнал — единственный источник правды об остатке (правило 5),
  и цифра на экране обязана приходить оттуда же, откуда остаток.
- **Ноль расхода не показывается.** «Витрачено 0 мл» рядом с нормой
  читается как поломка списания, а не как «ещё не проводили».
- **Уже добавленный засіб убирается из выбора.** Пара «вариант × засіб» —
  первичный ключ; вторая попытка упёрлась бы в отказ базы вместо
  понятного «уже є».
- **Товары намеренно не подключены.** Расход по рецептуре у товара
  тоже мыслим, но списывать его надо в момент ПРОДАЖИ, а `set_order_status`
  этого не делает вовсе. Показать блок у товара значило бы обещать
  списание, которого не произойдёт.

**Что осталось названным, а не сделанным:** заказы расход не списывают
(`set_order_status` не зовёт `consume_materials_for_variant` — в отличие
от записей). Это отдельное решение: у заказа нет момента «выполнено
руками мастера», и списывать надо либо при отгрузке, либо при оплате.

## М40. Склад целиком: учёт продаж, себестоимость, перемещение, дайджест, экраны

**Состав.** База: `0112_receipt_costing.sql`, `0113_stock_relocation.sql`,
`0114_order_ships_stock.sql`, `0115_reorder_digest.sql`; тесты `34`–`37`.
Код: все экраны `app/app/inventory/**` (движения, приёмки, инвентаризации,
рецепты, штрихкоды, «пора замовити», карточка засоба, документы, PAO,
формы), `components/confirm.tsx` (новый), пять иконок в
`components/icons.tsx`, кнопка дайджеста в `lib/email/queue.ts`,
удалён `mailReorderDigest` из `lib/email/templates.ts`, ~80 ключей
`uk.json`. **Ручных настроек нет** (cron-задания ставят сами миграции).
**Зависит от:** журнал `stock_movements` (0003), очередь уведомлений
(0011), правило получателей 0048, `stock_low_view` (0009).

**Главная находка — продажа не списывала товар НИКОГДА.** Найдено сверкой
«функция ↔ вызывающие»: `commit_reservation` лежала мёртвой с 0003.
Заказ резервировал (`create_order` → `reserve_stock_internal`), отмена
освобождала, а выполнение не делало НИЧЕГО: ни движения `'sale'`,
ни снятия резерва. Возврат при этом клал товар НА склад (0103) — продать
и принять назад значило +1 из воздуха. Теперь переход в «передано
перевізнику» коммитит резервы строк; резерв, истёкший до отгрузки,
списывается прямой веткой «без резерву» — факт отгрузки первичен.
Вторая половина той же дыры: `expires_at` ставился, а не гасил его никто —
брошенный гостевой заказ держал товар недоступным навсегда. Гасит
`expire_stale_reservations` раз в 15 минут.

**Грабли:**

- **Мёртвая функция не находится тестом «функция существует».** Все три
  резервных функции существовали и были покрыты грантами — но
  `commit_reservation` не звал никто, и фильтр «продажі» в журнале был
  вечно пуст. Проверка обязана идти от СЦЕНАРИЯ (оформил → отгрузил →
  остаток упал), а не от наличия объектов; тест 36 ровно такой.
- **Себестоимость — средневзвешенная, и строка без цены её НЕ трогает.**
  «Последняя цена победила» врёт при закупках разными партиями, а ноль
  вместо «не знаю» размывает среднее вникуда. Если старой цены нет —
  берётся цена строки целиком. Ручная правка остаётся: последний, кто
  знал факт, побеждает. Пересчёт стоит ДО `record_stock_movement` своей
  строки — формуле нужен остаток до прихода.
- **Перемещение — это пара `transfer_out`/`transfer_in` с нулевой суммой.**
  Модель мест одноместная (0009), поэтому переносится позиция целиком.
  Сами типы лежали в enum мёртвыми с 0003 — фильтры журнала по ним всегда
  возвращали пусто. Перенос нуля движений не пишет (знаковая проверка
  запрещает ноль) — меняется только колонка.
- **Дайджест — одно письмо в сутки, дедупликация ключом с ДАТОЙ.**
  `reorder:<tenant>:<дата>:<user>` — повторный запуск в тот же день
  молчит, завтрашний шлёт новое. Пустой дайджест не шлётся вовсе:
  «у вас всё хорошо» учит не открывать письма.
- **`to_char(x, 'FM…')`, а не trim-хак.** `trim(trailing '0')` из «100»
  делает «1» — почти уехало в письмо клиенту.
- **`window.confirm` в обёртке читается как сбой** (системное окно
  с адресом сайта в заголовке) и не переводится. Теперь один
  `useConfirm()` (`components/confirm.tsx`) — шторка с настоящими
  подписями; у разрушающих действий `tone: 'danger'`.
- **Скелетон стареет молча.** `loading.tsx` склада рисовал `.stat-tile`
  со значками на 2 колонки, когда экран давно рисует `.metric` без
  значков на 4, и в обратном порядке секций. Правило одно: переделал
  экран — переделай скелетон тем же коммитом. Теперь скелетон есть
  у ВСЕХ 12 маршрутов склада.
- **13 экранов показывали сырой `error.message` с сервера.** Серверные
  страницы передавали текст Postgres пропом в клиент — мимо М25. Теперь
  на сервере `dbErrorText(await getT(), error)`. Экранные переводчики
  (`humanize`) сохранены — заменён только их запасной путь.
- **Утилизация ёмкости остаток НЕ списывает — это названный вопрос,
  а не забытый.** Объём банки (compliance-слой) и количественный журнал
  материала не связаны нигде: связать их — отдельное решение владельца
  о двойном учёте, «дописать списание» здесь означало бы списывать
  дважды с расходом по рецептуре.

**Не сделано:** переименование записей справочников (деактивация есть);
частичное перемещение количества (требует остатков по местам — другая
модель учёта); дайджест для предупреждений о сроке ёмкостей (осознанно:
у них срок отправки в будущем, склейка = перепроектирование очереди).

## М41. Юзер-составляющая: вход, регистрации, смена пароля/почты/телефона, выход

**Состав.** База: `0116_email_change_sync.sql`, тест `38`. Код:
`lib/auth-errors.ts`, все `app/(auth)/*` (`/reset` УДАЛЁН), новая
`app/auth/confirm/`, `app/account/security`, `app/app/profile/*`,
`app/m/{login,register,shop}`, `components/app-shell.tsx` и
`settings-client` (signOut), `app/privacy/delete`, `mailEmailChange`
в `lib/email/templates.ts`, ~19 ключей uk.json.
**Ручные настройки:** Supabase → URL Configuration → Redirect URLs
добавить `https://cres-ca.com/auth/confirm`; Authentication → Emails →
Change Email Address — вёрстка `mailEmailChange` (ссылка
`{{ .ConfirmationURL }}`, НЕ код — у смены почты ДВА подтверждения).

**Главные находки аудита 19.08.2026:**

- **Смена почты не завершалась НИКОГДА.** Экран был, тексты обещали
  «листи на обидві адреси» — но ссылка из письма вела на несуществующий
  маршрут, а `profiles.email` синхронизировался только `after insert`:
  после смены приглашения, доступ инспектора, сторож команды и письма
  работали по СТАРОМУ адресу молча. Закрыто: триггер
  `on_auth_user_email_changed` (0116) + страница `/auth/confirm` +
  `emailRedirectTo` в обоих экранах смены.
- **«Вийти» гасил сессии на ВСЕХ устройствах.** В supabase-js v2
  умолчание `signOut()` — `scope: 'global'`: вышел на ноутбуке — вылетел
  на телефоне. Все пять точек выхода теперь `{ scope: 'local' }`,
  а «на всіх пристроях» — отдельная кнопка с подтверждением.
- **Вебового пользователя без заведения уносило в мобильный экран.**
  `nextRoute()` без аргумента поверхности даёт `'m'` — и `/login`,
  `/register`, `/forgot` слали человека на `/m/shop` со 100dvh и без
  навигации сайта. Веб обязан звать `nextRoute(supabase, 'web')`.
- **Регистрация продавца была сломана трижды:** сырой текст Postgres
  на экран (в т.ч. значение поля при нарушении уникальности — класс
  утечки М25), НОЛЬ строк согласия в `user_consents` (signUp без
  `options.data` → триггер молчит), и тупик «Підтвердіть пошту за
  листом» без поля кода — а в письме код, не ссылка. Переписана:
  шаги аккаунт → код → заведение.
- **Тупик «почта уже занята»** на веб-регистрации: для подтверждённой
  почты GoTrue письма не шлёт, человек вечно ждал код. Проверка
  `identities.length === 0` → карточка со ссылками на вход
  и восстановление (как давно сделано в `/m`).
- **Английские строки GoTrue текли с каждой кнопки «надіслати ще раз»:**
  «For security purposes…» не знал ни один переводчик. `humanAuthError`
  теперь переводит её со сроком, различает «новый пароль совпал со
  старым» (раньше врал «пароль закороткий»), знает reauthentication,
  а неузнанное отдаёт общей подписью + консоль — НЕ сырым текстом.

**Грабли:**

- **`user_consents.source` — check `('web','ios','android')`.**
  Захардкодить `signup_source: 'seller'` значит уронить триггер
  `handle_new_user` и ВСЮ регистрацию продавца. Источник — только
  `signupSource()`.
- **`profiles.email` закрыт от прямой правки сторожем** (0116, дописан
  в `profiles_guard` 0109 с переносом ветки is_staff): почта меняется
  только процедурой GoTrue с двумя подтверждениями. Имя и телефон
  правятся свободно (self-update RLS) — для них шторка «Особисті дані».
- **После сброса пароля сессия УЖЕ есть** (`verifyOtp` type recovery) —
  кнопка «Увійти» врала; правильный уход `nextRoute(supabase, 'web')`,
  а не жёсткий `/account` (владелец салона попадал в кабинет покупателя).
- **`/reset` удалён по правилу 8** — маршрут `/auth/callback`, ради
  которого он жил, не существовал; попасть туда было нечем. При
  удалении вычищать ссылки: `robots.ts`, регексп `lib/ratelimit/rules.ts`.
- **Повтор кода не должен тратить второй счётчик `guardSignIn`** —
  два повтора съедали предел 5/15хв; повтор заперт местным таймером 60с.

**Не сделано (названо):** аватар (колонка `avatar_url` мертва — политики
хранилища требуют тенантный префикс пути, аватар пользователя вне
арендатора — отдельное решение); список СВОИХ сеансов (данные есть
в `team_sessions`, экрана нет); вход по телефону (`DOMAIN.md:202` —
нужен SMS-провайдер, решение владельца); подтверждение смены телефона
SMS-кодом (канал SMS честно не подключён — телефон пока контактное
поле, не логин); `ru/en` словари auth-разделов.

## М42. Обёртка по скринам владельца: клавиатура, отклик, шапка, пуши, сканер

**Состав.** База: `0117_seller_push_and_scan_target.sql`, тест `39`.
Код: `app/globals.css` (шторка/отклик/подвал), `components/app-shell.tsx`
(порядок шапки), `components/scanner.tsx`, `components/native.tsx`,
`app/m/onboarding.tsx`, `app/app/inventory/inventory-client.tsx`,
`app/app/inventory/barcodes/{page,barcodes-client}.tsx`, ключи uk.json.
**Ручные настройки:** `NEXT_PUBLIC_ONESIGNAL_APP_ID` в Vercel ДО сборки;
`ONESIGNAL_APP_ID` в группе `push` Codemagic; APNs `.p8` и Firebase
Service Account в кабинете OneSignal (docs/STORE.md).

**Грабли:**

- **Шторка ужималась, но не поднималась.** Клавиатура ложится ПОВЕРХ
  страницы (`resize: 'none'`), `--kb` меряется honest — но у `.sheet`
  стоял `bottom: 0`: высота уменьшалась, а нижняя часть с полями
  и кнопкой оставалась ФИЗИЧЕСКИ под клавиатурой. Правильно —
  `bottom: var(--kb)`; `max-height: calc(92dvh - var(--kb))` при этом
  не даёт шторке вылезти за верх.
- **Отклик нажатия — только у действий.** Общий `:active`-scale стоял
  и на `.card-flat` (любая серая карточка «проседала», читаясь кнопкой),
  и на `.sheet-backdrop` — а это кнопка НА ВЕСЬ ЭКРАН: тап по затемнению
  сжимал весь фон, по краям вспыхивали светлые полосы.
- **Пуши на Android не работали из-за ОДНОЙ строки.** Push-эффект
  выходил по `isNative()`, а мост Capacitor на Android при удалённом
  `server.url` не инжектится ВООБЩЕ (М6) — `OneSignal.login` не звался
  никогда, external_id не ставился, каждый пуш уходил в failed.
  Остальной файл давно использовал `nativeish()` — забыли ровно
  push-эффект. Платформа теперь определяется по НАШИМ мостам
  (`window.AndroidOneSignal`), не по Capacitor.
- **Продавец не получал пуш о заказе ВООБЩЕ** — `enqueue_staff_alert`
  жёстко ставила только email, хотя весь нативный слой (перехват на
  переднем плане, переход по data.url) писался под этот сценарий.
  0117 ставит обе строки; у пуша СВОЙ ключ дедупликации (`:push:`),
  иначе ON CONFLICT съел бы вторую строку. Пуш-канал без шаблона — это
  очередь failed: шаблоны добавлены той же миграцией.
- **Камера перезапускалась на каждый рендер родителя.** Инлайн-колбэки
  в зависимостях эффекта: любой refresh глушил поток и звал getUserMedia
  заново (который может устройство и не выдать). Колбэки — в ref,
  зависимость — только `open`.
- **Скан без выхода — читается как «сканер не работает».** Результат
  «товар/засіб» был справкой без единой кнопки; «код не знайдено» —
  тупиком. Теперь: карточка засоба / карточка каталога (0117 отдаёт
  `offering_id`; `returns table` расширяется только drop+create,
  и search_path 'extensions' из 0084 обязан быть выставлен заново —
  иначе citext-сравнение снова регистровое и мимо индекса), промах —
  переход на привязку с `?code=`. Отказ RPC отделён от «не найдено».
- **`resize: 'none'» и порядок шапки** — решения владельца сведены
  в CLAUDE.md; календарь ВЕРНУЛСЯ левым якорем (вторая поправка дня).

**Названо, не сделано:** web-push в браузере (OneSignal Web SDK +
worker — работы с нуля); `patch-ios.sh` (NSCameraUsageDescription живёт
только в codemagic.yaml — локальная сборка iOS упадёт на камере);
алерт по `notification_outbox.status='failed'`; добавление строки
в открытую инвентаризацию со сканера (состав документа фиксируется
при старте — менять его со сканера значит менять модель документа).

## М43. CRESKO Web: десктопный кабинет (этап 1 — палитра и каркас)

**Состав.** `app/globals.css` (блок «CRESKO WEB»: переопределение токенов
в `[data-webapp]` на lg+, `--web-*`, классы `.webtop/.webside/.webdrop/
.webmain`), `components/app-shell.tsx` (веб-хедер 72px, сайдбар 232px
в порядке README, дропдаун профиля), `components/sheet.tsx` (скролл-лок
`position: fixed`). Хендофф лежал вложением у владельца; значения README
внесены дословно. **Ручных настроек нет.**

**Решение владельца:** десктопный кабинет — ВТОРОЙ визуальный язык
(светлая тема, акцент `#7C3AED`); мобильный не трогается. Записано
в CLAUDE.md, «Внешний вид».

**Грабли:**

- **Перекраска — переопределением ТОКЕНОВ, а не второй разметкой.**
  Внутри `[data-webapp]` на lg+ те же `--color-*` получают значения
  хендоффа: все существующие экраны перекрашиваются сами. Вторая копия
  экранов разъехалась бы с первой на первой же правке.
- **Тёмной темы у веб-версии НЕТ в хендоффе** — блок действует только
  вне `.dark`; тёмная тема на десктопе остаётся прежней (кобальт).
  «Дорисовать тёмный фиолетовый самому» — это ровно то подбирание
  на глаз, которое хендофф запрещает.
- **`lg:webmain` НЕ работает**: варианты Tailwind применимы только
  к utility-классам, свой класс с `lg:` молча игнорируется. Свой
  класс — всегда, а lg-условие — внутри CSS через media.
- **Шторка+клавиатура, вторая итерация.** Подъём всей шторки
  (`bottom: var(--kb)`) открыл ЩЕЛЬ с просвечивающим затемнением —
  «чёрный слой под клавой». Правильно: низ на нуле,
  `padding-bottom: var(--kb)` поднимает СОДЕРЖИМОЕ, фон шторки
  закрывает зону за клавиатурой сам.
- **Скролл при открытой шторке прятал клавиатуру**: overflow: hidden
  не запирает НАТИВНЫЙ скролл документа в веб-вью, а у того dismiss
  по скроллу. Лок — `position: fixed` на body с сохранением позиции.

**ЧЕК-ЛИСТ ЭКРАНОВ CRESKO Web** (веди его здесь — следующий чат
продолжает с первой строки «⛔», НЕ переделывая ✅):

| README § | Экран | Состояние |
| --- | --- | --- |
| каркас | палитра, хедер 72, сайдбар 232, дропдаун | ✅ этап 1 |
| 1 | Сьогодні: H1+дата, 6 карток (записи/терміни/закупівля/фінанси+спарклайн/замовлення/нагадування) | ✅ этап 2 — данные реальные, «нагадування» = очередь notification_outbox |
| 8 | Склад: метрики .wmetric-фильтры, .wtabs, грід-таблиця, btn-blue «Приймання» → receipts?new=1 | ✅ этап 2 — колонки «Категорія/Місце» заменены на «Тип/Термін» (категорий и мест в пропсах списка нет; фикстуры запрещены) |
| 9 | Картка товару (стат-рядок, партії, історія руху) | ⛔ |
| 10 | Модалка «Приймання» (490px, 4 секції) — сейчас ?new=1 открывает существующую шторку | ⛔ (шторка работает, hi-fi модалка не сделана) |
| 12–13 | Журнали, журнал стерилізації | ⛔ |
| 14 | Документація (5 метрик + таблиця) | ⛔ |
| 3 | Клієнти + права панель 398px | ⛔ |
| 16 | Співробітники + панель | ⛔ |
| 17 | Налаштування (9 рядків + панель 420px) | ⛔ |
| 5–7 | Техкарти + картка + візард | ⛔ (раздел /app/techcards есть, вид мобильный) |
| 2 | Календар (тижнева сітка) | ⛔ |
| 4, 11 | Послуги / Каталог сітками | ⛔ |
| 15 | Фінанси (метрики+тренд, графік доходу) | ⛔ |
| 18 | Публічна сторінка бізнесу | ⛔ |
| 19 | Замовлення (метрики+таблиця) | ⛔ |
| — | права рейка складу (швидкі дії/останні рухи/топ категорії) | ⛔ — «Швидкі дії» на lg скрыты, «Ще у складі» и fab оставлены видимыми, чтобы не отрезать входы |

**Этап 1 сделан:** палитра, хедер (лого 232 / поиск по центру /
колокол / профиль-кластер), дропдаун (262px, пункты по README минус
«Тарифи» — экрана нет), сайдбар с порядком README и help-картой НЕ
добавленной (телефона поддержки не существует — фейк хуже пустоты),
контент 28/32/40.
**Дальше по README, экран за экраном:** 1 Сьогодні → 8–10 Склад/картка/
модалка приймання → 12–14 Журнали/Документація → 3, 16, 17 Клієнти/
Співробітники/Налаштування (правая панель 398–420) → 5–7 Техкарти →
остальные. Грид-таблицы fr-колонками, метрики 25px/800, табы
с border-bottom 2px — примитивы заводить в первый экранный заход.

<a id="п-а"></a>
## П-А. Postgres-грабли

- `citext` живёт в схеме `extensions`: в сигнатурах — `extensions.citext`.
- `lower()` ДО `translate()`: иначе транслитерация съедает первую заглавную
  кириллическую букву («Пробник» → «robnyk»).
- **Право отзывается ТРЕМЯ строками: у `public`, у `anon` И у `authenticated`.**
  Новая функция получает EXECUTE для PUBLIC от самого Postgres, а Supabase
  сверх того держит `alter default privileges` на схеме `public` и выдаёт
  права ролям `anon` и `authenticated` НА КАЖДЫЙ НОВЫЙ ОБЪЕКТ отдельно
  от PUBLIC. Поэтому `revoke … from public` не отбирает ничего у них двоих,
  а `grant select` НЕ СУЖАЕТ уже выданный ALL — он только добавляет право,
  которое уже есть.
  Ловилось четыре раза, и каждый следующий раз выяснялось, что вывод
  из предыдущего был уже, чем грабля: 0036 (чинили только `anon`),
  0061 (выяснилось, что ролей две), 0094 (то же на триггерных функциях),
  0095 (то же на ПРЕДСТАВЛЕНИИ — и там это была настоящая дыра, см. ниже).
  Правило: сначала три `revoke`, потом одно `grant` на то единственное,
  что нужно. Порядок обратный ничего не отзывает.
- **То же и с ПРЕДСТАВЛЕНИЯМИ, и там дороже.** У представления нет RLS,
  а простое представление автообновляемо: UPDATE через него доходит до
  таблицы правами ВЛАДЕЛЬЦА, мимо политик и без WITH CHECK. `v_bookings`
  и `v_orders` держали у `authenticated` INSERT/UPDATE/DELETE, которых
  не выдавала ни одна миграция: право ЧИТАТЬ давало право ПРАВИТЬ
  и УДАЛЯТЬ. Источник — `alter default privileges … grant all …
  to anon, authenticated` в облаке Supabase; седьмой случай подряд.
  Отзывать разом по схеме, а не поимённо: список имён устаревает в день,
  когда заведут следующее представление.
- **`create or replace function` поверх чужой функции стирает тело целиком.**
  Читать действующее и переносить руками всё, чего не собирался трогать
  (как потеряли ветку INSERT сторожа членств — М17).
- **Ранний выход триггера по списку колонок стареет молча.** Новая колонка
  просто перестаёт покрываться, и тест этого не увидит.
- **Действие, которое ЗАВОДИТ строку, без ключа идемпотентности в очередь
  не кладётся.** Розлив (`decant_container`) уменьшал объём родителя И
  создавал новую ёмкость со своим кодом; ключа у него не было. Сеть рвётся
  и ПОСЛЕ записи — транзакция прошла, ответ не доехал, — значит досылка
  отлила бы второй раз и завела в реестре соответствия банку, которой нет
  на полке. Порядок в функции важен: поиск по ключу стоит ДО первого
  `update`, иначе повтор всё равно спишет объём. Форма — как у
  `stock_movements` (0003), второй способ не заводится (0100).
- `using (true)` в RLS = утечка всех арендаторов (так утекли графики).
- NOT NULL без default на «кто сделал» — проверять ВСЕ пути записи,
  особенно фоновые и отложенные.
- EXCLUDE-ограничение на «ресурс × отрезок времени» — двойная запись
  невозможна физически.
- Ошибочная строка журнала гасится встречной, не правится.

<a id="п-б"></a>
## П-Б. Среда Claude-агента

- Пуш в GitHub из сессии невозможен (403, многократно). Код — архивом
  через веб или на папку устройства через мост.
- **Рабочая копия в контейнере ОТСТАЁТ от `main`.** Заливка «поверх»
  тихо откатывает чужие правки: в диффе этого не видно, обнаруживается
  через неделю. Перед сборкой набора сверять каждый файл с `main`
  и обновлять копию.
- **Заливать ВЕТКОЙ и одним слиянием, а не серией коммитов в `main`.**
  Каждый коммит в `main` — производственный деплой, и промежуточное
  состояние кладёт домен в красное: файл, на который ссылается уже
  залитое, едет следующим коммитом (три красных деплоя подряд 16.08.2026).
- npm недоступен, собрать нельзя: типы верны С ПЕРВОГО РАЗА (проверять
  tsc --noEmit по файлам без резолва). Сборку до слияния проверяет
  задание «Збірка» (`.github/workflows/build.yml`) — `npm ci`,
  `next build`, потом `tsc --noEmit` списком всех ошибок; Vercel
  отвечает уже после деплоя, то есть поздно.
- Supabase MCP работает: миграции — напрямую в прод; источник правды —
  живая схема, не локальная папка миграций.
- ...но применённую миграцию НАДО тем же заходом положить файлом
  в `supabase/migrations/`. Сверка 11.08.2026: на проде нашлись
  `0023_notifications_delivery`, `0024_account_deletion` и
  `register_tenant_slug_case_fix`, которых в git не было ни в одном
  коммите. `0024` — это `delete_my_account()`, требование Apple 5.1.1(v):
  потерять её значит не пройти ревью и не понять почему. Восстановлено
  из `supabase_migrations.schema_migrations.statements` — там лежит
  полный текст. Расхождение ловится за минуту: `list_migrations` против `ls`.
- Схему сверять поколоночно, а не по числу столбцов. Расхождение
  `materials`/`stock_receipts` (на проде 20/10 против 21/11 «по миграциям»)
  оказалось не дрейфом, а `drop column supplier` в 0009. Любой отчёт,
  собранный парсером по `create table`, врёт на первой же миграции
  с удалением колонки.
- Чинишь баг — сначала проверь ЖИВОЕ состояние (базу, триггеры), а не
  вчерашний отчёт: половина «дыр» уже закрыта, а настоящая
  (неподключённая очередь) в отчёте выглядела готовой фичей.
- Локальная папка пользователя может отставать от контейнера: перед
  правкой файла сверять версии (mtime/размер), после работы — синхронизировать.
