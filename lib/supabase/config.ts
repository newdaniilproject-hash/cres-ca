// Адрес проекта и публикуемый ключ.
//
// ⚠️ ЗНАЧЕНИЯ ЖИВУТ В `shared/supabase-config.ts` — их читает и мобильное
// приложение. Здесь только перевыдача, чтобы не править полтора десятка
// импортов `@/lib/supabase/config` по всему кабинету. Второй копии адреса
// и ключа в проекте нет и заводить её нельзя: разъехавшийся адрес — это
// приложение, которое молча ходит в чужую базу.
export { SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY } from '@/shared/supabase-config'
